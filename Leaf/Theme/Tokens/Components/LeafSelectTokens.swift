//
//  LeafSelectTokens.swift
//  Track 2 / D1 — T3 tokens for LeafSelect. Sub-variants pick native Picker styles:
//  dropdown/.menu, combobox/.menu (editable variant deferred to D2 if needed),
//  segmented/.segmented.
//

enum LeafSelectTokens {
    enum Variant {
        case dropdown
        case combobox
        case segmented
    }
}
