//
//  LeafToolbarTokens.swift
//  Track 2 / D1 — T3 tokens for LeafToolbar. Surfaces horizontal/vertical
//  paddings + slot spacing so toolbar geometry stays consistent across
//  Activity / Team / Connections / Settings top bars.
//

import CoreGraphics

enum LeafToolbarTokens {
    static let horizontalPadding: CGFloat = LeafSpace.lg
    static let verticalPadding: CGFloat = LeafSpace.md
    static let slotSpacing: CGFloat = LeafSpace.md
}
