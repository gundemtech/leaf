//
//  LeafTabTokens.swift
//  Track 2 / D1 — T3 tokens for LeafTab. Surfaces tab spacing + indicator
//  geometry so the segmented tab strip stays consistent across surfaces
//  (Settings sub-section nav, Team sub-views).
//

import CoreGraphics

enum LeafTabTokens {
    static let tabSpacing: CGFloat = LeafSpace.lg
    static let labelToIndicator: CGFloat = LeafSpace.xs
    static let indicatorHeight: CGFloat = 2
}
