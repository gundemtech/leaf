//
//  LeafSectionTokens.swift
//  Track 2 / D1 — T3 tokens for LeafSection. Title / description type tokens
//  surfaced for downstream override; CTA slot is generic on the consumer.
//

import SwiftUI

enum LeafSectionTokens {
    static let titleFont: Font = LeafType.title.medium
    static let descriptionFont: Font = LeafType.body.small
}
