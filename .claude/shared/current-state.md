# Текущее состояние проекта

_Срез "где мы сейчас" за 30 секунд. Детали — git log + `docs/superpowers/specs/` + `.claude/plans/`. История — whitepaper changelog._

## Последнее обновление

**✅ Use-case coverage UC-1/2/3/5 (2026-06-11, #61 `fd67857b`; moat `1fec107`).** Сайтовые промисы → продукт (план `.claude/plans/use-case-coverage.md`): **UC-1 Search-вкладка** (сайдбар-роут над тем же `QueryEngine`, что MCP: decisions first c links-пилюлями → questions/blockers → FTS events; `SearchResultsComposer` TDD; окно 90д) · **UC-2 Nudges** (`DerivedInsights.nudges` + moat-фидер: stalled-PR ≥3д без merge, stuck-ticket = выход `LinearStuckScanner` впервые в UI, open blockers, high switch-rate; Home-карточка «visible only to you») · **UC-3 Brief** (`BriefComposer` TDD: PRs merged across N repos / tickets done / decisions / blockers resolved за 5д; self-loading блок; v1 = локальная память, team-mirror агрегация — follow-up) · **UC-5 decision recall** (триггеры детектора расширены EN/RU, FP-корпусы зелёные; диагноз: recall был ≈0 — корпус тонкий, решения команды живут в Telegram (не источник), Slack не подключён). **UC-4 handoff card** — payload-протокол, вопрос партнёру задан (`q_2026-06-11_dima_anton_519c19ad`), отдельной фазой после ack.

**✅ Home redesign — MVP-presentable (2026-06-11, #60 `d018667b`).** 4 зоны вместо 6 дублирующихся карточек: **NOW hero** (слияние RESUME + YOU'RE ON: task · session · files · WIP + CTAs + YOU·NOW бейдж; **trunk-suppression** — «N ahead of main» только на фича-ветках, nil-branch fail-quiet, «Diff with main» гейтится тем же правилом) · **TODAY** (focused = headline + 7d focus-спарклайн Swift Charts из `weeklyMetrics.dailySeries`) · **NEEDS YOU** (чипы реально фильтруют, честные per-filter счётчики — `InboxFiltering` заменил стаб IV.A.1; search убран; **title enrichment в moat** `fb7bacb` — Linear/GitHub фидеры джойнят локальные issue/PR тайтлы: «LEA-200 · Fix relay reconnect») · **WHILE YOU WERE AWAY** (кап 8, мета без дублей тайтла, «Detected by Leaf» вместо Track-1 D3; `uniqueKey` теперь с targetTitle) · TEAM-пустышка = компактная строка. Adversarial review (2 MEDIUM + 3 LOW) закрыт. **Попутный фикс: `Package.swift` dep-edge `LeafCoreTests → LeafCorePrivate`** (вопрос партнёра 2026-06-02; fresh-clone CI ловил «no such module» по build-order лотерее). Known-стаб: `whereStopped` nil (IV.A.1) — anchor/commit-строки hero оживут с `recentWhereStopped`. UI-смоук на Debug-билде за юзером.

**📋 Use-case coverage план (2026-06-11)** — 6 сайтовых промисов → `.claude/plans/use-case-coverage.md` (UC-1 in-app search S → UC-2 nudges → UC-3 Monday brief → UC-5 MCP polish → UC-4 handoff card, последний из-за payload-протокола). E2E-находка: MCP tools живые (blockers вернули реальные linear_stuck), но **DecisionDetector recall ≈ 0 на реальных данных** — `leaf_get_decision` null на любой топик; тюнинг паттернов = часть UC-5.

**✅ Team → Workspace Hub + Chats (2026-06-10, #53 `cdacc399` + #56 `4ff6d79f`).** Сайдбар: воркспейс-лист → компактный пикер (триггер-карточка + кастомный дропдаун + persistent profile-строка; Profile ушёл из ACCOUNT). Team-страница = хаб: шапка (LeafWorkspaceMark + Invite ⌘N) + табы **Chats / Members / Settings**. Chats — двухпанельный чат-менеджер: список диалогов (поиск, Activity-псевдочат = старая лента, unread-бейджи) + реальная 1:1 переписка (composer, реплаи-цитаты через прошитый `replyTo`, read-receipts, Task-баблы Open/Done; обычные сообщения едут kind `ping` — серверный CHECK, отдельный kind `message` = бэкенд-follow-up после Path B). Members/Settings-табы = переехавшие секции из Settings → Workspace (Settings теперь device-only: Privacy/Notifications/Data/General); join-request push → Team→Members. LeafCore TDD: `WorkspaceHubPresentation`, `ChatListPresentation`, conversation-запросы стора. **Каналы/группы невозможны до бэкенда** (парная E2E-крипта). Team-as-chat = whitepaper-уровень — синк в leaf-docs ПОСЛЕ ревью партнёра. Known bug ленты (прод): Today-секция с «1mo ago» строками — таймстамп-рассинхрон day-секционирования Linear-событий, отдельный фикс.

**✅ Live tabs (2026-06-10, #55 `90c6e2f4`).** Табы обновляются вживую без перезахода: Home/Activity/Analytics — `DatabaseChangeObserver` (LeafCore; debounced DispatchSource-watcher на `events.sqlite`+`-wal`, ловит кросс-процессные записи агента) → `LiveUpdateSignals` (`@Observable` счётчики) → non-force `InsightsReader.refresh()` с **warm refresh** (`.loaded`/`.empty` держатся на экране, `.error` держится на ambient — ушли и старые блинки при перезаходе); Team-лента — `onMirrorChanged`-хуки на DM/team-event ридерах (absorb/tick/mark-done) → warm refresh ленты, скролл на месте; новые Chats (#56) живые через `.onChange(inboxReader.recentMessages)`. Watcher живёт только при открытом окне (0 fd/wakeups при закрытом), RootView не тронут. LeafCore TDD (9 тестов). Подписка ленты пережила #56-рефактор (TeamView-оркестратор). **UI-смоук deferred** — проверить на бандл-билде перед Sparkle-шипом. Спек — `docs/superpowers/specs/2026-06-10-live-tabs-design.md`. (Team polish v1 #50 `a855ce7b` — поглощён хабом #56; `TeamFeedPresentation` TDD-логика живёт.)

**✅ E2E team-messaging две стороны заработали (2026-06-07, two-Mac live verified).** Два бага под одним симптомом «сообщения не ходят», оба пофикшены: **alpha.28 (#38 `92c5c677`)** — приёмный путь (`members.first` = создатель, не self → C2-гейт молча дропал; фикс — резолв self по identity-pubkey ×3 сервиса) и **серверный hotfix в ПРОДЕ (leaf-relay `ffc4175`, локально)** — отправка джойнера (инвайти не было в серверной `workspace_members`; `approve_join_request` теперь вписывает через service-role + backfill `20260607120000`). 🔴 **leaf-relay: 70 локальных коммитов НЕ в origin/main** (Path B, принято/отложено). Хвосты: ротация R2/CF ключей; site-redeploy.

**🏁 AI Coworker трек ЗАКРЫТ — P5 Verifiable (v1.5) landed (2026-06-06, #35 `53c7d9d0`, moat `d661d0c`).** Client-side attestation seam + рабочий PoC-verifier: opt-in `InferencePath.attested` (open-weight, verify-before-send, fail-closed, assurance `.poc`); Claude-пути остаются default trust-based (frontier+attestable сегодня не совмещаются). Verify-core публичен+тестируем (freshness+signature-vs-injected-root+constant-time-measurement+TLS-SPKI-binding); pins/endpoint/`AttestedSummarizer`/transport — moat (placeholder, fail-closed до Tinfoil-аккаунта). Honest-claim: НЕ маркетим «verifiable» пока `.poc`. Audit-DB/MCP-tool/M033 + real-Tinfoil-формат/VCEK+Rekor → deferred прод-вайринг. **`.attested` built+tested+injectable, НЕ runtime-wired** (зеркалит AI-included).

**Гигиена (Ph C, 2026-06-02) держится:** трунк объединён, enforced:
- **Один транк `dev`**; `main` = release-теги (сейчас `v1.0.0-alpha.24` @ `c54a6cef`). `git branch -a` = **3 уникальных имени** (`dev`, `main`, `feature/moat-leaf-private-bootstrap`). 13 local + 15 remote merged/folded веток снесены — контент сохранён (в `dev` либо в тегах `dev-track7-source` + `archive/*`).
- **`just preflight`** = phase-done gate (R1): leak-guard + check-tokens + check-migrations + gitleaks + build-all (5 схем, exit-honest) + SPM-тесты.
- **pre-push hook** (`.githooks/pre-push` → leak-guard, `core.hooksPath`; R2) + **gitleaks** (pinned binary + `.gitleaks.toml` allowlist) в CI+preflight (D-C3).
- **check-migrations.sh** (R3, линейность Mxxx). **migration-guard** (R7): несовместимая БД → `DatabaseRecoveryView` (Backup&Reset/Reveal/Quit), Agent `exit(0)`, не «Couldn't load Home». **agent-watchdog (#49 `28511065`):** location-guard SMAppService-регистрации (BTM-hijack копией из /tmp/DMG невозможен) + self-heal loop в main app (kickstart → reregister → escalate, intent-флаг; Diagnostics: launchd-строка + Repair).
- Правила гигиены **R1–R8** + phase-close чеклист в `conventions.md`.

## Где мы

- **Whitepaper v0.1-beta** на `leaf-docs.gundem.tech`. Решения — `docs/reference/decisions.md`, OQ — `open-questions.md`.
- **Foundation + Distribution + Layer B** (Linear/GitHub/Slack OAuth+polling), **Phase 4.6/4.7 A–C** (event_kinds + `presence_state` + MCP), **Phase 5.1–5.3/5.5** (E2E team relay: invite/rotation/removal) — shipped, в трунке.
- **Tracks 1–5 + integration-T10 P1–P7** влиты в трунк (integration-канон): T1 capture/FTS/detectors · T2 design tokens · T3 Linear/GitHub/Slack depth · T4 Layer A collectors + intensity · T5 Supabase backend + multi-workspace + magic-link + DM + broadcast + tier-gate · T10 Claude/Xcode/Browser/Zoom/IDE deep. **Ждут two-Mac signed-build acceptance gate** (работа в `dev`, не в отдельных ветках).
- **alpha.24** signed; grant-once daily-driver работает.
- **AI Coworker P0–P5 — ВСЕ ЗАКРЫТЫ (трек завершён 2026-06-06).** Граница §8.1 (`LLMPolicy.makeContext`/`makeEscalation` + opaque `PromptSafeContext`/`EscalatedBodies`, fail-closed) + `Summarizer`/`ModelGate`/`InferencePath` seam (P0/P0b). Live consumers: MCP `ask_about_my_work` (P1, BYOK-only), synthesis-кластеры (P2), `escalate_to_ai`+`get_ai_escalation_log` (P3, первый bodies-путь, audit-first, M031, prompt-injection §8 п.3), team-handoff «Draft with AI»+`get_handoff_log` (P4, M032, первая in-app AI-поверхность). P5: attested-путь (см. «Последнее обновление»). **Pending two-Mac signed-build smoke** (TCC/Keychain/APNs). Канон+все follow-ups — `.claude/plans/2026-05-31-ai-coworker-track.md` (§13.11).
- **AI-UI-1..3 landed (2026-06-10, #51 `772ee9bc` + #54 `9af1a3b2` + #57 `9a9eccd0`)** — in-app трек поверх AI Coworker. **AI-UI-1**: вкладка **Ask Leaf** (chat-Q&A, BYOK) + Settings «AI Answers» (BYOK-ключ, `FileAnthropicKeyStore`) + `AIWiring`. **AI-UI-2**: **escalation-модалка** (`EscalationSheet`, Activity multi-select + Ask Leaf «Dig deeper»; consent-preview = бит-в-бит send-путь, audit-first `AIDetailAnswerer`) + **Privacy-ленты «AI received»/«AI handoffs»** (M031/M032) + оживлён Activity Raw events (`ActivityFeedQuery`). **AI-UI-3 (умный handoff)**: prompt seam `HandoffPromptMoat` (prod-текст в leaf-private), period-picker в Draft with AI, **escalation-в-черновике** («Add details (AI)…» → consent-шаг по паттерну EscalationSheet → redraft с телами; audit-first M031 в `HandoffDrafter`, fail-closed без sink'а), **NL-вход** «передай коллеге…» в Ask Leaf (`HandoffIntentParser` EN/RU, локальный, ростер активного воркспейса; suggestion-плашка → prefilled шит), **pickup «Context for me»** на входящем handoff (`InboundHandoffExplainer` + `LLMPolicy.makeInboundHandoff` — чужой текст только как labeled data, имя отправителя не в инструкции; пустой свой контекст → no POST); фикс: чат-меню «Handoff…/Task…» открывает шит с нужным kind; epoch-guard в `HandoffDraftReader` против late-completion. LeafCore TDD; adversarial review (7 findings) закрыт. **AI-UI-4 (AI-included) landed (2026-06-11, #59 `b3909ea5`) — in-app AI трек ЗАКРЫТ:** все поверхности работают без BYOK-ключа через командный пул — `AIBackendRouter` (per-call BYOK-клапан: ключ → Anthropic, иначе `.aiIncluded` через relay-прокси) + `SupabaseSessionTokenProvider` (ensureAuthenticated→ensureFreshSession; на pre-login dev = anonymous-сессия, login-трек ужесточит без изменения кода) + typed `AIFailure` (CTA по kind, path-aware тексты: 402 на пуле предлагает свой ключ) + Settings «included via your team plan». `path: .byok`-хардкоды убраны — M031/M032 пишут реальный путь. Декаплено от `feature/account-login-phase1`; ветка `origin/feature/ai-ui-4-ai-included` (закрытый #58) = референс conflict-резолюций dev↔login для партнёра. **🚫 Sparkle-ship-gate: relay `/v1/ai/*` не существует** — до его деплоя keyless = честный transient-fail. **Deferred:** clickable smoke + live-ответы на LEAF_PROD (все фазы). Спеки — `docs/superpowers/specs/2026-06-10-ai-ui-{1-ask-leaf,2-escalation-privacy,3-smart-handoff}-design.md`; план — `.claude/plans/archive/ai-ui-4.md`.

## Архитектура

Полная — `architecture.md` + whitepaper `memory-architecture/`. TL;DR: MCP primary + Native macOS app + E2E team relay (Cloudflare DO → Supabase миграция отдельным треком). Layer A (NSWorkspace/CGEventSource/EventKit/Focus/AX/FSEvents/Claude Code hooks/AppleScript/intensity/browser/GoogleCalendar) + Layer B (Linear/GitHub/Slack), L1–L5 granularity, Share Controls (default OFF), **SQLCipher M001–M032**, zero-LLM substrate, Sparkle 2 + EdDSA + R2. **20 MCP tools** (+`ask_about_my_work`/`escalate_to_ai`/`get_ai_escalation_log`/`get_handoff_log`), **189 ShareEventTypeKey**, multi-workspace per device, tier-gate (`tier` default `"team"` MVP). AI Coworker = первая in-app AI-поверхность (P4): `HandoffDrafter`/`HandoffDraftReader`. P5: opt-in `InferencePath.attested` (verifiable open-weight, off-by-default, не runtime-wired) — public verify-core + moat pins.

## Следующим

- **Release-pipeline + changelog трек**: Ph0 (#42) + Ph1 (#43) + **Ph2 landed (#44 `303cea0b`)** — Ph0 `derive-version.sh` + `CHANGELOG.md`; Ph1 `gen-release-notes.sh`→committed `releases.json` + Sparkle `--html-only` + freshness-guard; **Ph2** `bump-version.sh` (atomic ×6 pbxproj + partial-bump guard) + `release.sh` рефактор (`assert_secrets` SSM-fail-fast, `--until` publish-gate, hardened `step_appcast` glob-restore+trap+`--embed-release-notes`, `assert_releases_json_matches_version` Ordering-инвариант **enforced in code**) + `upload-release.sh` (releases.json→root LAST + stable `Leaf.dmg`+`.sha256` + purge) + `/release-leaf` runbook + `just release`/`secrets-refresh`. Тулинг only (16-agent adversarial review → 7 findings fixed, mutation-verified); **live ship = `/release-leaf` под SSM `/leaf/release/*` сидингом (Prereq, prod-RW оператор) — НЕ выполнен**. **Ph3 landed (#45 `43278542`)** — LeafCore Changelog (SemverPrerelease alpha.N-by-Int + ReleaseNote/Catalog tolerant + WhatsNewTracker onboarding-gated, 28 tests) + in-app What's New sheet (Settings button + RootView auto-present markSeen-on-show + Sparkle `willInstallUpdate` suppress; releases.json auto-bundled, no pbxproj/migration). **Ph4 landed (leaf-web #1 `047e417` + leaf #46)** — build-time `version.ts`+committed `src/data/releases.json` (4 `alpha.15` хардкода→derived, homepage static) + dashboard live-fetch history (XSS-safe textContent + href-allowlist) + CSP `connect-src updates.gundem.tech` ×2 + `deploy.sh` fail-loud; leaf `step_site` копирует releases.json→leaf-web. Обе фазы adversarial-reviewed (SHIP); Ph3 ci green (parsed). **Deferred:** Ph3 signed two-Mac smoke (first-present/markSeen/Sparkle-suppress); Ph4 R2 CORS + live VPS deploy; Ph2 live ship (SSM-сидинг + `/release-leaf`). План local `.claude/plans/release-pipeline-changelog.md`.
- **Tracks 1–5 + integration-T10 acceptance gate** (two-Mac signed-build smoke) → whitepaper sync + VPS deploy (Supabase Edge Functions + migrations — см. specs/plans).
- **Track-7/9 detail-UI + GoogleCalendar-UI re-apply** на unified trunk — отдельный post-Ph-C трек. Источник: теги `dev-track7-source` + `archive/code-style-phase-2-3-C` (Track-7 P1–P5 detail screens).
- **leaf-private follow-up**: `SlackD3SmokeInspector.testInspectLiveDB` + 2 stale handshake integration-теста → catch `databaseSchemaFromFuture`/skip + org→workspace API refresh. NB: локальная dev `events.sqlite` может быть pre-unification-несовместима с трунком (migration-guard на ней корректно срабатывает; recovery/wipe — решение владельца машины).
- **Cleanup**: delete `KeychainKeyStore.swift` после stable runtime; leaf-relay README + CI deploy hook.
- **AI Coworker P5 прод-вайринг (deferred)**: Tinfoil-аккаунт + real SEV-SNP/Sigstore parser + реальные pins (measurement/root/endpoint) + VCEK→AMD-chain + Sigstore-Rekor (→ assurance `.full`) + M033 `llm_attestation_audit` + `get_attestation_log` + live routing `.attested` через тулы + EU-residency. До этого `.attested` = built+tested, не live. leaf-relay attested-backend (`/v1/ai/*` → TEE) — в `gundemtech/leaf-relay`.
- **AI Coworker follow-ups (то, что осталось после AI-UI-1..4)**: header-bell+счётчик (сейчас reuse inbox badge) · Slack temporal-B deriver (substrate-blocked) · array-body kinds на escalation · detector-excerpt-by-id · per-source forward-deny (§13.3) · AI-UI-4 ревью F4–F6 (`AIFailure.Kind.notEnoughData` taxonomy, login-aware Settings-лейбл, Opus-пикер на included молча клампится в Sonnet). **whitepaper §6 team-handoff + AI-included как МОДЕЛЬ — amendment ПОСЛЕ ревью партнёра** (internals/E2E/M031/M032 — НЕ синкаем).
- **leaf-relay Worker `/v1/ai/*`** (SigV4→Bedrock + per-team budget + JWT local-verify) — отдельный трек в `gundemtech/leaf-relay`; клиентский контракт зафиксирован (`RelayProxySummarizer`). До деплоя = Sparkle-ship-gate для AI-UI-4 + альфа-калибровка пула (§13.11) по реальному использованию.
- **GitHub commit-subject self-authored** (P1 CR-1 deferred): per-commit author==viewer verification → тогда `self_authored_commit` снова идёт.

## Open tensions

- **OT-1** distributed deletion локальной forever-history у ex-teammates.
- **OT-2** storage compression для forever retention.
