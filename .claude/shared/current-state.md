# Текущее состояние проекта

_Срез "где мы сейчас" за 30 секунд. История — в whitepaper changelog + `.claude/plans/phase-*.md` + git log._

## Последнее обновление
2026-04-30 — **Phase 4.6.C.2 done — `longestUninterruptedWindow` метрика (popover + новый MCP tool).** Commits `450f890` (contract+snapshot field) + `ff691dd` (UI/MCP surface) на `main` (НЕ pushed — pre-push approval pending). Новая derived метрика — самое длинное окно в period'е без events из Linear/GitHub/Slack (proxy для "deep async work session" — integration silence, не macOS-level idle, отличается от `focusSessions`). Single SQL query через window function LEAD() + period-anchored sentinel rows: gap может ограничиваться bounds period'а (если первый event сильно позже — gap from period.start; если последний сильно раньше — gap to period.end). **Popover**: новая строка `Deep window: Xh Ym` под `activityLine`, threshold ≥ 30min (короче — noise filter, hidden). Tooltip `Longest gap with no Linear/GitHub/Slack activity today (HH:MM–HH:MM). Active sources today: …`. **MCP**: 8-й tool `get_uninterrupted_window` (period today/yesterday/last_7_days), payload additive `{start, end, durationSeconds, sourcesActive[]}`, version=1 preserved. **Public types**: новый `UninterruptedWindow {start, end, durationSeconds, sourcesActiveInPeriod}` Codable+Sendable+Hashable; `DerivedInsights.longestUninterruptedWindow(period:)` с default nil impl (graceful для StubInsights / iOS-future); `InsightsSnapshot.longestUninterruptedWindow: UninterruptedWindow?` (default nil). Tests: 273 SPM green (+9: 6 SQL aggregation в moat — empty/no-events/single-mid/multi/gap-at-start/non-LayerB-noise filter; +3 public — snapshot default-nil + explicit value + StubInsights default), 6 xcodebuild green Debug+Release × 3 targets. **E2E smoke pending** (юзер делает сам). Tactical: `.claude/plans/phase-4-6-C-2.md`. **Pending**: pre-push-leaf check + push в `gundemtech/leaf:main` (require user OK) — bundle с `c9d6363` + 4.6.C.1 + 4.6.C.2 commits, docs sync в leaf-docs changelog, продолжение 4.6.C.3 (per-provider streaks) + 4.6.B (Linear transitions) → alpha.7 ship.

2026-04-30 — **Phase 4.6.C.1 done — surface global `weekOverWeekDelta` (popover + MCP).** Commits `07b3b71` (breakdown contract) + `f7f7312` (UI/MCP surface) на `main` (НЕ pushed — pre-push approval pending). Backend `weekOverWeekDelta() -> Double?` существовал с Phase 2.2 — теперь visible в двух surface'ах. **Popover**: ветка trends compact "Peak X · WoW +12% · Active N" → промоутировано: WoW вытащен в отдельную проминентную строку `activityLine` с arrows + полным контекстом ("Activity ↑12% vs last week" / "↓5%" / "no change"). Hidden gracefully когда baseline < 7 дней или prev week zero (`nil`). `trendsSummaryLine` теперь компактнее `Peak 14:00 · Active 5 days`. **MCP**: `get_linear_activity` / `get_github_activity` / `get_slack_activity` JSON responses расширены additive optional полем `wowDelta: Double` (тот же global scalar, дублируется в трёх tools — это global attention-time trend, не per-provider). Schema-compat: version=1 preserved, older clients ignore unknown keys. **Per-provider WoW (`linearWoWDelta`/etc) — out of scope этой micro-phase**: `wowDeltaPct: Double?` поле добавлено как reserved contract в LinearActivityBreakdown / GitHubActivityBreakdown / SlackActivityBreakdown, populated as nil сейчас, готово под 4.7+. Tests: 264 SPM green (+6 round-trip на breakdown wowDeltaPct: 3 default-nil + 3 explicit value включая negative -0.05 и zero 0.0 boundary), 6 xcodebuild green Debug+Release × 3 targets. **E2E smoke pending**: визуальная проверка popover на real workspace + MCP probe — юзер делает сам. Tactical: `.claude/plans/phase-4-6-C-1.md`. **Pending**: pre-push-leaf check + push в `gundemtech/leaf:main` (require user OK), сделать docs sync в leaf-docs changelog, продолжение 4.6.C.2 (`longestUninterruptedWindow`) + 4.6.C.3 (per-provider streaks) + 4.6.B (Linear status transitions) → alpha.7 ship.

2026-04-30 — **Phase 4.6.A.3 done E2E (Slack reactions + huddle session stats — Phase 4.6.A полностью закрыт по всем трём провайдерам).** Commit `3a01866` на `main` (НЕ pushed — pre-push approval pending). Public types: `SlackChannelMessageCount.reactionsCount: Int` (default 0), `SlackActivityBreakdown.reactionsReceived: Int?` + `.huddleSessionStats: LatencyStats?`, `InsightsSnapshot.slackReactionsReceived: Int` + `.slackHuddleSessionStats: LatencyStats?`. Reactions через **conversations.history** (НЕ search.messages — last не возвращает reactions field by design): per unique channel_id обнаруженный в search.messages → conversations.history call с `oldest=since&limit=100`, filter `msg.user==self_user_id` client-side, aggregate `reactions[].count` numeric only. Cost: +N API calls per tick (N≈1-5 unique channels с моей activity), Tier 2+ headroom 1000/min × 50. **Breaking scope change**: `userScopes` += `channels:history,groups:history,im:history,mpim:history` — existing alpha.6 tokens требуют OAuth re-consent (Disconnect → Connect). Slack OAuth app dashboard обновлён вне репо (юзер сам). Huddle distribution через sibling `walkHuddleSessions` static func рядом с `walkHuddleSegments` (Phase 4.4) — переиспользует existing `huddle_state_change` events array без duplicate fetch, emit'ит `[Int]` per-session durations в seconds → `LatencyStats.from(samples:)`. Surface: popover Slack-tooltip две новых rows `Reactions: N` и `Huddle sessions: N · avg: Xs` (hidden если ноль/нет samples), MCP `get_slack_activity` JSON additive optional `reactionsReceived` + `huddleSessionStats` (version=1 preserved). Schema additive — payload `reactions_count` ключ присутствует только если provider насчитал > 0 (различает "не знаем" от legitimate zero), no migration. ADR-010 enforced на parser-level (count only, никаких emoji names / member IDs / users[] / message text / permalinks) + verified inline через SQL inspect post-E2E (payload содержит `reactions_count: "3"` numeric, без PII). Tests: 258 SPM green (+5 reactions через conversations.history fixture path — happy + ADR-010 negative + per-channel + defensive decoding + auth-error graceful no-op; +4 huddle distribution + boundary clipping + reactions/huddle independence; +2 InsightsSnapshot round-trip). 6 xcodebuild green Debug+Release × 3 targets. **Real-workspace E2E smoke verified**: после OAuth re-consent с новыми scopes, fresh message + 3 self-emoji реакций → next tick `reactionsReceived: 3`; ~41-сек huddle pair (in_a_huddle 11:42:05 → default_unset 11:42:46) → `huddleSessionStats: {sampleCount: 1, avgSeconds: 41, maxSeconds: 41}`. SQL inspect post-E2E ADR-010 sanity passed. Whitepaper sync: `## Патчи v1.10` запись в `leaf-docs/docs/05-reference/changelog.md` (НЕ pushed — pending). Tactical: `.claude/plans/phase-4-6-A-3.md` с Implementation deviation секцией про conversations.history refactor. **Pending**: pre-push-leaf check + push в `gundemtech/leaf:main` (require user OK), push `leaf-docs:main`, продолжение Phase 4.6.B (Linear status transitions) + 4.6.C (synthesis: surface `weekOverWeekDelta` + `longestUninterruptedWindow` + per-provider streaks) — alpha.7 ship после B+C.

2026-04-30 — **Phase 4.6.A.1 + 4.6.A.2 done E2E (Layer B latency depth, alpha.7 in flight).** Commits `4bb07e7` (GitHub PR cycle + review delay) + `e99d36b` (Linear completion duration) на `main` (НЕ pushed — pre-push-leaf check pending). Public types: новый `LatencyStats { median, avg, max, sampleCount }` shared helper для всех latency aggregations; `GitHubEventSnapshot.cycleSeconds` / `.reviewDelaySeconds`, `LinearIssueSnapshot.completionSeconds` (optional, defaults nil); breakdown'ы расширены additive optional полями `prCycleStats`, `reviewDelayStats`, `completionDurationStats`. Surface: popover tooltip rows (`PRs merged: N · avg cycle: …`, `Reviews: N · avg wait: …`, `Closed: N · avg duration: …`) + MCP `get_github_activity` / `get_linear_activity` JSON responses. Все три hidden если `sampleCount=0`. Schema-compat: payload JSON additive, ключи `cycle_seconds` / `review_delay_seconds` / `completion_seconds` присутствуют ТОЛЬКО если producer заполнил (отличаем "не знаем" от instant-complete на 0s). Provider dedup в Linear через cursor — completion sample учитывается только когда `completedAt` упал в polling window (post-completion активность не пересчитывает). Discovery — **parser regression в Phase 4.3 fixed inline (moat)**: `/users/<login>/events` REST feed нормализует squash/rebase merges как `action: "merged"` (не `closed + merged: true` как webhook), Phase 4.3 PullRequestEvent parser handle'ил только `closed`-ветку → все merged PRs тихо скипались. Без этого фикса 4.6.A.1 cycle metric никогда бы не считался. Парсер дополнен третьим case `"merged" → pr_merged + cycleSeconds`. Tests: 246 SPM green (+9 новых: 5 LatencyStats edge cases — empty samples → nil, even-count median, instant-complete с 0s legitimate sample; 2 GitHubCollector encoding latency-fields в payload — nil → ключ ОТСУТСТВУЕТ а не пустая строка; 2 LinearCollector encoding completionSeconds). 6 xcodebuild green Debug+Release × 3 targets. **Real-workspace E2E smoke verified**: temp PR в `gundemtech/leaf-relay` (private sandbox) → 51-сек cycle от open до squash-merge → next polling tick подцепил PullRequestEvent action=merged (после parser fix) + PushEvent → popover показал `GitHub today: 2 events · gundemtech/leaf-relay` с populated `prCycleStats`. Linear issue без перевода в Done → `completionDurationStats: nil` (negative test — code path correct, samples=0 → строка hidden). Whitepaper sync: `## Патчи v1.10` запись в `leaf-docs/docs/05-reference/changelog.md` (НЕ pushed — pending). Tactical: `.claude/plans/phase-4-6.md`. **Pending**: pre-push-leaf check + push в `gundemtech/leaf:main` (require user OK), push `leaf-docs:main`, продолжение Phase 4.6.B (Linear status transitions) + 4.6.C (synthesis: surface `weekOverWeekDelta` + `longestUninterruptedWindow` + per-provider streaks) — alpha.7 ship после A+B+C.

2026-04-29 — **Phase 4.5 done E2E (Linear per-action attribution fix).** Commit `9c1d618` pushed в `gundemtech/leaf:main`. Bug: Phase 4.2 query `issues(filter:{updatedAt:{gt:$since}})` workspace-wide → teammate updates засчитывались как user actions в popover и `get_linear_activity` MCP. Fix: `IssueFilter.activity:{some:{user:{isMe:{eq:true}},createdAt:{gt:$since}}}` + `creator.isMe.eq` backstop в `or` ветке — server-side фильтр к viewer, коллег в response просто нет. Bootstrap path consolidated: `since=nil` → effectiveSince=now-7d локально, single query shape всегда (раньше bootstrap была без filter — заливала 50 случайных issues). One-time migration в `LinearCollector.start()` через UserDefaults flag `linear.attribution_v2_migrated`: wipe `events WHERE source=linear AND signal_type=action` + reset `collector_offsets WHERE collector_id=linear_polling` атомарно через `Database.purgeLinearAttributionV2()`. GitHub/Slack — out of scope (`/users/<login>/events` и `from:me` фильтруют viewer на API level). Tests: 6 LinearCollector (включая 2 migration) + 7 ProdLinearGraphQLProvider (включая 2 query-shape snapshot), 6 xcodebuild green. Real-workspace E2E verified — popover counter coheres with own actions, teammate updates invisible. Whitepaper sync: leaf-docs `0dacb10` (admonition v1.5 + changelog v1.10). Tactical: `.claude/plans/phase-4-5.md`. **Pending**: alpha.7 ship — отдельный цикл.

2026-04-29 — **Phase 4.4 done E2E** (Slack OAuth + collector + MCP + UI + HTTPS relay). B1-B8 code (commit `f2359c2`) + handoff state (`4cd1f0a`) + relay switch (`ddd47b0`). Layer B MVP теперь Linear+GitHub+Slack. 295 SPM tests, все 6 xcodebuild green Debug+Release × 3 targets. Real-workspace smoke прошёл по 12 пунктам плана `phase-4-4.md` § 6 — Connect, message events в 2+ channels (DM anonymized как "DM"), huddle on/off context events, popover показывает `Slack today: M msgs · K channels · huddle Xm`, MCP probe `get_slack_activity` отдаёт structured JSON, disconnect и reconnect-needed flow работают. **HTTPS relay solution:** stateless Cloudflare Worker на `oauth.gundem.tech/slack/callback` (приватный репо `gundemtech/leaf-relay`) 302's на `http://127.0.0.1:47824/callback?code=...` к loopback listener'у — обходит Slack distributed-app HTTPS requirement без shipping client_secret. `SlackOAuthEndpoints.redirectURI` теперь HTTPS relay; `loopbackHost/Port/Path` — отдельная сущность для bind'а listener'а. Worker invisible для Mac кроме redirect_uri value в /oauth/v2/authorize и /oauth/v2/access.

2026-04-29 (earlier) — **Shipped 1.0.0-alpha.6** (Phase 3 distribution полностью закрыт E2E). Sparkle delta upgrade alpha.5 → alpha.6 (1.7 MB, EdDSA signed, notarized, stapled) verified на real machine: bridge migration idempotent (db.key байт-в-байт identical pre/post — `FileKeyStore.fetchOrCreate()` hit `readExisting` path), events history intact (events.sqlite size unchanged), main app + popover поднялись на 1.0.0-alpha.6 (build 7), Sparkle release notes отрендерились корректно. Notary submission `03f4af73-f2e5-4c61-b10f-2814e0cdfa42`.

2026-04-29 (earlier) — **Shipped 1.0.0-alpha.6** (Phase 3 distribution полностью закрыт E2E). Sparkle delta upgrade alpha.5 → alpha.6 (1.7 MB, EdDSA signed, notarized, stapled) verified на real machine: bridge migration idempotent (db.key байт-в-байт identical pre/post — `FileKeyStore.fetchOrCreate()` hit `readExisting` path), events history intact (events.sqlite size unchanged), main app + popover поднялись на 1.0.0-alpha.6 (build 7), Sparkle release notes отрендерились корректно. Notary submission `03f4af73-f2e5-4c61-b10f-2814e0cdfa42`.

## Где мы
- **Whitepaper v1.4** published в `leaf-docs.gundem.tech`. Структура `01-vision / 02-product / 03-architecture / 04-market / 05-reference`.
- **Section A done (Phase 0-2, 2026-04-23 → 2026-04-26).** Foundation: 3-target Xcode project (Leaf app + LeafAgent + LeafMCP), `LeafCore`/`LeafCorePrivate` SPM split, encrypted storage (GRDB 7 fork + SQLCipher AES-256, file-based key store), Agent daemon + 4 MVP collectors, Derived Insights Engine, MenuBarApp Native UI, stdio MCP server, maintenance scheduler. 135 SPM tests. Tactical: `.claude/plans/phase-2*.md`.
- **Section B done (Phase 3.0-3.5, 2026-04-27 → 2026-04-28).** Distribution: Apple Developer ID + notarytool + Sparkle 2 + R2/CF + EdDSA-signed appcast. Shipped **1.0.0-alpha.5**. Tactical: `.claude/plans/phase-3*.md`.
- **Phase 4.1+4.2 done (Linear OAuth + collector + insights, 2026-04-28).** PKCE loopback, `LinearCollector` 5min polling, `DerivedInsights.linearActivity`, MCP tool `get_linear_activity`, `.reconnectNeeded` UI. `ProdLinearGraphQLProvider` — moat stub. 161 SPM tests. Tactical: `.claude/plans/phase-4-{1,2}.md`. **Pending**: real prod GraphQL query, sync-docs.
- **Phase 4.3 done (GitHub Device Flow + collector + insights, 2026-04-29).** OAuth Device Flow (RFC 8628, no PKCE — OAuth Apps не поддерживают), `GitHubCollector` 5min polling REST `/users/<login>/events`, `ProdGitHubAPIProvider` (moat) с PushEvent/PR/Issue/Review mapping + ADR-010 enforcement. 6-й MCP tool `get_github_activity`. 169 SPM tests, build green Debug+Release × 3 targets. **Real-workspace smoke verified**: после Connect → 5min poll → popover показал `GitHub today: 4 events · gundemtech/leaf`. Schema gotcha обнаружен и пропатчен в moat: authenticated REST events feed для `/users/<X>/events` возвращает **stripped** PushEvent payload (без `commits[]` массива — только `before/head/push_id/ref/repository_id`), public feed и webhook payload — full с `commits[]`. Парсер теперь поддерживает оба shape'а — full → N snapshots по N коммитам с subject (ADR-010 first line), stripped → 1 snapshot c `sha=head`, `title=""` (commit message в feed недоступен — ADR-010 trivially enforced). Tactical: `.claude/plans/phase-4-3.md`.
- **Phase 4.4 done (Slack OAuth + collector + insights + HTTPS relay, 2026-04-29).** PKCE distributed-app flow через Cloudflare Worker relay (`oauth.gundem.tech/slack/callback` → 302 на `127.0.0.1:47824/callback`, приватный репо `gundemtech/leaf-relay`), 5min polling `users.profile.get` + `search.messages` (24 calls/hr под Tier 2+ headroom 1000×), huddle state derived из context events `huddle_state_change`. `ProdSlackAPIProvider` (moat) с DM anonymization → "DM" bucket до записи + ADR-010 bodies/permalinks/status_text discard pre-RawEvent. 7-й MCP tool `get_slack_activity`. MenuBar `slackLine` показывает `Slack today: M msgs · K channels · huddle Xm`. 295 SPM tests (217 public + 78 moat), build green × 6. Real-workspace smoke прошёл — connect, message events 2+ channels включая DM, huddle on/off, popover, MCP probe, disconnect, reconnect-needed. Tactical: `.claude/plans/phase-4-4.md`. **Layer B MVP closed** — Linear+GitHub+Slack доставлены.
- **Phase 4.6.A.1 + 4.6.A.2 done (Layer B latency depth, 2026-04-30).** Layer B перешёл от "что произошло + сколько раз" к "что произошло + насколько долго + какой темп". GitHub: PR cycle time (closed_at − created_at для merged) + review delay (review.submitted_at − pull_request.created_at). Linear: completion duration (completedAt − startedAt для closed issues) с dedup-guard через cursor. Generic `LatencyStats` aggregation, shared между провайдерами. Surface через popover tooltips + MCP additive optional поля. Schema-compat additive. **Discovery + fix**: Phase 4.3 PullRequestEvent parser handle'ил только `action: "closed"`, тогда как `/users/<login>/events` REST feed нормализует squash/rebase merges как `action: "merged"` — все merged PRs тихо скипались. Patch добавил `case "merged" → pr_merged` в moat. 246 SPM tests, 6 xcodebuild green. E2E: PR в `leaf-relay` (private sandbox) → 51-сек cycle подцепился, popover показал `GitHub today: 2 events · prCycleStats populated`. Tactical: `.claude/plans/phase-4-6.md`.
- **Phase 4.6.A.3 done (Slack reactions + huddle session stats, 2026-04-30).** Phase 4.6.A полностью закрыт по всем трём провайдерам (GitHub PR cycle / Linear completion / Slack reactions+huddle). Reactions через **conversations.history** (search.messages не отдаёт reactions field by design): per unique channel_id из search.messages → conversations.history?channel=X&oldest=since&limit=100, filter msg.user==self, aggregate reactions[].count. **Breaking scope change**: `userScopes` += channels:history, groups:history, im:history, mpim:history; existing alpha.6 tokens требуют OAuth re-consent. Huddle distribution через sibling walkHuddleSessions (no duplicate fetch). 258 SPM tests, 6 xcodebuild green. E2E: 3 self-emojis → `reactionsReceived: 3`; 41s huddle pair → `huddleSessionStats: {sampleCount: 1, avgSeconds: 41}`. ADR-010 verified via SQL inspect (только `reactions_count` numeric, без PII). Tactical: `.claude/plans/phase-4-6-A-3.md` с Implementation deviation секцией. **Pending**: 4.6.B (Linear status transitions) + 4.6.C (synthesis) → alpha.7 ship.
- **Shipped 1.0.0-alpha.6 (2026-04-29).** Phase 3 (distribution) полностью закрыт E2E. Sparkle delta upgrade alpha.5 → alpha.6 (`Leaf7-6.delta`, 1.7 MB, EdDSA signed) verified на real machine — bridge migration idempotent (db.key identical pre/post, никакой regen), events history intact, version `1.0.0-alpha.6 (build 7)` displayed. Phase 4.1+4.2+4.3 features доставлены alpha-юзерам.
- **Linear** = только таски. Второй мозг = whitepaper.

## Архитектура
Полная картина — `.claude/shared/architecture.md` + whitepaper `leaf-docs/docs/03-architecture/`. TL;DR: two surfaces (Native UI primary + MCP bonus), opt-in transparency + Share Controls, granularity L1-L5, Layer A (NSWorkspace/CGEventSource/EventKit/Focus/AX/FSEvents/Claude Code hooks), Layer B MVP = Linear+GitHub, presence relay через Cloudflare DO + AES-GCM, 14 SQLCipher tables, zero LLM в MVP, Sparkle 2 + EdDSA + R2 distribution.

## Следующим
- **Phase 4.3 follow-ups (low priority):** ✅ (a) PushEvent regression закрыт 2026-04-29 (moat-side `LeafCorePrivateTests/ProdGitHubAPIProviderTests`, 3 теста — full + stripped + whitespace edge case, 177 SPM total). (b) расширить `mapPullRequestEvent`/`mapIssuesEvent`/`mapReviewEvent` под потенциальный stripped shape (мы не verified — все 4 PR events в feed были старше cursor); (c) опционально fetch `/repos/<owner>/<repo>/commits/<head>` per stripped PushEvent для recovery commit subject — extra API call, на v1.1.
- **Phase 4.2 prod provider**: ✅ pagination follow через `pageInfo.endCursor` (safety cap 10 страниц) + URLSession DI для tests. `ProdGitHubAPIProvider` — single-page MVP, без pagination follow / ETag (v1.1).
- **Layer B MVP closed (Linear+GitHub+Slack).** Layer C (MCP-aggregator: Figma/Notion/Jira/Gmail/Calendar) — V1.5+ scope.
- **Phase 4.6 (Layer B Depth) — A полностью done, B+C ahead.** Sub-phases остающиеся под alpha.7 ship: 4.6.B Linear status transitions (новый event flavor `linear_status_transition` через GraphQL history fragment, `LinearTransitionBreakdown { started, completed, canceled, reopened }` + `linearCompletionRate`), 4.6.C synthesis (surface global `weekOverWeekDelta` + новые `longestUninterruptedWindow` + per-provider streaks `commitStreak`/`issueCloseStreak`/`huddleParticipationStreak`).
- **Ship alpha.7 с Phase 4.6** — recommendation плана. Phase 5 (presence relay) — отдельный alpha.8 ship.
- **leaf-relay репо housekeeping**: добавить README + CI deploy hook (Wrangler), документация по oauth.gundem.tech routing для будущих v1.1+ providers (Notion / Figma) — `oauth.gundem.tech/<provider>/callback` pattern уже зарезервирован.
- **Phase 3.5+ cleanup**: delete `KeychainKeyStore.swift` + tests + `LeafError.keychainUnavailable` после ~2 недель stable runtime alpha.6 у alpha-юзеров (target ~2026-05-13).
- **Release tooling fix**: `scripts/upload-release.sh` грузит только `*.{dmg,zip,delta}` в `/releases/`, а `generate_appcast` ставит release notes URL без `/releases/` префикса. Сейчас фиксится вручную через `aws s3 cp` HTML в R2 root. Патч для будущих ship-циклов — добавить `*.html` в whitelist + upload в R2 root.
- **Sparkle ship gotchas** (зафиксировано в alpha.5 → alpha.6 ship cycle): (a) Xcode Debug LeafAgent может залипнуть в launchd registration слот → SMAppService.register() в shipped Leaf.app filter "already registered", фоновый Agent не подменяется. Pre-ship discipline — `pkill -f "DerivedData.*LeafAgent"` если был active Xcode Run; (b) macOS TCC AX permission иногда требует toggle off+on после Sparkle bundle replace (CDHash меняется).
- **Stamps gotcha в release.sh** для следующего ship'а: `release.sh 1.0.0-alpha.X` без флагов пропустит archive/export/... если стампы alpha.5/6 ещё в `build/releases/.stamps/`. Использовать `--redo-from archive`, не `--clean` (чтобы сохранить prior-version `.zip` для delta-генерации).

## Open tensions
Живут в `leaf-docs/docs/05-reference/open-tensions.md`. Топ-2:
- **OT-1** distributed deletion локальной forever-history у ex-teammates.
- **OT-2** storage compression для forever retention.
