# Текущее состояние проекта

_Срез "где мы сейчас" за 30 секунд. Обновляется вручную._

## Последнее обновление
2026-04-28 (Phase 3.5 done — alpha.5 shipped через 3 ship iterations; bridge migration K1 → file validated на real bytes; Sparkle path функционален с alpha.5+)

## Где мы
- Инфраструктура команды готова: `leaf-docs` (whitepaper v1.4) + shared memory в Leaf-репо.
- **Whitepaper v1.4 published** в `leaf-docs.gundem.tech` (актуально с 2026-04-25 — Phase 2.3 Claude Code AI collaboration). Структура: 01-vision / 02-product / 03-architecture / 04-market / 05-reference.
- **Phase 0 done 2026-04-23.** 3-target Xcode project (app + Agent CLI + MCP CLI), local SPM `LeafCore` с public API-каркасом (enums, protocols, unimplemented stubs) + moat-target `LeafCorePrivate` с tracked Placeholder. Все 3 таргета собираются, helper-бинари embed'нуты в `Leaf.app/Contents/MacOS/`, CI workflow написан.
- **Phase 1.1 done 2026-04-24.** Storage foundation: GRDB 7.10 (plain SQLite, SQLCipher → Phase 1.5), `Database` класс с writer/reader режимами через `DatabasePool`, migration M001 (events table + 2 индекса), `KeychainKeyStore` для 32-byte key (готов к использованию в 1.5), moat-injection pattern (optional `Config/Local.xcconfig` → `#if LEAF_PROD`). 20/20 тестов зелёные.
- **Phase 1.2 done 2026-04-24.** Agent daemon: `@main enum AgentMain` с `RunLoop.main.run()`, `ActiveAppCollector` на `NSWorkspace.didActivateApplicationNotification`, `IdleCollector` на `CGEventSource.secondsSinceLastEventType`, `EventWriter` actor с batching + periodic flush, graceful shutdown на SIGTERM/SIGINT через `DispatchSource`, hardcoded Phase-1 blocklist. LaunchAgent plist embedded в `Leaf.app/Contents/Library/LaunchAgents/` через Copy Files phase. App: `@Observable LaunchAgentService` обёртка над `SMAppService.agent(plistName:)`, Settings с real toggle + status indicator.
- **Phase 1.3 done 2026-04-24.** Первая визуальная метрика: `timeInApp` реализован в moat (`ProdInsights` с LEAD-window SQL + Swift tail-bonus), остальные 11 Insights методов throw `.notImplemented`. Factory перешёл с `#if LEAF_PROD` на runtime DI (регистрация провайдера в `LeafApp.init()`) т.к. Xcode не прокидывает compilation conditions в SPM dependencies. MenuBarExtra style сменён на `.window`, popover показывает Today header + top-5 apps + SwiftUI Charts bar chart + warning banner когда Agent off. `InsightsReader @MainActor @Observable` с state machine (notConfigured/loading/loaded/empty/error), cancellation previous task при новом refresh, file-exists precheck. `AppNameResolver` (3-tier lookup + cache), `formatDuration`, `Database.readSQL` как public moat bridge. Новый test target `LeafCorePrivateTests` с Placeholder + gitignore pattern для dev-only тестов. 32/32 тестов в dev (вкл. 5 ProdInsightsTests), 27/27 public simulation.
- **Phase 1.4 done 2026-04-24.** stdio MCP server: JSON-RPC 2.0 поверх newline-delimited stdio (protocolVersion `2025-11-25`). Pure protocol layer в новом SPM target `LeafMCPProtocol` (15 unit-тестов: JSONRPC round-trip, MCP types, Dispatcher, Initialize/ToolsList/ToolsCall handlers). Binary в Xcode target `LeafMCP/`: `enum MCPLog` с `nonisolated static` методами (stderr writes, не unified log — Claude Code MCP debug pipe ожидает stderr), `actor StdioTransport` на `FileHandle.standardInput.bytes.lines`, `@main enum MCPMain` с `async main()`. Shutdown через stdin EOF (нет signal handlers — в async main без `RunLoop.main.run()` DispatchSource на `.main` ненадёжен). Tool `get_timeline(period: today|yesterday|last_7_days)` → `DerivedInsightsFactory.make(database:).timeInApp(period:)` через тот же runtime DI что у MenuBar. `claude mcp add --transport stdio leaf -- ...` → `✓ Connected`; Claude Code на "что я делал сегодня?" получает JSON с реальным top-apps. 47/47 тестов зелёные (32 existing + 15 MCP).
- **Phase 1.5 done 2026-04-24.** SQLCipher AES-256 включён. Собственный fork `gundemtech/GRDB.swift-sqlcipher` (tracks groue v7.10.0) с раскомментированным "GRDB+SQLCipher" блоком + `sqlcipher/SQLCipher.swift` 4.14.0 (Zetetic SPM). Ключ 32-byte raw keyspec в Keychain (`events.db.key`, `$(TeamID).tech.gundem.leaf` access group, `AfterFirstUnlockThisDeviceOnly`), `PRAGMA key = x'HEX'` bypass PBKDF2. Public API additive: `EncryptionOptions(keyProvider:preKeyPragmas:postKeyPragmas:)` + `Database.openFor*(..., encryption:)`. Plaintext → encrypted миграция автоматическая (detection по header `SQLite format 3\0`, rename в `.pre-sqlcipher.bak`, cleanup WAL/SHM, fresh encrypted DB). Xcode 16 framework auto-embed fix: `TARGET_BUILD_DIR = $(CONFIGURATION_BUILD_DIR)/$(TARGET_NAME)` для LeafAgent/LeafMCP + `LD_RUNPATH_SEARCH_PATHS += @executable_path/../Frameworks`. Build OK, 52/52 тестов зелёные (47 existing → encrypted path + 1 migration + 4 SQLCipher integration).
- **Phase 2.0 done 2026-04-24.** Hygiene pre-work перед 10× event-volume расширением (Phase 2.3 Claude Code jsonl + 2.4 FSEvents). Три механизма: (a) `MaintenanceScheduler` actor в LeafCore — два loop'а (WAL checkpoint + retention sweep) с `performCheckpoint`/`performRetentionSweep` split'ом от start/stop, инжектируемые интервалы, chunk-limit 5000. `stop()` делает `cancel()` + `await task.value` — ждёт in-flight `pool.write` т.к. GRDB не cancellation-aware. Initial delays: checkpoint полный interval, sweep — половина (cleanup в рамках сессии). (b) `Database.deleteEventsOlderThan(tsMs:limit:)` subquery-pattern (`DELETE WHERE id IN (SELECT...LIMIT)` — SQLCipher fork не имеет `SQLITE_ENABLE_UPDATE_DELETE_LIMIT`). Moat retention cutoff 180 дней / sweep 24h в `AgentThresholdsProd`. (c) `ToolResponseBuilder.versionedJSONResult(_:)` в `LeafMCPProtocol` добавляет `"version": 1` top-level в каждый tool payload (per-tool output schema, НЕ MCP protocol version). `GetTimelineTool` отрефакторен. `SignalHandlers` принимает `shutdown: @Sendable () async -> Void` closure вместо typed writer — расширяемо под future stoppable'ы; порядок maintenance → writer flush → exit. Build OK (все 3 Xcode target'а), 61 SPM-тестов зелёные (52 → 61, +9: 3 Database retention + 4 Scheduler + 2 ToolResponseBuilder).
- **Phase 2.1 done 2026-04-24.** Session analytics — focus + switches видимы локально и через MCP. (a) `DerivedInsights` protocol расширен 13-м методом `lastActivity(bundleID:) -> ActivitySnapshot?` (returns nil вместо throws — null = "нет данных" semantically valid). `FocusSession` public-Core type получил `appCount: Int` (default 1, back-compat init) — single-app vs scattered session distinguishable. (b) `ProdInsights` реализовал три метода: `focusSessions(period:)` — CTE с `LAG(ts) OVER` + inline `SUM(is_session_start) OVER` для session_id, post-process в Swift для dominant app + appCount; `contextSwitchRate(period:)` — switches/h, denominator = sum of focusSessions duration / 3600, edge 0 active hrs → 0.0; `lastActivity(bundleID:)` — простой ORDER BY ts DESC LIMIT 1. Internal overloads `(period:now:)` для test-friendly tail-bonus. Init расширен default args `focusSessionGapMs` (non-breaking — callsite'ы в LeafApp.swift/MCPServer.swift не трогаются). (c) `AgentThresholds` (public) + `AgentThresholdsProd` (moat) пополнены `focusSessionGapSec` / `deepSessionMinSec`. Defaults в weakDefaults — 600s / 1500s; в moat — 360s (= idleThresholdSec*2) / 1500s (Pomodoro). (d) Новый public-Core type `InsightsSnapshot` (topApps + sessions + switchRate + precomputed deepSessionsCount + avgSessionDuration). `InsightsReader.State.loaded` рефакторен на `InsightsSnapshot` value-type — foundation для 2.2 trends / 2.3 aiRatio расширений; refresh выполняет 3 sequential calls в одном detached Task. (e) MCP-сервер регистрирует 2 новых tool: `find_last_activity(bundle_id?)` — проксирует insights.lastActivity, добавляет displayName + secondsAgo; `get_current_session()` — unified shape `{active, currentSession?, lastSession?, version: 1}`, recency через `now - lastActivity.timestamp < focusSessionGapSec`. focusSessionGapSec инжектируется в tool из MCPServer.swift через `#if LEAF_PROD` ветку (single source of truth). (f) `MenuBarContent.sessionsBlock` — между top-5 list и chart: "Sessions: N · avg M · deep K / Switches: X.X/h", empty placeholder "No focus sessions yet — keep working" если sessions empty но topApps есть. 70 SPM-тестов зелёные (61 → 70, +9: 5 focusSessions + 2 contextSwitchRate + 2 lastActivity). Все 3 Xcode-target'а собираются.
- **Phase 2.2 done 2026-04-25.** Historical trends — 4 insights (activeDaysInRow / deepWorkStreak / peakProductivityHour / weekOverWeekDelta) + trends strip UI, без новых collectors / MCP tools / LLM. `DerivedInsights` 13 методов, ProdInsights реализовал 4 trends с TZ-aware day bucket'ами + pre-check на min-active-days. AgentThresholds + AgentThresholdsProd получили 4 поля (activeDayMinAttentionSec / peakHourWindowDays / peakHourMinActiveDays / wowBaselineMinActiveDays). InsightsSnapshot + convenience init расширены. InsightsReader sequential 7 queries в Task.detached. MenuBarContent.analyticsBlock = 4 строки. 82 SPM-тестов (+12). p95 = 92ms на 30d/50k events.
- **Phase 2.4 done 2026-04-25.** FSEvents content collector — последний MVP signal source (files в watched folders). (a) M003 `watched_folders` table (id UUID PK, path UNIQUE, max_granularity L4/L5 CHECK, enabled, added_ts, updated_ms) + 4 Database CRUD методов (list/add/remove/update) с partial-update паттерном. (b) `FSEventsRouting` protocol async (Stub возвращает .filtered always; Prod actor — moat). `FSEventsCollector` actor — single FSEventStream over canonical paths, lifecycle через cancel + await как ClaudeCodeCollector. Re-config через `DistributedNotificationCenter` ("tech.gundem.leaf.watched-folders-changed") + 60s polling fallback (belt+suspenders). (c) `FSEventStream` internal wrapper над C-API: UseCFTypes flag, `FSEventStreamSetDispatchQueue` (Swift 6 friendly, без CFRunLoop), self via `passUnretained` info pointer, callback `@convention(c)` без captures. `kFSEventStreamEventIdSinceNow` — durability через WAL + offset на DB level, не системный event-id replay. (d) `FSEventsRouterProd` (LeafCorePrivate actor): drop системные flags (MustScanSubDirs/RootChanged/Dropped/OwnEvent), require ItemIsFile + skip Dir/Symlink, kind priority (removed > created > renamed > modified > metadata-only filtered), longest-prefix watched root match, ignore rules (common dev tool internals — full set в moat), coalesce dedup state (LRU в moat). L4 → payload.path = parent dir (privacy-first, default); L5 → full file path (per-folder opt-in). (e) AgentThresholds расширен 4 fsEvents fields (latency / poll / restart trigger / coalesce window) с back-compat designated init. AgentThresholdsProd values — точные prod-числа в moat. fsEventsLogger в LeafAgent. (f) Agent.swift: parser injection через `#if LEAF_PROD`, AgentLifetime.fsEventsCollector slot, shutdown order maintenance → fsEvents → claudeCode → writer (flush+stop). (g) `ProdInsights.filesTouched` — SQL `MAX(ts) GROUP BY path ORDER BY last_ts DESC LIMIT 10` с `json_extract(payload_json, '$.path')`. Не фильтрует по watched_folders.enabled (non-retroactive по architecture). (h) InsightsSnapshot extended (filesTouched: [String], default `[]` в convenience init — back-compat). InsightsReader 9-я sequential query. (i) UI: `WatchedFoldersService` @MainActor @Observable — App становится 2nd writer для watched_folders CRUD (busyTimeout serialize'ит, 100ms retry once на busy error); постит DistributedNotification. `FoldersSettings` Settings tab (3rd) с NSOpenPanel.runModal() + L4/L5 Picker + Enabled Toggle + Remove. `FilenameResolver` singleton (LeafCore) — basename + tooltip cache. MenuBarContent.analyticsBlock получил `filesLines` после aiLine: "Files touched (top 5):" с .help() tooltip + empty placeholder "Files: no activity yet". 121 SPM tests (98 → 121, +23: 6 watchedFolders CRUD + 4 collector lifecycle + 1 stub router + 9 router prod (4 ignore + 1 dir + 1 outside_watched + 2 L4/L5 + 1 longest-prefix + 1 coalesce) + 3 filesTouched SQL). Все 3 Xcode targets собираются.
- **Phase 2.3 done 2026-04-25.** Claude Code AI collaboration — первый AI signal source. (a) `DerivedInsights` protocol получил `aiActivityBreakdown(period:) -> AIActivityBreakdown` (ratio + aiActiveSeconds + totalActiveSeconds + sessionCount + topTools + topProjects); aiRatio делегирует в breakdown.ratio. ProdInsights — bucket-union SQL (per-minute (ts/60000) GROUP BY, three CTE: ai_minutes / attn_minutes / union_minutes), top tools/projects через json_extract(payload_json, '$.tool_name'/'$.cwd'). (b) Migration M002 — `collector_offsets` table (composite PK collector_id+source_id), reusable для future FSEvents. 4 public Database API: readOffset / listOffsets / writeOffset (UPSERT через INSERT ... ON CONFLICT) / writeEventsAndOffset (atomic events insert + offset upsert в одной транзакции). (c) ClaudeCodeJSONLParsing protocol + StubClaudeCodeJSONLParser (LeafCore) + ClaudeCodeJSONLParser (moat). Routing: `assistant` с tool_use[] → events; `user` со string content → user_prompt event без body; `tool_result` / `system` / `attachment` / `last-prompt` / `permission-mode` / `custom-title` / `file-history-snapshot` / `queue-operation` / `agent-name` / `summary` / `isMeta=true` → irrelevant. file_path только для file-ops tools (Read/Edit/Write/MultiEdit/Glob/NotebookEdit). Subagent (`isSidechain=true`) включается. ADR-010 закрыт на parser-level — ни prompt body / assistant text / thinking / tool_use input / tool_use_result в payload. (d) ClaudeCodeCollector actor в LeafCore — pattern MaintenanceScheduler. Tail-read: list `<projectsRoot>/<slug>/*.jsonl` → URLResourceValues + FileManager attrs для inode → bootstrap branch (mtime ≥ now-7d → backfill от 0; иначе skip-backward с byte_offset=size) или skip-unchanged или resume from byte_offset. Inode/truncate guard. FileHandle.seek + readToEnd. Split по \n с incomplete tail skip. atomic writeEventsAndOffset per file. canonical path через resolvingSymlinksInPath (без этого `/var → /private/var` symlink дублировал rows). (e) AgentThresholds (public) + AgentThresholdsProd: aiCollectIntervalSec (weakDefault=120, prod=90), backfillWindowDays=7, claudeCodeProjectsPath="~/.claude/projects" (раскрывается NSString.expandingTildeInPath). (f) Agent.swift wired: `#if LEAF_PROD` parser injection, AgentLifetime.claudeCodeCollector slot, shutdown order maintenance → claudeCode → writer (flush+stop). (g) MCP — extracted TimelinePeriod в LeafMCP/Tools/Period.swift (shared между 4 tools). New GetAiActivityTool (4-й tool): output {period, from, to, aiRatio, aiActiveSeconds, totalActiveSeconds, sessionCount, topTools[], topProjects[], version:1}. Зарегистрирован в MCPServer. (h) InsightsSnapshot extended (aiRatio + aiActiveSeconds, designated + convenience defaults — back-compat). InsightsReader.refresh — 8-я sequential query. MenuBarContent.analyticsBlock получил `aiLine` row после trendsLines: "AI ratio today: X% · Claude Code Y min" + .help() tooltip + empty placeholder. 98 SPM-тестов зелёные (82 → 98, +16: 5 LeafCore Database — 3 CollectorOffsets + 2 atomic; 7 LeafCorePrivate parser; 1 collector integration; 1 collector bootstrap-skip-backward; 2 ProdInsights — testAIRatioAndBreakdown + testAIEventsNotInTopApps regression). Все 3 Xcode-target'а собираются.
- **Phase 2.5 done 2026-04-26. Phase 2 закрыт целиком.** Wrap: empty-state polish (3 UI-fix'а + isEmpty unit-тесты) и два Phase 2.4 hotfix'а после первого smoke. (a) UI: `aiLine` 3-state — `aiActiveSeconds < 180s` показывает raw time без процента (sub-3min слишком мало для honest ratio); `trendsLines` collapse — при `streak.days==0 && peak==nil && wow==nil && activeDaysInRow<=1` одна строка "Trends appear after ~14 days of data" вместо 2×"—"; `permissionsBanner` — TODO-anchor с EmptyView marker для Phase 3 onboarding (AX/FDA denial surfacing). (b) `InsightsSnapshot.isEmpty` закреплён 10 unit-тестами в `InsightsSnapshotTests.swift` — load-bearing property для InsightsReader state machine, по одному test'у на каждое из 10 полей snapshot'а (regression guard для будущих snapshot полей). (c) Hotfix FSEvents ignore — расширен `FSEventsIgnoreRulesProd` (moat): `xcuserdata` dir component (Xcode UI state atomic-rename `*.xcuserstate.dat.nosync<HEX>.<RANDOM>`) + `*.tmp.*` glob (Foundation atomic write `<original>.tmp.<digits>.<digits>` через AppKit save flow) + `*.xcuserstate*` defensive glob. Без них popover "Files touched" floоding'ил Xcode internals. +2 regression теста. (d) Hotfix switches min-hold filter — `contextSwitchMinHoldSec` новое поле в `AgentThresholds` (public name, weakDefault=0 back-compat); точное prod-значение в `AgentThresholdsProd` (moat). `ProdInsights.contextSwitchRate` SQL расширен LEAD-window check: switch считается только когда current event длился ≥ minHold. Применяется в SQL post-process, raw events честно пишутся. На реальном workflow с messengers срезает inflated rate в несколько раз (фильтрует quick alt-tab peek'и). +2 regression теста (with/without filter). (e) 121 → 135 SPM tests (+14: 10 InsightsSnapshot + 2 FSEvents + 2 contextSwitch). Все 3 Xcode targets собираются. (f) Section B E2E walkthrough confirmed 2026-04-26: wipe local state → fresh Release install в `/Applications/Leaf.app` → MCP rewire → first-launch flow (Toggle ON / TCC grants / Watched Folder add) → popover renders all 5 blocks с Phase 2.5 fixes visible (3-state AI line на measured branch "AI ratio today: 88% · Claude Code 30m"; trends collapsed single-line "Trends appear after ~14 days of data"; switches 67/h sensible с min-hold filter). 4/4 MCP tools (`get_timeline` / `find_last_activity` / `get_current_session` / `get_ai_activity`) отдали корректный shape с `version: 1`. Backfill-window 7 дней jsonl сработал — fresh БД сразу подтянула AI history. WAL 1.22 MB при maintenance scheduler running. 2-3 дня passive smoke по-прежнему на горизонте, но операционная цельность Section B подтверждена.
- **Linear переведён в режим "только таски"**. Второй мозг = whitepaper.

## Ключевые архитектурные решения (актуальный срез)
Все живут в whitepaper `leaf-docs/docs/`. Сжатая выжимка:

- **Two surfaces:** Native UI primary standalone; MCP — bonus channel для AI-клиентов.
- **Opt-in transparency + Share Controls:** whitelist model per-app + per-event-type, default empty, preset в onboarding.
- **Private self + Symmetric visibility:** raw on-device (SQLCipher), admin = обычный team-member + billing.
- **Granularity L1-L5**, L6 content запрещено всегда.
- **Layer A:** NSWorkspace / CGEventSource / EventKit / Focus / AX / FSEvents / git polling / Claude Code hooks. Без AppleScript и без post-commit hooks в MVP.
- **Layer B MVP = Linear** (OAuth PKCE).
- **Presence Relay:** Cloudflare DO + WebSocket + AES-GCM-256 + ECDH invite/rotation + Share Controls filter до encryption.
- **Storage:** GRDB 7 fork + SQLCipher, 14 таблиц, multi-process WAL.
- **Derived Insights Engine:** 12 функций, Swift + SQL, zero LLM в MVP.
- **Distribution:** Sparkle 2 + EdDSA + Cloudflare R2 + notarytool.

## В работе прямо сейчас
- **Phase 3.0 done 2026-04-27.** Distribution prep: Apple Developer ID Application cert (4LZ84Z78M7, "Dmitrii Demidov") в Keychain; App Store Connect API key + `leaf-notary` profile через `xcrun notarytool store-credentials`; Sparkle 2.9.1 CLI в `~/bin/sparkle/` (`generate_keys`/`generate_appcast`/`sign_update`); EdDSA keypair (private — Keychain native + GH Secret; pub в `~/Desktop/leaf-eddsa-pub.txt`); brew `create-dmg 1.2.3` + `awscli 2.34.37` + R2 path-style addressing config. R2 bucket `leaf-updates` + Custom Domain `updates.gundem.tech` (партнёр в его CF account; зона `gundem.tech` там же); 2 Cache Rules (`releases-immutable` 1 month / `appcast-fresh` 5 min); 2 API tokens (R2 Edit + Cache Purge на zone `gundem.tech`). 11/11 GH secrets set. Round-trip smoke зелёный: aws s3 cp → curl `https://updates.gundem.tech/...` → 200/content. CI fixes по ходу: rename `LeafControl* → Leaf*` в `.github/workflows/ci.yml` (11 точек), workflow re-enable (`disabled_manually` после Phase 0), `runs-on: macos-14 → macos-15` (Xcode 16 для pbxproj `objectVersion=77`), Swift 6 strict-concurrency `nonisolated(unsafe)` на signal handler globals в `LeafAgent/SignalHandlers.swift`. 1Password third copy — skipped до production launch. Tactical doc: `.claude/plans/phase-3-0.md`.
- **Phase 3.1 Sparkle integration — done 2026-04-27.** Sparkle 2.x SPM dependency (auto-embedded в `Leaf.app/Contents/Frameworks/` для Xcode 14+ SPM dynamic frameworks — Embed & Sign explicit не требуется). Linked только в Leaf target (D5). 7 SU* keys в Info.plist (FeedURL=updates.gundem.tech/appcast.xml, EdDSA pubkey embedded, RequireSignedFeed=YES, EnableInstallerLauncherService=NO для non-sandbox in-process install, AutomaticallyUpdate=NO, **EnableAutomaticChecks=NO** до Phase 3.5 R2 unblock per D10 — убирает 404 spam, **ScheduledCheckInterval=86400** daily per D9 vs hourly из master plan). UpdaterController (Leaf/Models/) — thin @Observable wrapper над SPUStandardUpdaterController, manual checkForUpdates(). LaunchAgentService — D1 register() filter "already registered" для idempotent init. LeafApp — D13 explicit `_state = State(initialValue:)` + idempotent register guard `if !launchAgent.isEnabled { agent.register() }` для post-update relaunch restoration. SettingsView General tab → новый Updates section с "Check for Updates…" button + version display "1.0.0-alpha.1 (build 2)". Bump MARKETING_VERSION + CFBundleVersion × 3 targets. .gitignore переписан: ignore `swiftpm/configuration/` + `swiftpm/artifacts/` (`!` exception на ignored parent dir не работает в git) — Package.resolved committed для reproducible builds. **D14 (mid-execution scope cut от master plan A2 D2/D8): no SPUUpdaterDelegate choreography в 3.1.** Sparkle's `willInstallUpdateOnQuit` срабатывает только для silent-install-on-quit path (auto-download); manual install path использует internal hooks без публичного API. Без choreography flow: Sparkle bundle replace + relaunch → idempotent register → launchd SIGTERM old agent → existing SignalHandlers (`Agent.swift:131`) flush WAL gracefully → new agent acquires writer lock через busy_timeout=5000ms (Phase 1.1). Multi-process SQLCipher handles overlap. Cascading scope cuts: `waitUntilStopped`, agent.pid file, Agent.swift modifications — все skipped. 4 файлов (1 new — UpdaterController, 3 mod — LaunchAgentService/LeafApp/SettingsView + 4 — .gitignore/Info.plist/pbxproj/Package.resolved для 3.1a), ~+200/-15 LOC, 3 commits (b4938b6/ecaac30/181ee61). Build green Debug + Release × 3 targets. Sparkle.framework verified embedded в Leaf.app, otool clean в LeafAgent/LeafMCP.
- **Phase 3.4.5 done 2026-04-28.** File-based SQLCipher key store закрыл dual-encryption риск Phase 3.3 hotfix. Раньше: после удаления `keychain-access-groups` entitlement main app + LeafAgent + LeafMCP оказались бы в разных default per-bundle Keychain access groups → разные ключи → SQLCipher decryption mismatch при Toggle Background ON. Теперь: ключ в `~/Library/Application Support/Leaf/db.key` (mode 0600), все 3 бинаря читают тот же файл — нативный sharing без entitlement-инфраструктуры. Защита: file permissions + FileVault (sensitivity ≤ DB sensitivity). Новый модуль `LeafCore/Crypto/FileKeyStore.swift` (~110 LOC): atomic write (`Data.write([.atomic])` → POSIX rename) + read-back verify, mkdir parent defensively, mode 0600 через `setAttributes`, bridge migration от legacy Keychain item (alpha.2 main app's K1) если файла нет, `cleanupLegacyKeychainBestEffort()` отдельный вызов из main app eager init. **Critical race-fix**: eager `FileKeyStore.fetchOrCreate()` в `LeafApp.init()` ДО `agent.register()` — main app материализует файл из своего Keychain group до того как любой helper стартанёт; helpers (свои default groups пустые) на spawn видят файл already exists, читают K1. Без eager init lazy `InsightsReader` опаздывал бы на штатном Toggle ON path. `KeychainKeyStore.fetchOnly(...)` public для bridge migration; `KeychainKeyStore.swift` оставлен как deprecated до Phase 3.5+ cleanup; `KeychainAccessGroup.swift` удалён (-25 LOC). `LeafError.keyFileUnavailable(reason:)` + `.keyFileCorrupted` cases (Sendable). `EncryptionOptions` shape не менялся — 4 callsite (Agent / MCPServer / InsightsReader / WatchedFoldersService) поменяли только тело callback'а. 9 unit tests в `FileKeyStoreTests.swift` (32-byte / idempotent / not-zeros / mode-0600 / delete / delete-idempotent / corrupted-throws / concurrent-converges / migration); 144/144 SPM tests green (135 → 144). Build green Debug + Release × 3 targets. **E2E smoke verified**: full wipe (events.sqlite + db.key + Keychain item + LaunchAgent plist + /Applications/Leaf.app) → fresh launch Release build → `db.key` появился сразу 32 bytes mode 0600 (eager init сработал) → Toggle Background ON → LeafAgent (PID 56426) запустился, events.sqlite вырос до 2.4 MB за минуту → popover показал Today (Terminal/Figma/ChatGPT) + sessions + AI ratio 93% · Claude Code 25m → 3 процесса читают один ключ, никакого macOS keychain prompt. Public-safe synced в whitepaper: storage.md (Keychain section → "Ключ шифрования" + admonition v1.4 — 2026-04-28), philosophy.md, mvp-scope.md, changelog.md. Commits: `998f5a1` (leaf), `41c47b0` (leaf-docs).
- **Phase 3.2 done 2026-04-27.** Distribution pipeline: `scripts/upload-release.sh` ~70 LOC bash (committed, executable) — `aws s3 cp` с path-style addressing → R2 bucket `leaf-updates` (artifacts под `releases/` octet-stream + `appcast.xml` application/xml max-age=300) + Cloudflare edge cache purge через CF API. Все секреты (R2_*, CF_*) — через env vars; `--help` печатает usage с export-блоком. `.gitignore` defensive add `.envrc`/`.env` в секцию distribution secrets. Placeholder `build/releases/appcast.xml` (gitignored, 347 bytes minimal RSS-2.0) smoke: gate 1 — script exit 0 (3 шага без error); gate 2 — `curl -fsSI https://updates.gundem.tech/appcast.xml` → 200, content-type application/xml, cache-control max-age=300; gate 3 — `cf-cache-status: MISS` first call после purge, `HIT` second call (CF cache rule `appcast-fresh` 5min верифицирована); gate 4 — `xmllint --noout` exit 0; gate 5 — Sparkle "Check for Updates…" → "You're up-to-date" (placeholder без `<item>` = no updates). `SUEnableAutomaticChecks=NO` остаётся до Phase 3.5 (flip вместе с первым real release commit). Tactical doc: `.claude/plans/phase-3-2.md`.
- **Phase 3.5 done 2026-04-28. Phase 3 closed.** Shipped 1.0.0-alpha.5 в R2 через 3 ship iterations в одной сессии — alpha.3 / alpha.4 / alpha.5, каждая закрыла свой latent Sparkle bug. **Original scope (alpha.3)**: bump 1.0.0-alpha.2 → 1.0.0-alpha.3 + CFBundleVersion 3 → 4 × 3 targets, flip `SUEnableAutomaticChecks=NO → YES` в Info.plist (был disabled с Phase 3.1 D10 пока R2 unblock'нут не был), `kSecUseAuthenticationUI=kSecUseAuthenticationUISkip` в `KeychainKeyStore.baseQuery()` для silent migration без macOS keychain prompt при bridge fetch / legacy cleanup. Refactored `FileKeyStoreTests.testConcurrentFetchOrCreateConverges` → `testConcurrentReadersConverge` — оригинальный race test полагался на side-effect системного Keychain item для convergence (FAIL после UISkip change exposed flake), новый моделирует production sequence eager-init + concurrent reads. **Iteration 2 (alpha.4)**: alpha.3 smoke выявил Phase 3.1 D14 latent bug — `SPUStandardUpdaterController(startingUpdater: true)` в `LeafApp.init()` запускается ДО NSApplication runloop activation, Sparkle's auto-start silently no-op → "Error: checkForUpdates - updater hasn't been started yet" в логах, "Check for Updates…" вообще не открывает dialog. Фикс в `UpdaterController.init()`: `startingUpdater: false` + explicit `controller.startUpdater()` через `DispatchQueue.main.async` (runs after App.init возврата когда runloop ready). Bump alpha.3 → alpha.4, CFBundleVersion 4 → 5. **Iteration 3 (alpha.5)**: alpha.4 smoke выявил Sparkle 2.x security enforcement — `SURequireSignedFeed=YES` теперь требует ещё `SUVerifyUpdateBeforeExtraction=YES` (mandatory pair, security hardening), без него "Fatal updater error (5)". Один key в Info.plist, bump alpha.4 → alpha.5, CFBundleVersion 5 → 6. После alpha.5 install Sparkle init clean: `[Sparkle] OK: EdDSA signature is correct for appcast` в логах, scheduled check работает. **Bridge migration validated на real bytes (Phase 3.4.5 closure)**: alpha.2 install → Toggle Background ON → events.sqlite вырастает до 2.4 MB encrypted с K1 в Keychain → SIGTERM + manual ditto bundle replace alpha.4 → open → eager `FileKeyStore.fetchOrCreate()` находит K1 в Keychain (UISkip silent) → пишет `db.key` 32 bytes mode 0600 → SQLCipher reads events.sqlite с K1 successfully → popover показывает Today (Terminal 49s) с историей alpha.2. ZERO macOS keychain prompts на alpha.4/alpha.5 launches (UISkip + ACL "Always Allow" trust на Designated Requirement = TeamID + bundleID, не CDHash). **release.sh battle-tested** — 3 successful runs, idempotent stamps работают, `--clean` flag rebuild всё. Notary submissions `78021fa7` (alpha.3) + `cdd...` (alpha.4) + alpha.5 — все Accepted на первом submit. Acceptance gates: stapler validate × 3 ✅, spctl assess "Notarized Developer ID accepted" × 3 ✅, appcast.xml latest = 1.0.0-alpha.5 (sparkle:version 6) EdDSA signed ✅, R2 200 на каждый .dmg ✅. **NOT validated в этой сессии**: real Sparkle update path end-to-end (alpha-юзер кликает Check for Updates → Sparkle dialog → Install → relaunch). alpha.2 имела broken Sparkle (Phase 3.1 D14 bug), update path с неё через Sparkle невозможен — alpha-юзеры (~2 на этом этапе) делают manual download alpha.5 .dmg. Real Sparkle E2E будет verifiable начиная с alpha.5 → alpha.6 next ship cycle. **Известный quirk**: TCC permissions (Accessibility) после manual bundle replace иногда показывают "disabled" в banner хотя юзер дал access — macOS TCC database не реконсилирует new binary CDHash через manual ditto path; fix `tccutil reset Accessibility tech.gundem.leaf` + re-toggle. Sparkle's relaunch protocol избегает этого, через next Sparkle update edge case не воспроизведётся. Files changed: `Leaf.xcodeproj/project.pbxproj` (12 строк bumped), `Leaf/Info.plist` (+2 SU* keys), `Leaf/Models/UpdaterController.swift` (deferred startUpdater), `Packages/LeafCore/Sources/LeafCore/Crypto/KeychainKeyStore.swift` (+1 UISkip line в baseQuery), `Packages/LeafCore/Tests/LeafCoreTests/FileKeyStoreTests.swift` (race test refactor). 144/144 SPM tests green.

- **Phase 3.3 done 2026-04-27.** First real notarized release shipped 1.0.0-alpha.2 (alpha.1 retracted — см. ниже). Notary submissions: `f79a3c33` (alpha.1) + `dd352c57` (alpha.2), Accepted на первом submit обе. `scripts/release.sh` ~324 LOC bash — 9-шаговый local-first orchestrator (archive→export→verify→dmg→notary→staple→zip→appcast→upload), idempotent через stamp-файлы под `build/releases/.stamps/`, flags `--redo-from <step>` / `--clean`. Цветной prefix (cyan/green/red), set -euo pipefail, helpful `usage()`. Найдены и закрыты 4 distribution-specific bug'а: (a) **archive collision** — все 3 target (Leaf+LeafAgent+LeafMCP) collide на `UninstalledProducts/macosx/Frameworks/SQLCipher.framework`, fix через `SKIP_INSTALL=YES` + per-target `UNINSTALLED_PRODUCTS_DIR` в pbxproj; (b) **`xcodebuild -exportArchive` требует Developer ID Provisioning Profile** (Xcode 16+), bypass через прямое `cp .xcarchive/Products/Applications/Leaf.app + manual inside-out re-sign` с `Developer ID Application` cert (Sparkle XPC services → Updater.app → Autoupdate → Sparkle.framework → SQLCipher.framework → LeafAgent → LeafMCP → Leaf.app); (c) **stapler не работает после re-create .dmg** (hash mismatch с Apple's CDN), решение — staple .app + .dmg отдельно от ОДНОЙ notary submission (.app inside .dmg остаётся unstapled, online Gatekeeper check на launch — invisible UX); (d) **generate_appcast rejects .dmg + .zip same version** (duplicate updates), решение — temporarily mv .dmg out перед `generate_appcast`. **Bug discovered post-upload:** `upload-release.sh` recursive cp залил 260MB мусора (stamps/ + .xcarchive/ + exported/Leaf.app/ как 1000+ файлов) — fix: `--exclude "*"` + `--include "*.dmg|*.zip|*.delta"` extension whitelist. Cleanup: 425 R2 объектов удалены (`aws s3 rm --recursive` × 3 prefix). LaunchAgent plist hygiene (D10): убраны `StandardOutPath`/`StandardErrorPath` `/tmp/*.log` пути. Acceptance gates все прошли: stapler validate × 2, appcast.xml live с 1 `<item>` (sparkle:edSignature OK + signed feed wrapper), CF cache MISS→HIT, `spctl assess -t exec -vv Leaf.app` → "accepted, source=Notarized Developer ID, origin=Developer ID Application: Dmitrii Demidov (4LZ84Z78M7)". 2 commits: `2376333` (xcode archive fix) + `d9bca0f` (release.sh + plist + upload whitelist). **alpha.1 retraction**: post-upload smoke launch revealed `taskgated-helper: tech.gundem.leaf: Unsatisfied entitlements: keychain-access-groups → Disallowing` (AMFI exit 137). Root cause: `keychain-access-groups` в `Leaf.entitlements` требует Developer ID Provisioning Profile (которого нет) — без profile macOS убивает app на launch. Hotfix `499a006`: убран entitlement из `Leaf.entitlements`, `KeychainAccessGroup.current()` всегда возвращает `""` (default per-bundle keychain group; sharing с helpers и так broken — Phase 3.4.5). Bump 1.0.0-alpha.1 → 1.0.0-alpha.2 (CFBundleVersion 2 → 3 × 3 targets). Re-release через `bash scripts/release.sh 1.0.0-alpha.2 --clean` exit 0 на первом проходе. alpha.1 .dmg/.zip удалены из R2 + CF cache purged → broken URL'ы возвращают 404. Tactical doc: `.claude/plans/phase-3-3-async-panda.md`.

## Следующим
- **Linear OAuth + first integration** (Layer B MVP) — следующая большая фаза, не блокирована Phase 3.
- **Real Sparkle update path E2E (alpha.5 → alpha.6)** — следующий ship cycle закроет verify "alpha-юзер кликает Check for Updates → Sparkle dialog → Install → relaunch → bridge migration". Сейчас infrastructure готова, только нужен новый ship для testing.
- **Phase 3.5+ cleanup**: delete `KeychainKeyStore.swift` + `KeychainKeyStoreTests.swift` + `LeafError.keychainUnavailable` после ~2 недель stable runtime у alpha-юзеров (migration confirmed).

## Open tensions
Живут в `leaf-docs/docs/05-reference/open-tensions.md`. Топ-2:
- **OT-1** distributed deletion локальной forever-history у ex-teammates.
- **OT-2** storage compression для forever retention.
