# Текущее состояние проекта

_Срез "где мы сейчас" за 30 секунд. История — в whitepaper changelog + `.claude/plans/phase-*.md` + git log._

## Последнее обновление
2026-05-02 — **Phase 4.7.B landed on `feature/phase-4-7-a`** (20 коммитов от `be26210` до `0a1a367` поверх Phase 4.7.A, не merged в main, не shipped). Track B scope ("presence-first first-class APIs") полностью реализован: B-0 `PresenceStateWriter` + atomic `Database.writeEventsOffsetAndPresence`; **GitHub** (B-1..B-5) — `fetchNotifications` / `fetchPRsAwaitingReview` / `fetchMyOpenPRs` / `fetchActionsRunsForActor` (top-10 active repos via `Database.queryActiveGitHubRepos`) / `fetchCheckRunsForCommit` (push-triggered) / `fetchContributionsCalendar` (daily GraphQL cooldown) + composite `presence_state.github` writer; **Linear** (B-6..B-8) — query расширен `viewer.assignedIssues` / `viewer.teams.activeCycle` / per-issue `attachments(first:10)` enrichment + `LinearAttachmentParser` (GitHub PR + Slack permalink regex) + composite `presence_state.linear` writer; **Slack** (B-9..B-13) — `fetchPresence` (users.getPresence) / `fetchDND` (dnd.info) / `fetchMentionsReceived` (search.messages `<@USER_ID>`) / `fetchFilesUploaded` (search.files `from:me`, mime-type bucket image/code/doc/other) + composite `presence_state.slack` writer (added Slack scopes `dnd:read` + `files:read` — re-consent для existing alpha.7 tokens); **B-14** — `ShareEventTypeKey` registry +11 keys (33 total); **B-15..B-18** — 4 новых MCP tools `get_current_presence` / `get_workload_pulse` / `get_review_activity` / `get_cross_provider_thread` (12 tools total в MCPServer); **B-19** docs(architecture) updated. **495 SPM tests**, 0 failures, 1 skipped (baseline 4.7.A = 377, +118 новых). ADR-010 regression-тесты с sentinel-injection на каждом провайдере. xcodebuild LeafMCP target builds clean (B-16 + B-17/B-18 verification). Substrate под Phase 5 broadcast готова (3 composite presence_state rows, encrypted broadcaster reads from `PresenceStateWriter.readAll`). `derived_mode` column остаётся NULL до Phase 4.9. Жду C перед merge & ship; alpha.9 = A+B+C bundle. Tactical: `.claude/plans/phase-4-7-B.md`.

2026-05-01 — Phase 4.7.A landed (commits `0610816..70b5f0f`): 11 новых event_kind discriminators поверх existing tick'ов, M005 `presence_state` table (writes в B), `LinearIDExtractor` + GitHub commit/PR hooks, `ShareEventTypeKey` registry. **377 SPM tests** (+56 от baseline 4.6=321). Tactical: `.claude/plans/phase-4-7-A.md`. **Native UI redesign** (`74386a2`) ранее на main, ждёт alpha.8. **alpha.7 SHIPPED 2026-04-30** (Phase 4.6 A+B+C). Live: `https://updates.gundem.tech/appcast.xml`.

## Где мы
- **Whitepaper v1.10** published в `leaf-docs.gundem.tech`. Структура `01-vision / 02-product / 03-architecture / 04-market / 05-reference`.
- **Section A done (Phase 0-2, 2026-04-23 → 2026-04-26).** Foundation: 3-target Xcode project, `LeafCore`/`LeafCorePrivate` SPM split, encrypted storage (GRDB 7 fork + SQLCipher AES-256), Agent daemon + 4 MVP collectors, Derived Insights Engine, MenuBarApp Native UI, stdio MCP server. Tactical: `.claude/plans/phase-2*.md`.
- **Section B done (Phase 3.0-3.5, 2026-04-27 → 2026-04-28).** Distribution: Apple Developer ID + notarytool + Sparkle 2 + R2/CF + EdDSA-signed appcast. Shipped **1.0.0-alpha.5** → **alpha.6** (2026-04-29) → **alpha.7** (2026-04-30, bundled Phase 4.6). Tactical: `.claude/plans/phase-3*.md`.
- **Layer B MVP closed (Phase 4.1-4.5, 2026-04-28 → 2026-04-29).** Linear (PKCE loopback fixed-port + GraphQL per-action attribution через `activity.user.isMe` + `creator.isMe` backstop), GitHub (Device Flow RFC 8628, REST `/users/<X>/events` polling), Slack (PKCE distributed-app через HTTPS relay `oauth.gundem.tech/<provider>/callback` Cloudflare Worker — bouncer на loopback). 7 MCP tools, popover lines per provider. Tactical: `.claude/plans/phase-4-{1,2,3,4,5}.md`.
- **Phase 4.6 done E2E (Layer B Depth, 2026-04-30, alpha.7 ready).** Layer B перешёл из "что произошло + сколько раз" → "что произошло + насколько долго + какой темп". **A** latency depth (GitHub PR cycle + review delay; Linear completion duration; Slack reactions count + huddle session distribution — all через generic `LatencyStats` aggregation). **B** Linear status transitions (новый `linear_status_transition` event flavor через nested history fragment + client-side actor filter). **C** synthesis (surface existing `weekOverWeekDelta` + новая `longestUninterruptedWindow` 8-й MCP tool + per-provider streaks 60-day lookback). **8 MCP tools** total. **321 SPM tests**, 6 xcodebuild green. Tactical: `.claude/plans/phase-4-6{,-A-3,-B,-C-1,-C-2,-C-3}.md`.
- **Phase 4.7.A landed (feature branch, NOT shipped, 2026-05-01).** 11 новых event_kind discriminators поверх existing tick'ов, `presence_state` table M005, `LinearIDExtractor` + GitHub commit/PR hooks, `ShareEventTypeKey` registry. Tactical: `.claude/plans/phase-4-7-A.md`.
- **Phase 4.7.B landed (feature branch, NOT shipped, 2026-05-02).** Track B scope ("presence-first first-class APIs") closed E2E: B-0 atomic `PresenceStateWriter` + `Database.writeEventsOffsetAndPresence`; B-1..B-5 GitHub (5 fetch methods + `presence_state.github`); B-6..B-8 Linear (3 GraphQL extensions + `LinearAttachmentParser` + `presence_state.linear`); B-9..B-13 Slack (4 fetch methods + `presence_state.slack`, +`dnd:read`/`files:read` scopes); B-14 ShareEventTypeKey +11 keys (33 total); B-15..B-18 4 новых MCP tools (12 total); B-19 docs. **495 SPM tests** (+118 от 4.7.A). 11 новых event_kind discriminators (presence-first pulses + actions runs + check-runs + workload + cycle + Slack DND/presence/mentions/files) + 4 link-counts enrichment of existing `issue_updated` payload. ADR-010 layered defense (query schema → parser → snapshot → JSON serialization → MCP whitelist projection); sentinel-injection regression tests на каждом провайдере. Branch awaits C для alpha.9 bundle. Tactical: `.claude/plans/phase-4-7-B.md`. xcodebuild LeafMCP target verified, full project xcodebuild смок deferred до alpha.9 ship pre-flight.
- **Linear** = только таски. Второй мозг = whitepaper.

## Архитектура
Полная картина — `.claude/shared/architecture.md` + whitepaper `leaf-docs/docs/03-architecture/`. TL;DR: two surfaces (Native UI primary + MCP bonus), opt-in transparency + Share Controls, granularity L1-L5, Layer A (NSWorkspace/CGEventSource/EventKit/Focus/AX/FSEvents/Claude Code hooks), Layer B MVP = Linear+GitHub+Slack (+ Phase 4.6 latency/transitions/synthesis depth, + Phase 4.7.A wide-cheap event_kind expansion + cross-provider Linear ID linking, + Phase 4.7.B presence-first state-snapshot endpoints + `presence_state.{github,linear,slack}` composite writers + 4 new MCP tools), presence relay через Cloudflare DO + AES-GCM, 15 SQLCipher tables (+ `presence_state` M005), zero LLM в MVP, Sparkle 2 + EdDSA + R2 distribution. **12 MCP tools** total (8 baseline + 4 Phase 4.7.B presence-first).

## Следующим
- **alpha.8 ship cycle** — UI redesign (`74386a2`) + bump `MARKETING_VERSION` 7→8 + `CURRENT_PROJECT_VERSION` 8→9 + release.sh + appcast. Phase 5 (presence relay) идёт отдельным циклом.
- **Phase 4.7 — Provider Surface Expansion — target alpha.9.** Tracks A + B landed на `feature/phase-4-7-a` (не merged, не shipped). **C ещё предстоит**: triage depth (priority / label / assignee / cycle / estimate transitions через Linear history extension + `pr_review_thread_resolved` + Mode classifier skeleton, `presence_state.derived_mode` остаётся NULL до 4.9). После C — single merge в main + ship alpha.9. Substrate под Phase 5 broadcast уже готов (3 composite `presence_state` rows из B). Spec: `.claude/plans/phase-4-7-design.md`. План C пишется через writing-plans skill в отдельной сессии — base SHA `0a1a367` (последний B-19 commit), нужны: `pr_review_thread_resolved` GitHub event_kind + Linear history fragment extension (priority/label/assignee/cycle/estimate transitions поверх Phase 4.6.B `linear_status_transition` pattern) + Linear `ProjectUpdate`/`Document`/`viewer.initiatives` skeleton (graceful degrade) + `ModeClassifier` protocol + `Mode` enum + `Pulse` enum + `ClassifiedMode` struct в `LeafCore/Insights/`. Перед alpha.9 ship: `xcodebuild` smoke + manual flow на dev Mac (Linear/GitHub/Slack reconnect, query новых event_kinds в SQLCipher) + Slack re-consent flow (B-10/B-12 added `dnd:read` + `files:read` scopes).
- **Phase 4.7.B carry-overs (non-blocking, file as Linear cleanup для 4.7.C/4.8):** (a) hoist GitHubCollector UTC `DateFormatter` to static (B-3 M-1); (b) split `GitHubAPIProvider.swift` (~403 lines / 6 structs) → `GitHubAPISummaries.swift` before C adds more structs (B-3..B-5 M); (c) add `testTick_LatestPushCheckStatus_PicksMostRecentPush` test (B-5 I-1 — current fixture builds ASC but production REST is DESC); (d) document `fetchEvents` failure short-circuit asymmetry в GitHubCollector (B-5 I-2). None block alpha.9.
- **Phase 4.8 — Deep payload metadata (NOT designed yet, follow-up к 4.7).** `commit_pushed` += files_count/+/-lines, `pr_opened` += requested_reviewers/size_category, `linear_status_transition` enrichment, slack message += mention_count/link_count, `linear_reactions_given/received`, expression-индекс на `payload.event_kind` если query latency surfaces, cycle-time analytics. Brainstorm в свою очередь после 4.7 ship'а.
- **Phase 4.9 — Derived modes (NOT designed yet, follow-up к 4.7).** Имплементация `DefaultModeClassifier` (skeleton groundwork лежит в 4.7.C): code/coordination/review/focus/meeting + pulse heavy/medium/light. Mode transitions log в новую `mode_history` table. `presence_state.derived_mode` начинает писаться. Новые MCP tools `get_current_mode` + `get_mode_distribution`. Self-UI mode-tinted timeline. Brainstorm после 4.8 ship'а.
- **Phase 5 (presence relay)** — Cloudflare Durable Objects + WebSocket Hibernation, AES-GCM envelope, X25519 ECDH invite, key rotation на removal. Reads `presence_state` substrate из 4.7 (готова) и 4.9 (modes added). Расширение `gundemtech/leaf-relay` (приватный repo от Phase 4.4 Slack OAuth bouncer'а), не from-scratch. Идёт отдельным циклом, ship после 4.9.
- **Layer C (V1.5+)**: MCP-aggregator поверх Notion / Figma / Jira / Gmail / Calendar.
- **Phase 3.5+ cleanup**: delete `KeychainKeyStore.swift` + `LeafError.keychainUnavailable` после ~2 недель stable runtime alpha.6 (target ~2026-05-13).
- **leaf-relay репо housekeeping**: README + CI deploy hook (Wrangler) + документация по `oauth.gundem.tech/<provider>/callback` routing для будущих v1.1+ providers (Notion / Figma).
- **Release tooling improvements** (после alpha.7 lessons): (a) `release.sh` должен auto-bump `CURRENT_PROJECT_VERSION` (или хотя бы fail-fast если build number == prior-version) — alpha.6 ship забыл bump, обнаружили только при alpha.7; (b) `upload-release.sh` whitelist не включает `*.html` release notes — добавить когда понадобится.
- **Sparkle ship gotchas**: (a) Xcode Debug LeafAgent залипает в launchd registration → SMAppService.register() в shipped Leaf.app filter "already registered" — `pkill -f "DerivedData.*LeafAgent"` pre-ship; (b) macOS TCC AX permission иногда требует toggle off+on после Sparkle bundle replace (CDHash меняется); (c) **CFBundleVersion должен быть monotonically increasing per ship** — Sparkle update detection ломается без bump'а; (d) **dev-only**: переключение между `/Applications/Leaf.app` (alpha) и `DerivedData/.../Leaf.app` (Debug) сбрасывает BTM parent disposition в `disabled` — агент `spawn failed exit 78` пока юзер не toggle'нёт Login Items → "Allow in the Background" → Leaf ON. Production update path (Sparkle in-place replace) не страдает.

## Open tensions
Живут в `leaf-docs/docs/05-reference/open-tensions.md`. Топ-2:
- **OT-1** distributed deletion локальной forever-history у ex-teammates.
- **OT-2** storage compression для forever retention.
