# Текущее состояние проекта

_Срез "где мы сейчас" за 30 секунд. История — в whitepaper changelog + `.claude/plans/phase-*.md` + git log._

## Последнее обновление

**2026-05-12 — Phase Track-3 D2 (GitHub Deep Sweep) landed на `feature/track-3-D2-github-deep-sweep`** (off `feature/linear-schema-reconciliation-2026-05-11`, baseline 1283 SPM). GitHub coverage **21 → 52 event_kinds** (+31 net-new: hot 7 / warm 13 / cold 11) + first OAuth scope-bump re-auth UX + canonical `gh_*` prefix discipline (M016 retroactive rename + `GitHubEventKindKey` enum anti-drift fence). **26 public commits** через subagent-driven TDD (per-task two-stage spec+code-quality review) + Task 4.5 consumer-sweep amendment (split out post-discovery: `Database`/`EventLinksStore`/`DetectorPipeline`/`ActivityFeedMapper`/`ReviewActivityInsights`/`PresenceInsights`/`QueryEngine`/`ActivityRow` + 12 test files + doc-comment hygiene sweep) + Tasks 26/27 moat-only (gitignored, no public commit). **M016** retroactive event_kind rename of 21 legacy GitHub events; idempotent UPDATE statements per old name (re-runs find zero rows, no double-prefix bug — `"gh_*"` already-renamed strings cannot match `"old_*"` pattern). **`GitHubEventKindKey`** typed enum (52 cases, raw String + CaseIterable + Sendable + Hashable) — single source of truth across 4 mirror sites (this enum / ShareEventTypeRegistry / M016 renameMap / EventsFullTextStore body-kind dispatch); `legacyRenamed: Set<>` (21) + `bodyBearing: Set<>` (12 after pruning discussion per Task 23 fence — discussion bodies not currently captured by moat per ADR-010). **Hot tier (5m piggy-back)** в existing `GitHubCollector`: 5 inline emit-site renames + `makeEvent` promoted private→internal + 7 new event_kinds (`gh_pr_review_submitted` discriminator with `pr_review_state` ∈ {approved/changes_requested/commented} + `gh_issue_locked`/`_unlocked` + `gh_workflow_manual_triggered` + `gh_deployment_created` + `gh_deployment_status_changed` + `gh_repo_created` + `gh_repo_forked`). Parsing landed in moat (Task 26): `mapWorkflowDispatchEvent` / `mapDeploymentEvent` / `mapDeploymentStatusEvent` / `mapForkEvent` + extended `mapIssuesEvent` + `mapReviewEvent` + rewrote `mapCreateEvent` (now handles `repository` ref_type with visibility derivation). `EventsFullTextStore.topLevelBodyKind` rewritten — bare `hasPrefix("gh_pr_")` catch-all replaced with explicit `gh_pr_opened/_merged/_closed`/`gh_issue_opened/_closed`/`gh_gist_created/_updated`/`gh_release_published`/`gh_deployment_created` cases (closes spurious-match risk for body-less `gh_pr_review_thread_resolved`/`gh_pr_awaiting_review_count`). **Warm tier (15m)** new `GitHubWarmCollector` actor + `GitHubWarmScheduler` (mirror `LinearWarmScheduler` verbatim — half-interval startup delay, runLoop error swallow): 13 event_kinds across 5 endpoint groups — projectsV2 (3 kinds: `gh_project_card_moved`/`_iteration_changed`/`_field_updated`, top-10 bounded fan-out by `updatedAt` desc, gated on `read:project`) + gists (3: `_created`/`_updated`/`_deleted`, snapshot diff) + repo invitations (2: `_received`/`_accepted`, invitation_id-keyed diff) + codespaces (4: `_created`/`_started`/`_stopped`/`_deleted`, name-keyed with state-transition emission — only `Available`→non-Available emits stopped, non-Available→`Available` emits started; transient states like `Provisioning`/`Building` silent) + issue reactions (1: `_reaction_received`, top-10 viewer-authored issues last 7d via `Database.queryRecentViewerAuthoredIssues`, only positive emoji-bucket deltas emit). Pure static `GitHubWarmCollector.projectsV2Diff(prior:current:)` helper landed in Task 7 (dedicated test file, 10 cases) — collector composes it with bootstrap discipline (priorSnapshotPresent gate). **Cold tier (4am local daily)** new `GitHubColdCollector` + `GitHubColdScheduler` (mirror `LinearColdScheduler` — Calendar-based 4am anchor + `lastColdMs > 24h` catch-up gate + injectable clock/calendar/loginProvider): 11 event_kinds — starred/unstarred set-diff (2: `gh_repo_starred`/`_unstarred`) + watched/unwatched (2: `gh_repo_watched`/`_unwatched`) + security alerts trio (6: secret/code/dependabot × observed/resolved, top-10 active repos × per-repo failure tolerance, gated on `security_events` with one-shot session warn via `loggedSecurityScopeWarn: Bool`) + audit log (1: `gh_audit_action_observed`, gated on `read:audit_log` AND non-empty `fetchOrganizations` membership; cursor in `github_audit_cursor` snapshot, advance via `max(entry.createdAtMs)`). Snapshot value types ship as **non-`Codable` public Sendable Hashable structs** with private inner Codable JSON wrappers translating at snapshot boundaries (mirror Linear D1 cold pattern). Bootstrap discipline: first cold/warm tick writes snapshots, emits zero diff events; Day-2 normal events. **`GitHubScopesService`** actor with **`GitHubScopesChecking`** protocol (Task 8 forward-declared shim — Task 12's actor adds conformance via empty extension): `requiredCore = {repo, read:user, read:org, read:project}` + `requiredOptional = {security_events, read:audit_log}` + `missing()` derived from `integrations.scope` row (whitespace-tolerant parse, multi-space + tab, trimmed). Two init paths — `init(grantedOverride:)` for tests + `init(database:)` for production lazy-fetch with `refresh()` reload semantics. **Re-auth UX ceremony** (first scope-bump in stack history): `ConnectionState.connectedScopeOutdated(login:connectedAt:missing:)` case + free `parseScopeString(_:)` consistency-mirroring Task 12 + `connect(scopes:)` overload accepting explicit scope array (default = `GitHubScopesService.requested()`) → `GitHubScopesReader` `@Observable` wrapper (`@MainActor` + `DistributedNotification("tech.gundem.leaf.github-integration-changed")` subscriber + `nonisolated(unsafe) var observer` for Swift 6 deinit-cleanup) → Home banner `LeafBanner.warning` (proactive on launch, single CTA "Re-authorize" + X-icon dismiss per `LeafBanner` actual API — substrate doesn't support secondary labeled CTA, mapped semantic intent; session-dismiss via per-launch `AppSessionID.current` UUID + `UserDefaults` key `github.reauth.bannerDismissedSessionID`) + Connections "Scopes" section (LeafSection + LeafCard.raised + `LazyVGrid(.adaptive(minimum:110))` for badge matrix per missing FlowLayout in codebase + per-scope explainer `LeafBanner.warning` for missing-core + subtle `LeafType.body.small` hint for missing-optional + "Re-authorize GitHub" `LeafButton` CTA when something missing; renders on both `.connected` informational AND `.connectedScopeOutdated` actionable states; localized DB read for `currentGranted` set since reader doesn't expose it) + Sidebar `LeafDot(tone: .danger, size: .sm)` overlay on Connections nav item (no `LeafBadge.dot` in substrate — used existing Atom A2 LeafDot per Task 20 reviewer). `GitHubOAuthEndpoints.scopes` + `scopeParameter` deprecated via `@available(*, deprecated, message: ...)` annotations pointing to `GitHubScopesService.requested()` / `requestedScopeParameter()` (Task 14 expanded the spec literal scope to also catch the actual call site). **`AgentLifetime`** gains 5 new slots (`githubScopesService`/`githubWarmCollector`/`githubWarmScheduler`/`githubColdCollector`/`githubColdScheduler`); `Agent.main()` constructs warm/cold gated on `!agentThresholds.githubOAuthClientID.isEmpty && scopes != nil`; shutdown chain reverses: cold scheduler → cold collector → warm scheduler → warm collector. **`AgentThresholds` +2 fields** (`githubWarmPollIntervalSec = 900`, `githubColdPollIntervalSec = 86_400`; mirrored `weakDefaults`). LeafApp wires shared `GitHubScopesReader(service: GitHubScopesService(database: openDatabase()))` via `@State` + `.environment(githubScopes)` propagated to Window content (where Home/Connections/Sidebar live; MenuBarExtra skipped — doesn't render those views). **Moat (LeafCorePrivate, gitignored, ~37KB extended `ProdGitHubAPIProvider.swift`):** Task 4.5 sweep already renamed inline `eventKind` literals to `gh_*` form; Task 26 added 7 new hot-tier mappers; Task 27 replaced 12 stub returns from Task 6 with real impls — GraphQL `viewer { projectsV2(first:10, orderBy:{UPDATED_AT,DESC}) }` query + 11 REST endpoints (`/users/<login>/gists`, `/user/repository_invitations`, `/user/codespaces`, `/repos/{o}/{r}/issues/{n}/reactions`, `/users/<login>/starred`, `/users/<login>/subscriptions`, `/repos/{o}/{r}/secret-scanning/alerts`, `.../code-scanning/alerts`, `.../dependabot/alerts`, `/user/orgs`, `/orgs/{org}/audit-log`) + shared `httpGet` / `parseISOToMs` / `parseNextLink` helpers + 50-page hard cap on audit-log Link-header pagination + per-method try/catch graceful degrade (non-200/parse-fail/network → `.empty` or `[]`; scope-related 403 → empty + collector-tier one-shot warn). Defensive: secret-scanning severity defaulted to `"high"` since endpoint has no per-alert severity field; ProjectsV2 GraphQL field shape may need real-API smoke during acceptance gate (mirror Linear introspection sweep precedent — fragments sized for typical workspace, may need correction post-smoke). **ShareEventTypeRegistry 66 → 97** — 21 GitHub raw values renamed to `gh_*` form (Swift identifiers preserved for call-site stability), 31 new defaults-OFF entries per ADR-020 + flipped `githubReviewSubmitted` default ON→OFF per Task 5 spec ("treat as new" per discriminator-only upgrade — user-visible default change). **`testEnumRawValuesMatchGitHubEventKindKey`** added in Task 5 — doubles as part of fence (1:1 enum/registry mirror). **Privacy regression** (ADR-010 §6) — 9 new `RelayBodyLeakageTests` walkbacks (23 total): gist descriptions / release bodies / deployment descriptions / projectV2 field values / codespace names / invitation sender logins / security alert rules / audit actions / issue reaction emoji bodies — none leak in `presence_state.state_json`. **`DispatchCoverageTests`** (anti-D1-drift fence, the lesson encoded from Linear schema reconciliation): 4 assertions iterating `GitHubEventKindKey.allCases` proving (1) every case appears in ShareEventTypeKey rawValue set, (2) `legacyRenamed` ↔ M016 renameMap targets 1:1, (3) every `bodyBearing` case has `topLevelBodyKind` dispatch entry (via `bodyKindForTesting(eventKind:)` public test helper), (4) every case has `ShareEventTypeDefaults.all` entry. Test #3 surfaced discussion-in-bodyBearing gap during Task 23 — resolved by pruning `discussionAuthored`/`discussionCommentAuthored` from `bodyBearing` (option b: moat doesn't capture discussion bodies per ADR-010 §6, would be phantom dispatch entry). **1464 SPM tests** (1283 baseline + 181 new D2 — exceeded spec target of +70-90 due to Task 26+27 moat fixture coverage thoroughness; 0 failures, 1 skipped baseline xfail) + **5/5 xcodebuild schemes green** (Leaf / LeafAgent / LeafCore / LeafCorePrivate / LeafMCP) + `just check-tokens` PASS + `just check-tokens-self-test` PASS. **28 SQLCipher tables** (M016 is data migration not new table, M001-M016 total). M011/M012/M013 migration-count tests bumped 15→16 (Task 1 surfaced gap, fixed in Task 4.5 sweep). **Не merged в main** — Track 3 stack (D1+linear-reconciliation+D2+D3+D4) ждёт коллективный merge after Track 3 acceptance gate per design spec §13: manual smoke на real GitHub workspace (star repo → next cold tick emits `gh_repo_starred` row; revoke `read:project` scope → reader transitions to `connectedScopeOutdated` → Home banner + Sidebar dot + Connections explainer; re-authorize → grants → banner clears + projectsV2 fetches resume; etc.). **Whitepaper sync deferred** до Track 3 ship per design spec §13 — D2 — implementation moat (GraphQL fragments, REST endpoint shapes, scope discovery logic, audit cursor format, `fetchEvents` parser switch shape, Task 26 mapper internals) не публикуется; architectural framing на public-safe уровне ("GitHub coverage expanded to ~50 event_kinds across hot/warm/cold tiers; OAuth scope-bump UX surfaced as ambient reauth ceremony with proactive banner + per-launch session-dismiss + Sidebar attention indicator") пойдёт в `leaf-docs` after full Track 3 collective merge in separate session. **Open follow-ups for Track 3 acceptance gate / next session:** (a) D2 `LeafBanner` API limitation — single CTA + X-dismiss only; if labeled "Dismiss for now" button is design-required, extend substrate with `secondaryCtaTitle:`/`onSecondaryCTA:` slot. (b) `GitHubScopesReader` doesn't expose `currentGranted` Set — Connections section opens local DB read on each scope update; if a 3rd consumer surfaces, hoist into reader. (c) ProjectsV2 GraphQL real-API field shape may need correction (Linear precedent — schema introspection sweep during acceptance gate revealed field renames). (d) Migration-count tests M011/M012/M013 bumped to 16 — pre-existing brittle pattern; refactor to `>= N` lower bound. Tactical: `docs/superpowers/plans/2026-05-12-track-3-D2-github-deep-sweep.md` (gitignored), spec: `docs/superpowers/specs/2026-05-12-track-3-D2-github-deep-sweep.md`, contract: `docs/superpowers/specs/2026-05-11-track-3-providers-deep-sweep-design.md`.

**2026-05-12 — Linear schema reconciliation pass landed на `feature/linear-schema-reconciliation-2026-05-11`** (off `feature/track-3-D1-linear-deep-sweep`). Hotfix workstream — не Track 3 D2. Track 3 D1 ship'нулся, acceptance gate smoke выявил 10 Linear API schema drift bugs накапливавшихся с Phase 4.6.B / 4.7.C / Track-1 D1 (тихо ломали production alpha.10). Full introspection-driven correction через Linear `__schema` API: **3 GraphQL query bodies переписаны** (LeafPoll hot / LeafWarm warm / LeafCold cold), **6 parsers пере-wired** к schema-truthful field paths. **Все правки в `LeafCorePrivate/Prod/Collectors/ProdLinearGraphQLProvider.swift` (gitignored moat) + его fixture tests** — substrate (atomic write, schedulers, M015 registry, ShareEventTypeRegistry 66 entries) untouched, public ABI preserved (`LinearColdBatch.roadmaps` остаётся в struct как always-empty). **Resurrections (16 из 18 новых Track-3 D1 event_kinds работают):** notifications trio через top-level `notifications` + `... on IssueNotification { issue { ... } }` inline fragment (Notification — INTERFACE из 10 subtypes); subscription added/removed через `subscribed: issues(filter:{subscribers.some.isMe})` alias; cycle started/completed через `Cycle.currentProgress` rename (parser dual-key fallback); relation_added через `relationChanges { identifier type }` re-enable (block previously disabled per D1 fix #7); triage pick-up/resolved уже работали; project_membership added/removed через replacement найден — top-level `projects(filter:{members.some.isMe})` returns projects где viewer is member; initiatives re-enabled как top-level `initiatives(first:25)` piggy-back в hot tier (User.initiatives doesn't exist; semantically same heartbeat). **Permanent zero-emit (2):** `linear_relation_removed` — Linear lost add/remove direction discriminator (`IssueRelationHistoryPayload` reduced to `{identifier, type}`, no `relatedIssue` nested + no `flavor` on IssueHistory); v1.1 candidate = cache-diff approach. `linear_roadmap_state_observed` — Linear deprecated `roadmaps` query ("use initiatives instead"). **Permanent drops (schema-confirmed):** `IssueHistory.{type, flavor}`, `Attachment.contentType`, `Comment.reactions(filter:)`, `User.{notifications, subscribedIssues, projectMemberships, initiatives, customViews}`. **Field renames:** `Cycle.progress` → `Cycle.currentProgress` (both queries + parsers with dual-key forward-compat fallback). **Fixture tests:** targeted edits to 7+ existing tests (cold tier payloads, warm tier viewer-block restructure, query-shape assertions, relation discriminator semantics) + 1 new test `testWarmState_NotificationsMixedSubtypes_Reconciliation` (IssueNotification + DocumentNotification graceful degrade per inline fragment scope). **1283 SPM tests** (1282 baseline + 1 new, 0 failures, 1 skipped) + **5/5 xcodebuild schemes green** (Leaf / LeafAgent / LeafCore / LeafCorePrivate / LeafMCP) + `just check-tokens` PASS. **Code review verdict: ACCEPT** (independent subagent verified all 15 spec matrix items against actual code line numbers; tests exercise new shapes, not just compile). **Live smoke deferred to acceptance gate** — Debug Release build ad-hoc signed, Gatekeeper rejects (`spctl: rejected`); нужен properly-signed build через `release.sh` (стандартный flow). alpha.10 в `/Applications/Leaf.app` восстановлен из backup. **Public commits на branch:** 4 — spec (a1d481d), spec amendment initiative tier (b696a62), spec amendment relation_removed (5fb103f), current-state update (this commit). Все moat changes invisible to git (gitignored, ~150 lines edited в ProdLinearGraphQLProvider.swift + ~250 lines в fixture tests). **Не merged в main** — reconciliation merges вместе с Track 3 stack (D1+D2+D3+D4) после Track 3 acceptance gate per design spec §13. Whitepaper sync deferred — implementation moat (GraphQL fragments, deprecation status, query shapes) не публикуется; risk addendum "External provider schema drift — Linear/GitHub/Slack APIs may change fields without notice; mitigation via introspection in pre-merge smoke + graceful degrade pattern" пойдёт в `leaf-docs/docs/05-reference/decisions.md` отдельной session после Track 3 ship. **TODO юзеру:** revoke Personal API key создан для introspection at https://linear.app/settings/api **before merge'ом в main** (token в conversation history). Tactical: `docs/superpowers/plans/2026-05-12-linear-schema-reconciliation.md` (gitignored), spec: `docs/superpowers/specs/2026-05-12-linear-schema-reconciliation.md`.

**2026-05-11 — Phase Track-3 D1 (Linear Deep Sweep) landed на `feature/track-3-D1-linear-deep-sweep`** (off `main`, Track-2 D4 baseline 1213 SPM tests). Track 3 first sub-phase: Linear coverage expanded 14 → 32 event_kinds (18 new) через three-tier cadence + atomic write generalization + D2 carry-over FTS dispatcher fix. **13 atomic commits** через 8-step phase-session workflow (Discovery → Brainstorm → Spec → Plan → TDD impl → Spec review → Code-quality review → Verification → Ship). **M015** generic `provider_snapshots(provider, snapshot_kind, snapshot_json, captured_at_ms)` composite PK `WITHOUT ROWID` — D2/D3 reusable, D1 ships 4 snapshot_kind values (linear_subscribed_issues / linear_custom_views / linear_project_memberships / linear_roadmap_state). **`Database.writeEventsOffsetsAndSnapshots`** atomic helper — single GRDB transaction wrapping events[] + offsets[] + snapshots[] (partial arrays allowed, rollback on any throw). **Hot tier (5m) piggy-back** в existing `LinearCollector` через extended `fetchIssues` query: 5 new event_kinds (`linear_comment_reaction_added` viewer-filtered server-side, `linear_relation_added`/`_removed` blocks/blocked_by/related/duplicate, `linear_triage_item_picked_up`/`_resolved` discriminated by Linear WorkflowState.type enum). Plus 5 new `make*Event` static methods + wiring между existing initEvents и workload pulse. **Warm tier (15m)** new `LinearWarmCollector` actor + `LinearWarmScheduler` (interval task loop, intervalSec/2 stagger): 7 event_kinds (`linear_notification_received` + `_read` + `_archived` via `viewer.notifications` cursor compare with OQ-D1-1 `updatedAt`→`createdAt` fallback; `linear_cycle_started`/`_completed` provider-bucketed via `viewer.teams.activeCycle`; `linear_subscription_added`/`_removed` via set-diff against `linear_subscribed_issues` snapshot — bootstrap emits zero subscription events, just writes snapshot). Notification titles routed через `linear_notification_title` FTS body_kind для UC1/UC3 search. **Cold tier (4am local daily)** new `LinearColdCollector` + `LinearColdScheduler` (Calendar-based 4am anchor + `lastColdMs > 24h` catch-up gate; injectable clock/calendar/workspaceIDProvider, `nextLocal4am` + `shouldCatchUp` exposed `public nonisolated` для tests): 6 event_kinds (`linear_roadmap_state_observed` heartbeat-per-(roadmap×project) every tick mirroring `linear_initiative_observed` precedent; `linear_custom_view_created`/`_updated`/`_deleted` via diff with name OR updatedAtMs change as update trigger; `linear_project_membership_added`/`_removed` via set-diff by projectId). Bootstrap discipline gates diff emissions on snapshot row presence — first cold tick emits only roadmap heartbeats, writes all 3 snapshots, Day-2 diff produces normal events. **Track-1 D2 carry-over fix** — dispatcher checked `"linear_issue_updated"` but `LinearCollector` emits `"issue_updated"` без `linear_` prefix; bug existed in 3 parallel dispatchers (FTS / EventLinks / DetectorPipeline) + 6 dependent test files — все исправлены в Task 6 atomic commit (root-cause fix). UC1/UC3 Linear bodies теперь indexed в FTS. **`ShareEventTypeRegistry` 48 → 66** (+18 new D1 kinds, **все default OFF** per ADR-020 — capture-everything locally, share-selectively через Share Controls). **38 new EventPayloadKeys** + `BodyKinds.linearNotificationTitle` + `CollectorID.linearWarmPolling`/`linearColdPolling`. **`LinearIssueBatch`** extended +5 backward-compat fields (all `= []` defaults preserving 22+ existing call sites). 12 new public snapshot value types: `LinearWarmCursors`, `LinearCommentReactionSnapshot`, `LinearRelationSnapshot`, `LinearTriageTransitionSnapshot`, `LinearNotificationSnapshot`, `LinearSubscribedIssueSnapshot`, `LinearCycleLifecycleSnapshot`, `LinearWarmBatch`, `LinearRoadmapProjectSnapshot`, `LinearRoadmapSnapshot`, `LinearCustomViewSnapshot`, `LinearProjectMembershipSnapshot`, `LinearColdBatch` — все `public Sendable Hashable` с public init + `static let empty` constants. Protocol `LinearGraphQLProvider` gains `fetchWarmState(accessToken:cursors:)` + `fetchColdState(accessToken:)` методы. **`AgentLifetime`** gains 4 new slots (linearWarmCollector / linearWarmScheduler / linearColdCollector / linearColdScheduler); `Agent.main()` constructs warm/cold gated on `!agentThresholds.linearOAuthClientID.isEmpty`; shutdown chain stops cold scheduler → cold collector → warm scheduler → warm collector между `rotationFetchScheduler` и `maintenance`. **`AgentThresholds` +2 fields** (`linearWarmPollIntervalSec`=900, `linearColdPollIntervalSec`=86_400; defaults в weakDefaults + moat ProdConfigs.agent). **Privacy regression** (ADR-010 §6) — 3 new walkbacks в `RelayBodyLeakageTests` (14 total): notification_title / reaction emoji / relation+triage metadata НЕ leak в `presence_state.state_json`. **Swift 6 strict concurrency**: `LinearColdScheduler.workspaceIDProvider` + `clock` closures marked `@Sendable` для actor isolation safety. **Moat (LeafCorePrivate, gitignored, +1517 lines source + tests):** `ProdLinearGraphQLProvider` extended с full GraphQL bodies для `fetchWarmState` (3 top-level fields под viewer + per-endpoint failure tolerance + OQ-D1-1 createdAt fallback) + `fetchColdState` (roadmaps + customViews + projectMemberships + 401 graceful) + 5 piggy-back fragments в `fetchIssues` (reactions filter + relation history dispatch + triage classification по WorkflowState.type) + complexity-error fallback (scans GraphQL errors[] for complexity/rate-limit/quota signatures → emit existing 14 event_kinds, new 5 empty arrays, cursor still advances). +14 moat fixture tests covering happy/partial/complexity-error paths. **1282 SPM tests** (1213 baseline + 69 new, spec §10 target +40-50 exceeded due to moat contributions; всё passing, 1 skipped, 0 failures) + **5/5 xcodebuild schemes green** (Leaf / LeafAgent / LeafCore / LeafCorePrivate / LeafMCP) + `just check-tokens` PASS. **28 SQLCipher tables** (27 baseline + presence_state и pre-existing + M015 provider_snapshots). **18 new event_kinds** + 12 baseline = 30 total Linear event_kinds. Token-discipline guard untouched (D1 не трогает UI). **Не merged в main** — Track 3 stack (D1 → D2 → D3 → D4) ждёт коллективный merge после Track 3 acceptance gate per design spec §13: manual smoke 15 acceptance criteria на real Linear workspace (comment reaction → `linear_comment_reaction_added` row; @-mention notification → `linear_notification_received` + FTS hit; cycle start → `linear_cycle_started`; 4am cold tick → roadmap heartbeats + `provider_snapshots` 4 rows; etc.). Whitepaper sync deferred до Track 3 ship per design spec §13 — D1 — implementation moat (GraphQL fragments, complexity-error signatures, polling intervals) не публикуется. Tactical: `docs/superpowers/plans/2026-05-11-track-3-D1-linear-deep-sweep.md`, spec: `docs/superpowers/specs/2026-05-11-track-3-D1-linear-deep-sweep.md`, contract: `docs/superpowers/specs/2026-05-11-track-3-providers-deep-sweep-design.md`.

**2026-05-10 — Phase Track-2 D4 (Final Migration & Cleanup) landed на `feature/track-2-D4-final-migration-and-cleanup`** (off `feature/track-2-D3-data-surfaces`; D3 ещё не merged в main, Track 2 stack D1→D2→D3→D4 ждёт коллективный merge после Track 2 acceptance gate). **Track 2 closed на substrate-уровне.** Все production views мигрированы in-place на D1 substrate (Snapshot Replacement complete); старая палитра retired hard delete'ом; token-discipline guard финализирован третьим RETIRED tier'ом. **20 atomic commits** (T1-T19 implementation + T20 verification + 1 review follow-up): Wave 1 leaf nodes (ShareTemplateButton / WaitingForInviteView / CreateTeamStepView / JoinTeamStepView), Wave 2 sheets (AcceptInviteSheet / GenerateInviteSheet / RemoveMemberSheet — все на `LeafSheetLayout(title:, onDismiss:)` + `LeafCard.raised` inner blocks + `LeafBanner.danger` errors + `LeafIconLabel` success states; ad-hoc `.padding(28).frame(width:height:)` dropped), Wave 3 Settings (GeneralSettingsSection / PrivacySettingsSection / FoldersSettings / WindowSettingsView host — drop `Form(formStyle: .grouped)` chrome universally per OQ-1; LeafSection chain throughout; FoldersSettings native `Picker(.segmented)` для L4/L5 property-setter preserved per OQ-11; folder row ad-hoc HStack — LeafListRow doesn't fit nested Picker+Toggle), Wave 4 windows (OrganizationView / ProfileView / RemovedFromTeamBanner / MenuBarContent / OnboardingView root — ProfileView `LeafAvatar.lg` (56pt) + `LeafMetricCard` × 2 LazyVGrid с caption параметром fold'нутым в title per OQ-13; RemovedFromTeamBanner на `LeafEmptyState`; MenuBarContent `LeafMenuBarLayout` 360pt (+80pt vs старых 280 per substrate-as-source-of-truth OQ-2/OQ-16) + `LeafBanner.warning` × 3 inline replacing BannerView + native borderless Quit preserved per OQ-19; OnboardingView inline migration NOT `LeafOnboardingStepLayout` per OQ-20 (popover constraint 320pt fixed)), Wave 5 retirement + guard (T17 token previews — `LeafProminentButton`/`LeafSecondaryButton` → `LeafButton`; T18 hard delete 8 files: `Leaf/Theme/{Colors,Fonts,GlassModifiers,CategoryColors}.swift` + `Leaf/Views/Window/Shared/{GlassCard,EmptyStateView,MetricCard}.swift` + `Leaf/Views/BannerView.swift` + empty `Leaf/Views/Window/Shared/` dir; `LeafType.swift` doc-comment refresh × 2 lines (label-doc + leafSectionLabel-doc); T19 check-tokens RETIRED tier banning old-palette names anywhere в `Leaf/`). **Token-discipline guard final form** (`scripts/check-tokens.sh`): three-tier scope — Tier 1 BASE (`Leaf/Theme/` + `Leaf/Views/Tokens/` — bans raw colors/padding/cornerRadius), Tier 2 MIGRATION (entire `Leaf/Views/` — inherits BASE + bans old-palette refs), Tier 3 RETIRED (entire `Leaf/` — bans old-palette names anywhere as defense-in-depth alongside compile-time deletion); 11 RETIRED-tier checks (Color.leaf*/.leaf<ShapeStyle>/Font.leaf*/.font(.leaf*)/`.leafLabelStyle()`/`GlassCard`/Leaf{Prominent,Secondary}Button/LeafGlassGroup/`MetricCard` exclude LeafMetricCard family/`EmptyStateView`/`BannerView`/`Color.leafCategory(_:)`); MIGRATION pattern bug fix (latent через D2/D3) — dropped `Glass|GlassGroup` from old-palette alternation (legitimate D1 ViewModifier `.leafGlass(...)` was tripping false-positive). Self-test extended 17 → 23 cases (Case 7 flipped pass→fail per RETIRED tier; Cases 18-23 cover Color.leafInk/leafLabelStyle/GlassCard fail paths + LeafMetricCard exclude-regex preservation + MIGRATION whole-Views/ clean+bad fixtures). **Architectural decisions per OQ-decided spec (35 OQs)**: sheets adopt template w/o glass-on-glass (LeafCard.raised inside LeafSheetLayout — OQ-3); WindowSettingsView part of unified product surface, NOT macOS Settings scene (OQ-1); LeafTab для GenerateInviteSheet inputMode picker mirroring D3 Activity precedent (OQ-6); Folders empty state — `LeafEmptyState` (OQ-10); ProfileView LeafMetricCard caption fold into title — single use-site не warrant substrate extension (OQ-13); MenuBarContent FOCUS-TODAY hero — ad-hoc Type composition not LeafMetricAmbient overkill (OQ-17); OnboardingView template not adopted — full-bleed template ambiguous in MenuBarExtra popover без external maxHeight constraint (OQ-20); hard delete old palette over deprecated stub per CLAUDE.md root rule (OQ-22). **Architectural anchor preserved per OQ-19**: native borderless `Button("Quit") { NSApplication.shared.terminate(nil) }.buttonStyle(.borderless).keyboardShortcut("q")` для menu-bar Quit — macOS conventional pattern, LeafButton.ghost reads heavier than expected. **5/5 xcodebuild schemes green** (Leaf / LeafAgent / LeafCore / LeafCorePrivate / LeafMCP) + **1213 SPM baseline preserved** (zero new tests — D4 — pure UI substrate, manual-smoke-only validation per AC1-14). **Не merged в main** — Track 2 stack (D1→D2→D3→D4) ждёт Track 2 acceptance gate (manual smoke per AC1-14 на собственных рабочих данных + collective merge юзером). Whitepaper sync deferred per OQ-29 — D4 — implementation moat (component внутренности, retirement decisions, RETIRED tier regex); architectural framing на public-safe уровне ("Track 2 — full design-language refresh: tokenized 3-tier substrate + atoms/molecules/organisms/templates библиотека + миграция всех production surfaces. Old palette retired.") попадёт в whitepaper после полного Track 2 merge. Tactical: `docs/superpowers/plans/2026-05-10-track-2-D4-final-migration-and-cleanup.md`, spec: `docs/superpowers/specs/2026-05-10-track-2-D4-final-migration-and-cleanup.md`.

**2026-05-10 — Phase Track-2 D3 (Data Surfaces) landed на `feature/track-2-D3-data-surfaces`** (off `feature/track-2-D2-navigation-and-home`; D2 ещё не merged в main, Track 2 merges коллективно после D4). Activity / Team / Connections мигрированы in-place на D1 substrate (Snapshot Replacement). **9 atomic commits** (T1-T9 implementation, T10 = verification-only sweep). **Activity:** `SessionRow` rewrite на LeafListRow (drop decorative category dot — app icon + name carry identity); `ActivityRow` rewrite на LeafListRow + neutralised ProviderIcon (LeafColor.surface.inset background, LeafColor.text.tertiary symbol tint, single-tone — no purple-Linear/green-Slack chip backgrounds); `ActivityView` full rewrite на LeafTab mode picker (Sessions / Raw events) + LeafTab filter (rawEvents-only, 6 tabs с counts) + 5-state UX (loading / notConfigured / empty / error / loaded) + ActivityMode promoted to Identifiable for LeafTab generic constraint. **Team:** `PendingInviteRow` на LeafAvatar + LeafBadge + LeafIconButton; `PendingInvitesSection` на LeafSection + LeafCard + LeafBanner.info pollMessage + LeafBanner.danger error; `TeamView` full rewrite на adaptive LazyVGrid (`GridItem(.adaptive(minimum: 240, maximum: 360))`) + LeafEmptyState gain-framed («You're solo for now» + «Open Organization» CTA) + Add member CTA в LeafSection cta-slot (top-right, replaces floating bottom button). **Connections:** `ConnectionsView` full rewrite + folds `Leaf/Views/ConnectionsSettings.swift` inline (Phase 4.1 lone Settings-tab artifact deleted) — 3 LeafSection blocks per provider, native Form chrome gone, per-provider state machine preserved 1:1 (notConnected / progress / awaitingAuthorization / connected / reconnectNeeded / error); GitHub `.awaitingAuthorization` user-code rendered with `LeafType.mono.large` (28pt SF Mono semibold — additive substrate token landed в T7 mirroring D2's `LeafStatusPillTokens.activeThresholdSeconds` precedent). **Token-discipline guard** (`scripts/check-tokens.sh`): MIGRATION_PATHS extended +5 entries (Activity dir / Team file-level for TeamView+PendingInvitesSection+PendingInviteRow / Connections dir); sheets `GenerateInviteSheet` + `RemoveMemberSheet` deliberately excluded (D4 carry-over). Self-test extended 13→17 cases (Activity dir clean+bad, Team file-level via env override, Connections dir clean). **5/5 xcodebuild schemes green** (Leaf / LeafAgent / LeafCore / LeafCorePrivate / LeafMCP) + **1213 SPM baseline preserved** (zero new tests — D3 — pure UI substrate, manual-smoke-only validation). **Не merged в main** — Track 2 stack (D1 → D2 → D3 → D4) ждёт коллективный merge после Track 2 acceptance gate (manual smoke per 12 AC юзером перед merge'ом). Whitepaper sync deferred до Track 2 ship — D3 — implementation moat (component внутренности, token usage, layout decisions); architectural framing «Activity/Team/Connections migrated on D1 substrate» попадёт в whitepaper после полного Track 2 merge. Tactical: `docs/superpowers/plans/2026-05-10-track-2-D3-data-surfaces.md`, spec: `docs/superpowers/specs/2026-05-10-track-2-D3-data-surfaces.md`.

**2026-05-10 — Phase Track-2 D1 (Design Foundation) landed на `feature/track-2-D1-design-foundation`** (off `main`, Track-1 D3 baseline 1213 SPM tests). Track 2 первая sub-phase: полный rewrite дизайн-языка — atomic 3-tier T1/T2/T3 token-система (primitive → semantic → component) + 28 компонентов (4 atoms · 10 molecules · 10 organisms · 4 metric primitives) + 4 templates + TokensPreview debug-only screen за `⌘⌥T` + light/dark с system sync + macOS 14 Material fallback / macOS 26 Liquid Glass через `EnvironmentValues.forceLegacyGlass` + `\.leafReduceMotionOverride` debug overrides. **44 atomic commits** через 7 sessions: Session 1 token foundation (T1 primitives + T2 LeafColor/Type/Space/Radius/Elevation/Glass/Motion + 19 Asset Catalog colour sets + TokensPreview shell), Session 2 atoms A1-A4, Session 3 molecules M1-M10, Session 4 organisms O1-O10, Session 5 metrics MT1-MT4 + raw→T3 token deviations (`LeafMetricTokens.Delta.arrowSize:11`, `LeafMetricTokens.Sparkline.minHeight:24`), Session 6 templates T1-T4 + raw→T3 token deviations (`LeafWindowLayoutTokens.sidebar 220/260/320`, `LeafSheetLayoutTokens.minWidth:480/minHeight:360`, `LeafMenuBarLayoutTokens.width:360`, `LeafOnboardingStepLayoutTokens.progressBarHeight:4/contentMaxWidth:560`) + static mockup pattern для full-bleed templates inline previews (NavigationSplitView ignore .frame bounds; .frame(maxWidth/Height: .infinity) propagates fill — рендерим semantic mockup используя те же tokens вместо актуального layout sub-window scale), Session 7 spec-string audit + acceptance gate. **Token-discipline guard** (`scripts/check-tokens.sh` + `just check-tokens`/`-self-test` recipes, run pre-commit ad-hoc): forbids raw `.padding(<int>)` / `cornerRadius:<int>` / `RoundedRectangle(cornerRadius: <int>)` outside Theme/Tokens; CLI fixture self-test + runtime real-codebase break+revert verified. **EnvironmentKey propagation**: `forceLegacyGlass` (LeafGlassModifier reads via `@Environment` → `glassEffect` macOS 26 vs `.regularMaterial` fallback) + `leafReduceMotionOverride` (LeafAnimationModifier collapses to `.linear(duration: 0)` если system OR debug override true; LeafStatusPill pulse halo respects). **Зеро regressions** на production screens: existing Activity / Team / Connections / Settings / Onboarding migrate per-phase в D2+ (Snapshot Replacement migration), D1 — substrate-only. **5/5 xcodebuild schemes green** (Leaf / LeafAgent / LeafCore / LeafCorePrivate / LeafMCP) + **1213 SPM baseline preserved** (zero new tests — D1 — pure UI substrate, нет logic worth automating, validated runtime через TokensPreview). **Не merged в main** — стэк ждёт Track 2 D1 acceptance gate (юзер manually decide когда merge); whitepaper sync deferred — D1 — implementation moat (component внутренности, token values, spec strings) не публикуется, architectural framing на high-level («design system foundation: tokenized atoms→molecules→organisms→metrics→templates») может попасть в whitepaper отдельным sync после merge. Tactical: `docs/superpowers/plans/2026-05-10-track-2-D1-design-foundation.md`, spec: `docs/superpowers/specs/2026-05-10-track-2-D1-design-foundation.md`.

**2026-05-09 — Phase Track-1 D3 (Detectors + structured MCP tools) landed на `feature/track-1-D3-detectors-and-structured-mcp`** (off `feature/track-1-D2-fts-and-link-graph`, D2 baseline 1108 SPM tests, M001-M013). Track 1 третья и последняя sub-phase: 5 pattern-based detectors (zero LLM) + 3 high-level structured MCP tools + M014 schema. **13 atomic commits** per TDD discipline + два-stage review (spec compliance + code quality) после каждого. **M014** new tables `decisions` / `open_questions` / `blockers` / `where_stopped_log` / `detector_offsets` (8 indexes incl. partial unique `idx_blockers_open` WHERE resolved IS NULL и 4 partial filtered indexes на open_questions; 3 pre-seeded detector_offsets rows). Logical FKs к `events.id` без SQL FOREIGN KEY (repo convention — mirror M013). **`DetectorPipeline`** (LeafCore) с `runIncremental` (per-event cursor pass dispatching bodies к decision/open_question/blocker_pattern detectors через тот же body-kind dispatch table что D2 EventsFullTextStore — 9 kinds) + `runScheduled` (idle scheduler — LinearStuckScanner + WhereStoppedDeriver). **Open-question resolution flow**: при emission'е decision row для event E pipeline derives E's context (Slack thread_ts из payload + Linear issue refs + GitHub PR refs из `event_links`) и UPDATE'ит `open_questions.resolved_by_event_id` для всех unresolved rows sharing context — single transaction with the decision INSERT, atomic rollback. **`DetectorMoat` injection struct** (LeafCore, 6 protocol-typed fields + 6 NoOp/ExactMatch `.publicSubstrate` impls) — mirrors D2 LinkDerivers boundary. **`DetectorScheduler` actor** (LeafCore Agent layer) — два sibling Task loops (incremental 30s + scheduled 300s cadence) mirroring `RotationFetchScheduler` pattern; errors caught + logged via os.Logger но никогда не propagate (collector tick stays green). Wired в `Agent.swift` + `AgentLifetime` shutdown chain (детектор stops first — purely read-side from collectors' POV). **`QueryEngine`** (LeafCore) с тремя composition methods — `queryActivity(period:filter:)` / `getDecision(topic:period?:)` / `currentWork(nowMs:)` — over events + FTS + links + detector tables + on-the-fly absence flags; SQL `LIMIT 200` backstop + post-serialize 64KB byte budget с oldest-event trim (`removeLast()` на ts-DESC list) + `truncationNote`. **3 new MCP tools** (`leaf_query_activity` / `leaf_get_decision` / `leaf_current_work`) wired в `MCPServer.swift` (12 → 15 tools). Schema versioning `schemaVersion: "1.0"` top-level field на каждом QueryEngine response (single source of truth `QueryEngineSchema.currentVersion`). **Testable substrate split**: D3ToolSchemas / D3ToolParams / D3ResponseEncoder в LeafCore (LeafMCP не имеет SPM test target — substrate split позволяет xcodebuild-free testing of tool definitions + handle dispatch). **Moat (LeafCorePrivate, gitignored, 8 source + 8 test files):** `ProdDecisionDetector` (en+ru pattern catalogue, confidence weighting, sentence-bounded excerpt extraction) / `ProdOpenQuestionDetector` (question-mark + interrogative cues + alternative extraction на " or " / " vs " / " либо ") / `ProdBlockerPatternDetector` (en+ru "blocked on" / "stuck" / "заблокирован" patterns) / `ProdLinearStuckScanner` (configurable stuckDays threshold, status filter via WorkflowState.name, window-function SQL для latest-transition lookup) / `ProdWhereStoppedDeriver` (idle window threshold, excerpt construction "commit · ticket · file") / `ProdAbsenceMatcher` (3-tier exact → substring → Levenshtein fuzz match, Wagner-Fischer impl) / `BodyExcerptCapPrivate` / `ProdDetectorMoat` factory. Public substrate ships только NoOp impls + `ExactMatchAbsence` (userID == githubLogin). Prod factory wired через `#if LEAF_PROD` branch в обоих `MCPServer.swift` + `Agent.swift`. **Privacy regression** (Track 1 контракт §6 amendment): 5 новых тестов в `RelayBodyLeakageTests` — decision / open_question / blocker / where_stopped excerpts + cross-table walk НЕ leak в `presence_state.state_json` (`writeEventsOffsetAndPresence` signature unchanged per Commit 9 architecture decision; tests call `DetectorPipeline.runIncremental` / `runScheduled` explicitly post-write). **ShareEventTypeKey registry 43 → 48** (+5: `decision_detected` / `open_question_opened` / `open_question_resolved` / `blocker_started` / `blocker_resolved`, все default OFF — semantic facts must be opt-in per контракт §5.3). **Contract §5.2 amendment**: `where_stopped_log` table заменяет планировавшийся `sessions` extension (substrate не имеет sessions table — Derived Insights Engine compute on-the-fly). **1213 SPM tests** total (1108 baseline + 105 new D3 tests, target ~1195-1210) + 5/5 xcodebuild schemes green. **Не merged в main** — стэк Track 1 (D1 → D2 → D3) merges коллективно после Track 1 acceptance gate per контракт §13: manual smoke UC1/UC3/UC4/UC5/UC6 на собственных рабочих данных через MCP, Claude Code формулирует sensible narrative из structured JSON в пределах 3s end-to-end. Whitepaper sync deferred до Track 1 ship per контракт §12. Tactical: `docs/superpowers/plans/2026-05-09-track-1-D3-detectors-and-structured-mcp.md`, spec: `docs/superpowers/specs/2026-05-09-track-1-D3-detectors-and-structured-mcp.md`.

**2026-05-09 — Phase Track-1 D2 (FTS5 + event_links) landed на `feature/track-1-D2-fts-and-link-graph`** (off `feature/track-1-D1-capture-extension` off `feature/track-1-detection-substrate` off `main`, D1 baseline). Track 1 second sub-phase: FTS5 keyword index over D1-captured bodies + cross-source `event_links` graph + atomic write-path refactor + privacy regression. **6 atomic commits** per TDD discipline. **M012** `events_fts` FTS5 contentless virtual table (tokenizer `unicode61 remove_diacritics 2 tokenchars '_-'` для ru/en + snake/kebab-case identifiers) + sidecar `events_fts_meta(fts_rowid, event_id, body_kind)` (contentless mode не retain'ит UNINDEXED columns post-MATCH; sidecar — source of truth для retrieval, написан атомарно вместе с FTS5 INSERT). **M013** `event_links(from_event_id, link_kind, target_kind, target_ref, confidence, created_at_ms)` composite PK + reverse-lookup index `idx_event_links_target`. **`EventsFullTextStore`** (LeafCore) с `indexEvent` (body fan-out per body_kind dispatch table — 9 kinds: linear_desc/linear_comment/slack_msg/slack_thread_parent/slack_thread_reply/gh_pr/gh_issue_comment/gh_pr_review_comment/commit_msg) + BM25 `search` over period. **`EventLinksStore`** (LeafCore) с `deriveLinks` / `eventsLinkingTo` / `linksFrom`. **`LinkDerivers` injection struct** в LeafCore (closures + `LinkConfidenceValues`) — LeafCore не может импортировать LeafCorePrivate, moat reach через `prodLinkDerivers()` factory. 5 link_kinds: `linear_id_in_text` / `branch_name_linear_ref` / `pr_url_in_slack` / `pr_number_hash_ref` / `reviewer_assigned`. **`LinearIDExtractor.extractAll`** sibling (existing first-match `extract` untouched). **Atomic write refactor:** все 3 `Database` write entry-points (`write(_:)`, `write(_:[RawEvent])`, `writeEventsOffsetAndPresence`) рефакторены через shared `writeEventAndDerived` private helper внутри single `pool.write {}` transaction; defaulted `knownLinearPrefixes: Set<String> = []` + `derivers: LinkDerivers = .publicSubstrate` parameters (existing 3 collector callers compile unchanged). Failure-rollback test (`Date` injection в presence state ловится `JSONSerialization.isValidJSONObject`) подтверждает atomicity. **Moat (LeafCorePrivate, gitignored):** `LinkConfidence` numeric values + `BranchNameLinearParser` + `PRURLParser` + `PRHashRefParser` regex'ы + `ProdLinkDerivers.swift` factory + 4 moat test files. Public substrate ships только conservative defaults (`LinkConfidenceValues.publicDefault` 0.5/0.5/0.5/0.5/1.0). **Privacy regression** (Track 1 контракт §6 amendment, ADR-010 §6): 3 новых тестов в `RelayBodyLeakageTests` — bodies (FTS) + link target_refs (LEAF-NN, owner/repo/pull/N, reviewer login) НЕ leak в `presence_state.state_json`. **1108 SPM tests** (1055 baseline + 53 new D2 tests, target ~1103 ±5) + 5/5 xcodebuild schemes green. **Не merged в main** — стэк Track 1 (D1 → D2 → D3) merges коллективно после D3 ship + acceptance gate per контракт §13. Whitepaper sync deferred до Track 1 ship per контракт §12. Tactical: `docs/superpowers/plans/2026-05-09-track-1-D2-fts-and-link-graph.md`, spec: `docs/superpowers/specs/2026-05-09-track-1-D2-fts-and-link-graph.md`.

**2026-05-09 — Phase Track-1 D1 (Capture Extension) landed на `feature/track-1-D1-capture-extension`** (off `feature/track-1-detection-substrate` off `main`, alpha.11 baseline). Track 1 first sub-phase: bodies + attachments + Phase 4.8 PR metadata for Linear / GitHub / Slack collectors; Claude Code hooks audited (no parser gap surfaced — `default: .irrelevant` covers v2.1.133 schema additions). M011 expression index `idx_events_event_kind_ts` (`json_extract(payload_json, '$.event_kind'), ts`) для D3 query path. New payload keys via `Schema.EventPayloadKeys`: `body` / `body_truncated` / `attachments_json` / `comment_bodies_json` / `thread_replies_json` / `messages_json` / `files_count` / `additions` / `deletions` / `requested_reviewers_json` / `mention_count` / `link_count`. `AttachmentMeta` value type (LeafCore public). Moat: `BodyCap` 64KB cap + `SlackBudgets` thread fan-out + `PRBodyParser` regex'ы (LeafCorePrivate). **Architectural workaround:** `BodyCap.apply()` calls inside Prod*APIProviders (LeafCorePrivate); snapshots carry `*Truncated: Bool` to bridge moat boundary (LeafCore cannot import LeafCorePrivate). Linear: GraphQL fragment extended (description / comment.body / attachments title+contentType+metadata). GitHub: full commit message + PR body + comment body + release.assets[] + inline image URL parsing + Phase 4.8 PR metrics direct from REST. Slack: additive extension (no replacement) of SlackChannelMessageCount/SlackFileUploadSummary; new `fetchThreadReplies` (`conversations.replies` Tier 3) с `SlackBudgets.maxThreadsPerTick` cap; per-thread cursor (`slack:thread:<channelID>:<threadTs>`) + 429 graceful degrade с cursor preservation; aggregate-level body_truncated по sentinel detection. RateLimitError type. Bodies on-device only (ADR-010 §6 amendment); RelayBodyLeakageTests asserts bodies НЕ leak в presence_state.state_json. **1055 SPM tests** total (1022 baseline + 33 new) + 5/5 xcodebuild schemes green. **Не merged в main** — стэк Track 1 (D1 → D2 → D3) merges коллективно после D3 ship + acceptance gate. Whitepaper sync deferred до Track 1 ship per контракта §12. Tactical: `docs/superpowers/plans/2026-05-09-track-1-D1-capture-extension.md`, spec: `docs/superpowers/specs/2026-05-09-track-1-D1-capture-extension.md`.

## Где мы

- **Whitepaper v0.1-beta** (team-first re-positioning, 2026-05-08) в `leaf-docs.gundem.tech`. Byterover-style структура: `getting-started / memory-architecture / llm-providers / ai-tool-connectors / activity-connectors / surfaces / team-sharing / privacy-security / cookbook / faqs / reference`. Стратегические решения — `docs/reference/decisions.md`, открытые вопросы — `docs/reference/open-questions.md`.
- **Section A done (Phase 0-2).** Foundation: 3-target Xcode project, `LeafCore`/`LeafCorePrivate` SPM split, GRDB 7 + SQLCipher, Agent + 4 collectors, Derived Insights Engine, Native UI, stdio MCP server.
- **Section B done (Phase 3.0-3.5).** Distribution: Apple Developer ID + notarytool + Sparkle 2 + R2/CF + EdDSA appcast.
- **Layer B MVP closed (Phase 4.1-4.5).** Linear / GitHub / Slack OAuth + polling.
- **Phase 4.6 done.** Latency depth + Linear status transitions + synthesis (week-over-week, longest uninterrupted window, streaks). 8 MCP tools.
- **Phase 4.7 A+B+C SHIPPED в alpha.9 (2026-05-04).** 33 новых event_kind discriminators + `presence_state` M005 + `LinearIDExtractor` cross-provider linking + 4 presence-first MCP tools (12 total) + ModeClassifier types-only skeleton + Native UI redesign. ShareEventTypeKey registry 22 → 43.
- **Phase 4.10.B merged (2026-05-04).** Activity tab session enrichment (window_title + browser_url AX collector + on-device session aggregation + colored category dots + app icons). 652 SPM tests baseline.
- **Phase 5.1 stack CLOSED (2026-05-05).** Schema substrate (M006/M007/M008) + value types + GRDB helpers + `EnvelopeCodec` AES-GCM-256 (`ProdEnvelopeCodec` moat) + `OrgService.createPersonalOrg` + `TeamKeystore` + `OrgReader` + UI + persistence E2E. 714 SPM tests.
- **Phase 5.2 stack CLOSED (2026-05-05).** Invite handshake E2E: `IdentityService` X25519 + `InviteKDF` HKDF-SHA256 + `InviteBlobCodec` AES-GCM-256 + leaf-relay `/v1/invite/*` (live на `oauth.gundem.tech`) + `RelayClient` + `InviteService` admin + `InviteAcceptService` invitee + `AcceptInviteSheet` 2-step UX + Onboarding `.team` step. 802 SPM tests.
- **Phase 5.3 stack SHIPPED в alpha.10 + alpha.11 patch (`NSFullUserName` placeholder fix).** Member removal + key rotation: DB lifecycle mutators (5.3.A) + `RotationBlobCodec` substrate (5.3.B) + leaf-relay `/v1/key-rotation/*` wire (5.3.C, live) + `KeyRotationService` admin + M009 `rotation_outbox` (5.3.D) + `RotationFetchService`/`Scheduler` peer loop + `MemberRemovalReader` UI + `RemovedFromTeamBanner` (5.3.E).
- **Phase 5.5 stack in flight.** 5.5.A (substrate — `JoinCode` + `InviteURL` + M010 `pending_invites` + `PendingInvitesStore`, 971 tests) + 5.5.B (UX surface — deep-link routing + scenePhase clipboard auto-fetch + `.team` three-way + sheet rewrites + JoinCode/URL service overloads, 989 tests) + 5.5.C (admin recall — `PendingInvitesService` orchestrator + 5 Database wrappers + `PendingInvitesReader` Observable + `PendingInvitesSection`/`Row` UI + TeamView wiring, 1007 tests, optimistic D4 revoke). Все три ship'нули в свои feature branches. Стэк ждёт коллективный merge после two-Mac smoke gate.
- **Track 1 stack ready for acceptance gate.** D1 (capture extension — bodies + attachments + Phase 4.8 PR metadata, 1055 tests) → D2 (FTS5 + event_links — M012/M013 + EventsFullTextStore + EventLinksStore + LinkDerivers + atomic write refactor + privacy regression, 1108 tests) → D3 (M014 detection tables + DetectorPipeline + QueryEngine + 3 structured MCP tools + 6 detector protocols + DetectorMoat + Agent integration + LeafCorePrivate prod moat + privacy regression, **1213 tests**). **28 SQLCipher tables** (12 base + presence_state M005 + org/team_members/team_keys M006-M008 + rotation_outbox M009 + pending_invites M010 + idx M011 + events_fts virtual + events_fts_meta sidecar M012 + event_links M013 + decisions/open_questions/blockers/where_stopped_log/detector_offsets M014). **15 MCP tools** (12 low-level + 3 structured high-level). **48 ShareEventTypeKey** entries (43 baseline + 5 D3 semantic-fact discriminators, all default OFF). Все три sub-phase ship'нули в свои feature branches; стэк merges коллективно после Track 1 acceptance gate per контракт §13. **Whitepaper sync deferred** до Track 1 ship.
- **Track 2 stack ready for acceptance gate.** D1 (Design Foundation — atomic 3-tier T1/T2/T3 token system + 28 components / 4 templates + TokensPreview `⌘⌥T`, 1213 tests baseline preserved) → D2 (Navigation Shell + Home — RootView/Sidebar/Home migration на D1 substrate) → D3 (Data Surfaces — Activity/Team/Connections in-place migration на D1 substrate) → D4 (Final Migration & Cleanup — sheets/Org/Settings/Profile/MenuBar/Onboarding/RemovedFromTeamBanner/ShareTemplateButton/2 token previews migrated; old palette + Window/Shared wrappers retired hard delete'ом 8 files + 1 dir; check-tokens три-tier finalized BASE+MIGRATION+RETIRED; **1213 tests baseline preserved** через все 4 sub-phase, zero new tests — pure UI substrate, manual-smoke-only validation per AC1-14). **Snapshot Replacement migration complete** — все production view files на едином D1 substrate, zero old-palette compiles. **Token-discipline guard final form** — three-tier scope с RETIRED tier (entire `Leaf/`) banning old-palette names anywhere as defense-in-depth alongside compile-time deletion. Все 4 sub-phase ship'нули в свои feature branches; стэк merges коллективно после Track 2 acceptance gate (manual smoke per 14 AC юзером перед merge'ом). **Whitepaper sync deferred** до Track 2 ship per OQ-29.
- **Linear** = только таски. Второй мозг = whitepaper.

## Архитектура

Полная картина — `.claude/shared/architecture.md` + whitepaper `leaf-docs/docs/memory-architecture/`. TL;DR substrate'а: MCP primary surface (Cursor / Claude Code / Claude Desktop) + Native macOS app + Phase 5 E2E team relay. Layer A (NSWorkspace/CGEventSource/EventKit/Focus/AX/FSEvents/Claude Code hooks), Layer B MVP = Linear+GitHub+Slack, granularity L1-L5, Share Controls, presence relay через **Cloudflare DO + AES-GCM** (фактически built, whitepaper v0.1-beta планирует миграцию на Supabase + XChaCha20-Poly1305 — substrate готов, миграция отдельным треком), SQLCipher tables (12 base + presence_state M005 + org/team_members/team_keys M006-M008 + rotation_outbox M009 + pending_invites M010 + idx M011 + events_fts virtual + events_fts_meta sidecar M012 + event_links M013), zero-LLM substrate (whitepaper планирует tier-based summarization: on-device Apple FM / BYOK / Leaf Cloud — отдельный track), Sparkle 2 + EdDSA + R2 distribution. **12 MCP tools** total.

## Следующим

- **Track 2 acceptance gate** — manual smoke per AC1-14 (per spec § "Acceptance criteria") на собственных рабочих данных перед коллективным merge стэка D1+D2+D3+D4 в `main`. AC1 `⌘⌥T` Tokens Preview (LeafSheetLayoutPreview / LeafOnboardingStepLayoutPreview render с LeafButton); AC2-3 Sheets × 3 (GenerateInviteSheet / AcceptInviteSheet / RemoveMemberSheet — LeafSheetLayout adopted, state machines preserved); AC4 OrganizationView 4 OrgReader states; AC5 WindowSettingsView 3 sub-sections без Form chrome; AC6 ProfileView LeafAvatar.lg + 2 LeafMetricCards; AC7 RemovedFromTeamBanner LeafEmptyState (mental smoke — hard to trigger); AC8 MenuBarContent 360pt + LeafBanner banners + LeafButton.primary "Open" + native Quit; AC9 Onboarding 5-step + 4-substep TeamSubStep walk-through; AC10 ShareTemplateButton 3 LeafButton с icons; AC11 retirement compile-time enforce (already verified all 5 schemes green); AC12 `just check-tokens` + `-self-test` (already green); AC13 5/5 xcodebuild schemes (already green); AC14 1213 SPM tests (already preserved). После acceptance — collective merge стэка в `main` + whitepaper sync per OQ-29 (post-merge separate session targeting public-safe architectural framing — implementation moat не публикуется).
- **Track 1 acceptance gate** — manual smoke 5 use cases UC1/UC3/UC4/UC5/UC6 на собственных рабочих данных через MCP (per контракт §13). UC1 "что я делал в пятницу по auth?" (`leaf_query_activity` filter="auth"), UC3 "что менялось в /payments за 2 недели?" (`leaf_query_activity` filter="payments"), UC4 "почему мы вынесли OAuth refresh на сервер?" (`leaf_get_decision` topic="OAuth refresh"), UC5 "что мне знать до ревью PR #142?" (`leaf_query_activity` scope=PR-NN), UC6 "что мы решили по бэкапам?" (`leaf_get_decision` topic="бэкапы"). Each — coherent JSON within 64KB budget; Claude Code формулирует sensible narrative в пределах 3s. После acceptance — коллективный merge стэка D1+D2+D3 в `main` + whitepaper sync per контракт §12.
- **D3 known issue (carry-over for acceptance gate or follow-up):** D2's FTS body-kind dispatcher uses `event_kind == "linear_issue_updated"` но Linear collector emits `"issue_updated"` — Linear descriptions не индексируются в FTS. Surfaces during UC1/UC3 если фильтр пытается hit'ить Linear bodies. Fix locally в D2 dispatcher или global rename в Linear collector. Также: D3 `BodyKind` typed enum coexists with `Schema.BodyKinds` string namespace — нет round-trip test предотвращающего drift при добавлении нового body_kind. Body-dispatch logic дублирована 3 раза (EventsFullTextStore / EventLinksStore / DetectorPipeline) — extract `BodyExtractor` walker before adding 4-го caller.
- **D3 LinearStuck status filter** uses canonical Linear `WorkflowState.name` strings; workspaces с custom statuses (e.g. "Coding", "Reviewing") не detect. v1.1 — switch к `WorkflowState.type` discriminator (требует collector emit `to_state_type` на `issue_updated` events).
- **Phase 5.5 two-Mac smoke gate** — manual end-to-end check 5.5.A+B+C на двух Mac'ах (admin + invitee, full 7-step golden path per decomposition §4.8 + 5.5.C-specific 8-step §5.3) перед коллективным merge стэка в `main`.
- **Phase 5.5.D candidates** (post-smoke, scoped if adoption surfaces gaps): `inviteeDisplayNameHint` capture в GenerateInviteSheet (D7); per-row [Refresh]; "Joined N min ago" ghost row для `.consumed`; bulk Revoke-all-expired / Dismiss-all-terminal; centralize "transient mutation error doesn't blank list" pattern across PendingInvitesReader/InviteOutboxReader/MemberRemovalReader.
- **Phase 5.6** — auto-poll loop через HEAD `/v1/invite/<token>` (requires `gundemtech/leaf-relay` extension); background scheduler.
- **Phase 5.4** (parallel track) — `presence_outgoing` + `presence_history` M011/M012 + WS broadcast loop в Agent + Swift WS client + Team presence grid live UI; first real consumer `ProdEnvelopeCodec` 5.1.C.
- **Phase 4.7 carry-overs** (non-blocking, Linear cleanup для 4.8): hoist GitHubCollector UTC `DateFormatter` to static; split `GitHubAPIProvider.swift` если 4.8 добавит structs; document `fetchEvents` failure short-circuit; tighten `testQueryShapeIncludesAssignedIssuesBlock` slice. Plus 4.7.C: refactor C-8 piggy-back на separate HTTP call если Linear API rejects whole query на legacy workspaces.
- **Phase 4.8 — ABSORBED by Track 1 D1.** files_count / +/- lines / requested_reviewers / mention_count / link_count + M011 expression index — все ship'нули в D1 (alpha.11). Standalone Phase 4.8 закрыт.
- **Phase 4.9 — Derived modes (NOT designed yet).** `DefaultModeClassifier` impl поверх 4.7.C skeleton, `mode_history` table, новые MCP tools.
- **Layer C (V1.5+)**: MCP-aggregator поверх Notion / Figma / Jira / Gmail / Calendar.
- **Cleanup**: delete `KeychainKeyStore.swift` + `LeafError.keychainUnavailable` после ~2 недель stable alpha.6 runtime (target ~2026-05-13). leaf-relay README + CI deploy hook.
- **Release tooling improvements** (lessons alpha.7+9): (a) `release.sh` auto-bump `CURRENT_PROJECT_VERSION` или fail-fast если build number == prior; (b) version-aware stamps в `build/releases/.stamps/` (auto-clean или `archive.<version>` имя) — сейчас на новой версии переупаковывает старые артефакты.
- **Sparkle ship gotchas**: (a) Xcode Debug LeafAgent залипает в launchd → `pkill -f "DerivedData.*LeafAgent"` pre-ship; (b) AX permission иногда требует toggle после bundle replace (CDHash меняется); (c) `CFBundleVersion` monotonic per ship — без bump'а Sparkle detection ломается; (d) dev-only: переключение `/Applications/Leaf.app` ↔ DerivedData сбрасывает BTM disposition (production update path не страдает).

## Open tensions

Substrate-level (технические, не в whitepaper):
- **OT-1** distributed deletion локальной forever-history у ex-teammates.
- **OT-2** storage compression для forever retention.

Стратегические open questions (whitepaper) — `leaf-docs/docs/reference/open-questions.md`.
