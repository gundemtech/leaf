# Текущее состояние проекта

_Срез "где мы сейчас" за 30 секунд. История — в whitepaper changelog + `.claude/plans/phase-*.md` + git log._

## Последнее обновление
2026-05-06 (день) — **Phase 5.3.A landed на `feature/phase-5-3-A`** (4 commits stack'ом поверх 5.2.E: `ee19b64` spec, `7cac207` `markTeamMemberRemoved`, `271f551` `deprecateTeamKey`, `28a20ed` `readTeamKey(byID:)`). Первая sub-phase Phase 5.3 ("member removal + team key rotation") — substrate-only, pure DB lifecycle helpers без crypto / wire / UI / orchestration. Mirror'ит роль 5.1.B helpers'а в стеке 5.1 — pure SQL + GRDB. **3 helpers** в новой `// MARK: - Team lifecycle (Phase 5.3.A)` секции `Database.swift` (после 5.1.B Team block, перед Linear v2 migration): (1) `markTeamMemberRemoved(memberID:at:)` — conditional UPDATE с `WHERE removed_at_ms IS NULL` для happy-path single-statement; SELECT-on-zero disambiguation distinguishes already-removed (idempotent no-op preserving original timestamp) от missing row (throws `.invalidPayload`); (2) `deprecateTeamKey(keyID:at:)` — sole-active invariant guard (count active keys; refuse if deprecating leaves 0 active rows — caller обязан `insertTeamKey(new)` first в той же tx через 5.3.D `KeyRotationService`) + idempotent re-call (preserves first `deprecated_at_ms`) + missing-key throws `.invalidPayload`. Single `pool.write` tx — count + UPDATE атомарны. Edge case: deprecating already-deprecated row не trip'ает sole-active guard (target уже deprecated — bypass to idempotent no-op path); (3) `readTeamKey(byID:)` — reader-mode safe lookup regardless of deprecated status (no mode guard, mirror'ит `readOrg` / `readActiveTeamKey` / `readTeamMembers` semantic). Forward-compat under contract §12 forever-retained team_keys для past `presence_history` decrypt в 5.3.E peer-side flow. **Reuse без modification:** `Self.mapTeamKeyRow` / `Self.mapTeamMemberRow` / `Schema.TeamMembers.*` / `Schema.TeamKeys.*` / `LeafError.invalidPayload` / `.databaseUnavailable` — всё уже от 5.1.B. **Никаких новых migrations / schema changes / value types** — M007/M008 columns (`removed_at_ms`, `deprecated_at_ms`) уже от 5.1.A. **Tests:** **816 SPM tests** (+14 от 5.2.E baseline 802) в новом `DatabaseTeamRemovalTests.swift`: 4 mark (HappyPath partial-index isolation / IsIdempotent timestamp preservation / MissingMemberThrowsInvalidPayload / ReaderModeThrowsDatabaseUnavailable) + 6 deprecate (HappyPathWithMultipleActive / SoleActiveGuardThrows / IsIdempotentOnAlreadyDeprecated bypass guard / MissingKeyThrowsInvalidPayload / ReaderModeThrowsDatabaseUnavailable / AfterDeprecateLatestActiveReadActiveReturnsOlder partial-index `team_keys_active` consistency) + 4 read (ByIDReturnsActiveRow / ByIDReturnsDeprecatedRow / ByIDReturnsNilForMissing / ReaderModeWorks). All 5 xcodebuild schemes BUILD SUCCEEDED. Code review verdict (`superpowers:code-reviewer`): APPROVED-WITH-NITS, BLOCKING none — все nits defer'able micro-observations (двойной `count(*)` query в guard step / optional clarifying comment на activeCount==0 fall-through path / `_MissingKey` test может использовать `readTeamKey` вместо `writeSQL` raw count — defer). Substrate под 5.3.B (`RotationBlobCodec` + `RotationKDF` HKDF-SHA256 info-string `"leaf/rotation/v1"` для domain separation от invite codec) → 5.3.C (`RelayClient` extension + leaf-relay `/v1/key-rotation/*` endpoints) → 5.3.D (`KeyRotationService` admin orchestrator: `insertTeamKey(new)` → `deprecateTeamKey(old)` → optional `markTeamMemberRemoved` атомарно + `RotationOutbox` journal) → 5.3.E (peer fetch loop + UI + ship-gate two-Mac smoke) полностью готов. Spec: `docs/superpowers/specs/2026-05-04-phase-5-3-A-db-mutators.md`. Tactical: `.claude/plans/phase-5-3-A.md`. **Whitepaper sync:** none — pure implementation moat (column-level mutations / SQL invariants — implementation detail, не уходит в public docs); changelog entry отложен до Phase 5.3 ship'а в alpha.X (5.3.E ship-gate), pattern consistent с 5.1.A-E.

2026-05-05 (поздно вечер) — **Phase 5.2.E landed → Phase 5.2 закрыт E2E** на `feature/phase-5-2-E` (8 commits stack'ом поверх 5.2.D: `2888984` LeafError.inviteAlreadyAccepted, `5a68c29` InviteAcceptService.fetchInvite, `ef3a4f5` acceptInvite full crypto+DB, `ae03e1a` InviteAcceptReader Observable, `a554546` AcceptInviteSheet 2-step view, `16d6595` OrganizationView second CTA + LeafApp wiring, `59b8dcf` Onboarding `.team` step, `1eecc66` codec error re-classification fix). Пятая (финальная) sub-phase Phase 5.2 — invitee accept-flow поверх admin substrate'а 5.2.A-D. **`InviteAcceptService`** (LeafCore public): `fetchInvite(token:)` — wraps `RelayClient.getInvite` one-shot KV-consume; `acceptInvite(blob:, otp:, displayName:)` — full chain (preflight `inviteAlreadyAccepted`/`invalidPayload` → `IdentityService.ensureLocalIdentity` invitee X25519 priv → `InviteBlobHeader.peek` → `KeyAgreement.sharedSecret` symmetric ECDH(invitee_priv, admin_pub_from_header) → `inviteKDF.deriveWrapKey` → `inviteBlobCodec.decode` (codec'owy `inviteBlobMalformed` re-classified здесь как `inviteOTPInvalid` — codec moat conflates AES-GCM tag fail / JSON parse / short-cipher; service distinguishes "wrong OTP retry" от "blob structurally broken peek-fail" — matches decomposition §8 risks "OTP doesn't match" UX) → base64-decode 32B teamKey → **keystore-first** (orphan file < orphan rows) `TeamKeystore.writeTeamKey` → `Database.upsertOrg` + `insertTeamMember(admin from blob)` + `insertTeamMember(self with new UUID + invitee displayName + own pubkey hex)` + `insertTeamKey`). Returns `AcceptedInvite{orgID, orgName, teamKeyID, selfMemberID}`. Inject'абельные factories `now`/`identity`/`generateMemberID` mirror InviteService 5.2.D. **`InviteAcceptReader`** (Leaf app `@MainActor @Observable`): 6-state machine `.idle / .fetching / .otpEntry(blob, attempts) / .accepting / .success(orgName, memberCount) / .error(message, recoverable)` — blob persists в RAM (caller side), `submitOTP` catches `inviteOTPInvalid` → `.otpEntry(attempts+1)` для local retry без re-fetch (relay GET one-shot consumed на fetch step); 5+ attempts surface "Discard + ask admin" CTA. `myPubkeyHex()` exposes invitee X25519 pub (через IdentityService.ensureLocalIdentity) для AcceptInviteSheet step 1 copy-row. Lazy `ensureService()` mirror InviteOutboxReader. **`AcceptInviteSheet`** (Leaf Views) — 2-step modal: Step 1 pubkey-share GlassCard (auto-loaded onAppear, Copy button) + token paste TextField + "Fetch invite"; Step 2 OTP TextField (6 digits monospaced) + display name TextField + "Accept invite" (disabled until valid + non-empty + attempts<5). Inline retry counter "OTP didn't match — try again (N/5)" / Discard CTA при 5 fails. Success card "Joined `<orgName>` with N members" → Done invokes `orgReader.refresh()` → OrganizationView/TeamView flip из `.empty` → `.loaded`. Error card recoverable=true → "Try again" / non-recoverable → "Close". **OrganizationView .empty createCTA** приобретает second action блок (Divider + "OR JOIN A TEAM" label + "Accept invite" .bordered button — secondary visual hierarchy относительно primary "Create personal org"). **OnboardingStep** enum расширен `.team` между `.fda` и `.done`; `.fda` Skip-for-now / auto-advance → `.team`; `teamStep` minimal partial UI per decomposition §7 (final styling/motion → 5.5): subhead "Has someone invited you to a team?" + Accept invite (opens sheet) + Skip-for-now → `.done`. Sheet onDisappear handler детектит `inviteAcceptReader.state == .success` → discardAndReset + step = `.done`. **LeafApp wiring** — `@State inviteAcceptReader = InviteAcceptReader()` + `.environment(...)` для main Window scene + **дополнительно** для MenuBarExtra scene (OnboardingView рендерится в MenuBarContent popover до `hasCompletedOnboarding` flip). **Tests:** **802 SPM tests** (+13 от 5.2.D baseline 789): 4 fetchInvite (URLProtocol stub: 200/404/transport/empty-token) + 6 acceptInvite (happy path materializes 2 team_members + org + team_key + keystore file + asserts wrapKey captured / wrong OTP DB-untouched / `inviteAlreadyAccepted` preflight refuses pre-codec / truncated blob → `inviteBlobMalformed` peek / bad teamKey base64 / empty displayName → `invalidPayload`); +3 E2E moat handshake в gitignored `InviteHandshakeIntegrationTests.swift` (POST/GET routing URLProtocol stub: full admin↔invitee handshake **byte-for-byte matching teamKey** invariant assertion / wrong OTP invitee DB untouched + keystore not created / pre-existing org refuses без codec call). All 5 xcodebuild schemes BUILD SUCCEEDED. **5.2.E moat-локально:** ProdInviteBlobCodec не модифицирован (existing 5.2.B contract preserved); error re-classification живёт в LeafCore service layer — public side. **Substrate под 5.3** (member removal + key rotation + первые `markTeamMemberRemoved` / `deprecateTeamKey` lifecycle mutators) → 5.4 (`presence_outgoing` + `presence_history` M009/M010 + WS broadcast loop + `ProdEnvelopeCodec` first real consumer) → 5.5 (Onboarding final integration + screen 6 polish) полностью готов. **Two-Mac handshake** ship-gate (decomposition §6) — manual за юзером (Bash без accessibility hooks). Spec/plan: `docs/superpowers/specs/2026-05-04-phase-5-2-decomposition.md` + `~/.claude/plans/5-2-e-invitee-stateful-shamir.md`.

2026-05-05 (день) — **Phase 5.1.D + 5.1.E landed** — substrate-stack 5.1.x закрыт. **5.1.D** (`feature/phase-5-1-D`, 4 commits: `baa00cb` spec, `ed26be8` `LeafError.orgAlreadyExists`, `1cf60ba` `TeamKeystore`, `92afb47` `OrgService.createPersonalOrg`+`currentOrg`) — domain service + file-based keystore (X25519 priv 32B + per-rotation teamKey 32B, mode 0o600, atomic rename). 12 `OrgServiceTests` + 8 `TeamKeystoreTests`. Inject'абельные factories `now`/`randomBytes`/`randomUUID`/`randomX25519PrivateKey` для deterministic round-trip. **Keystore-first DB-second** ordering (orphan-файл при DB-fail < orphan-row при keystore-fail). **5.1.E** (`feature/phase-5-1-E` stack'ом поверх D, 5 commits: `bb09b49` spec, `5f52cc6` integration test, `53b115d` OrgReader, `3786c61` OrganizationView+LeafApp wiring, `c181238` TeamView) — UI surface + persistence E2E test + landing закрывают substrate 5.1.x. **`OrgReader`** — `@MainActor @Observable` wrapper над OrgService с state machine `.loading / .empty / .loaded(Org, [TeamMember]) / .error(message:)`; lazy `ensureDatabase()` mirror OAuthService pattern; sync `refresh()` + `createPersonalOrg(displayName:)` (file/DB write < 50ms). LeafError → user-facing string mapping inline. **`OrganizationView`** rewrite: `.empty` → headline "Create your personal org" + GlassCard form (TextField + button disabled-when-empty + onSubmit); `.loaded` → org name + CREATED row + single-org-per-device blurb; `.error` → message + Retry. **`TeamView`** rewrite: `.empty` → "No team yet" + "Go to Organization" jump (через `windowState.section`); `.loaded` → header `TEAM · N MEMBER(S)` + ForEach GlassCard rows (initials avatar + display name + role badge Capsule + monospaced pubkey hex prefix `aabbccdd…eeffgghh` 8+8). **`LeafApp` wiring** — `@State var orgReader = OrgReader()` + `.environment(orgReader)` для main Window scene (MenuBarContent НЕ получает — out of 5.1.E, 5.4 presence indicator). **Persistence E2E test** (`OrgPersistenceIntegrationTests`, 3 теста): reopen-DB-and-keystore-survive, reopen-idempotency-throws-`orgAlreadyExists`, file-bytes-match-injected-sentinel — закрывают gap beyond `OrgServiceTests` single-instance flow. **714 SPM tests** (+3 от 5.1.D baseline 711), все 5 xcodebuild schemes BUILD SUCCEEDED. Substrate под 5.2 (`RelayClient` + invite UX + первый real call-site X25519 ECDH + HKDF-SHA256 helpers per contract §9 deviation) полностью готов — после `Create personal org` solo-юзер имеет один `org` row, один `team_members` self-admin, один `team_keys` initial rotation, X25519 keypair в keystore, `ProdEnvelopeCodec` с готовым AES-GCM-256 (5.1.C), всё на диске под FileVault + 0o600. Spec: `docs/superpowers/specs/2026-05-04-phase-5-1-E-org-views.md` (+ 5.1.D `2026-05-04-phase-5-1-D-org-service.md`). Tactical: `.claude/plans/phase-5-1-{D,E}.md`. Manual UI smoke (Xcode Run → Organization tab → submit "My Workspace" → relaunch persistence) — за юзером, не запускается через Bash без accessibility hooks. **Whitepaper** — sync только changelog entry при alpha-ship'е, не правки контента (`presence-relay.md` уже описывает intended flow абстрактно).

2026-05-05 (ночь) — **Phase 5.1.C landed на `feature/phase-5-1-C`** (4 commits stack'ом поверх 5.1.B: `9c17eb5` contract amendment + spec, `929e17c` `EnvelopeHeader.peek`, `28681cb` codec keyID signature, `2c92332` `ProdEnvelopeCodec` AES-GCM impl). Третья sub-phase Phase 5 — real envelope codec поверх 5.1.B value types. **Контрактная deviation §9:** только AES-GCM-256 в 5.1.C; X25519 ECDH + HKDF-SHA256 helpers переехали в 5.2 row (где первый real call-site — invite handshake) — никакого dead-code substrate без consumer'а. **Public surface (LeafCore):** `EnvelopeHeader.peek(from:)` static — read-only parse первых 17 байт (1B version + 16B keyID), throws `LeafError.corruptedEnvelope` (existing case, no new error type) на short bytes / unknown version (per contract §12 "implementations MUST reject unknown versions"); `EnvelopeCodec.encode` signature расширена `keyID: Data` parameter; `UnimplementedEnvelopeCodec` обновлён под новую сигнатуру. **Moat impl (`Packages/LeafCore/Sources/LeafCorePrivate/Prod/Crypto/ProdEnvelopeCodec.swift` — gitignored):** CryptoKit `AES.GCM.seal/open`, JSON-encoded `PresenceSnapshot` plaintext, AAD = full 17B header (binds version+keyID — header tamper invalidates AES-GCM tag), random 12B `AES.GCM.Nonce()` per call (never reuse), envelope assembly `[ver:1B|keyID:16B|nonce:12B|ct|tag:16B]` per architecture contract §6 (45-byte fixed overhead). Validates keyID size = 16, teamKey size = 32 на encode + decode. **PresenceSnapshot conforms to Equatable** (synthesised; one-line addition для round-trip теста). **Composition root в Agent.swift НЕ тронут** — substrate-only mirror 5.1.A/B; первый caller `ProdEnvelopeCodec` будет в 5.4 broadcast loop. **Тесты:** 4 public в `Tests/LeafCoreTests/EnvelopeHeaderTests.swift` (peek round-trip / short-bytes / unknown-version / Unimplemented placeholder still throws after sig change) + 13 moat в `Tests/LeafCorePrivateTests/ProdEnvelopeCodecTests.swift` (gitignored — round-trip / envelope structure / keyID via peek / nonce uniqueness / distinct encodings / tampered ct·tag·header / wrong teamKey / truncated + 3 input-validation tests на bad keyID/teamKey sizes — addressing review N-1 finding). **691 SPM tests** (+17 от 5.1.B baseline 674), все 5 xcodebuild schemes BUILD SUCCEEDED. Substrate под 5.1.D (`OrgService.createPersonalOrg` — first rows + keystore writers — раздаст `ProdEnvelopeCodec` уже готовый) → 5.1.E (UI + integration test + landing) → 5.2 (X25519/HKDF helpers + invite UX). Spec: `docs/superpowers/specs/2026-05-04-phase-5-1-C-envelope-codec.md`. Contract amendment: `docs/superpowers/specs/2026-05-04-phase-5-architecture-contract.md` §9. Code review verdict (`superpowers:code-reviewer`) — APPROVED, BLOCKING: none, N-1 (encode-side input-size dead-untested guard) addressed in commit 5 round; N-2 (direct integration assert version-reject в decode) deferred to 5.2 cleanup. Tactical: `.claude/plans/phase-5-1-C.md`.

2026-05-04 (ночь) — **Phase 5.1.B landed на `feature/phase-5-1-B`** (4 commits stack'ом поверх 5.1.A: `b634965` value types, `931b35a` upsertOrg/readOrg, `b3f330e` insertTeamMember/readTeamMembers, `a8c2c02` insertTeamKey/readActiveTeamKey + reader-guard). Вторая sub-phase Phase 5 — substrate уровня value types + GRDB helpers поверх 5.1.A schema (M006/M007/M008). **3 value types** в `LeafCore/Sources/LeafCore/Team/` рядом с `TeamMemberRole.swift`: `Org` (id/name/createdAt/createdByMemberID), `TeamMember` (id/orgID/role/pubkeyHex/displayName/addedAt/removedAt nullable), `TeamKey` (id/generatedAt/deprecatedAt nullable/generatedByMemberID — pure DB metadata, raw 32-byte AES key — keystore-файл 5.1.D). Все `Sendable, Hashable`, Date timestamps (mirroring `IntegrationRecord` style); no `Codable`, no `FetchableRecord`. **6 helpers** inline в `Database.swift` под новой `// MARK: - Team (Phase 5.1.B)` секцией: `upsertOrg(_:)` (UPSERT по id, mirror `upsertIntegration` — single-row-per-device идемпотентность), `readOrg() -> Org?` (`LIMIT 1` defensive), `insertTeamMember(_:)` (strict INSERT — caller отвечает за идемпотентность через `readOrg()` first), `readTeamMembers(orgID:includeRemoved:)` (default `false` использует partial index `team_members_org_active`, ordered by addedAt ASC), `insertTeamKey(_:)` (strict INSERT, history forever-retained), `readActiveTeamKey()` (`WHERE deprecated_at_ms IS NULL ORDER BY generated_at_ms DESC LIMIT 1` — ORDER+LIMIT defensive под edge case "две active rows"). 3 mapping функции рядом с `mapIntegrationRow` в private static секции. **Lifecycle mutators (`markTeamMemberRemoved`, `deprecateTeamKey`) отложены в Phase 5.3** (rotation policy + removal flow); тесты partial-index'ов в 5.1.B используют `db.writeSQL` raw-SQL escape для setup'а removed_at_ms / deprecated_at_ms (mirror PresenceStateWriterTests pattern). **674 SPM tests** (+11 от 5.1.A baseline 663 — 11 spec-mandated в `DatabaseTeamTests.swift`), все 5 xcodebuild schemes BUILD SUCCEEDED. Substrate под 5.1.C (`EnvelopeCodec` real AES-GCM-256/X25519/HKDF impl) → 5.1.D (`OrgService.createPersonalOrg` — first rows + keystore writers) → 5.1.E (UI + integration test + landing). Spec: `docs/superpowers/specs/2026-05-04-phase-5-1-B-helpers.md` (TBD — фазная сессия Stage 3 expand'ит plan'у). Tactical: `.claude/plans/phase-5-1-B.md`.

2026-05-04 (поздно вечер) — **Phase 5.1.A landed на `feature/phase-5-1-A`** (5 commits: `100d759` schema namespaces + TeamMemberRole, `10ad791` M006 org, `4f19a67` M007 team_members, `c66affc` M008 team_keys, `46a2fc3` idempotency tests, `ddf3438` review R1 tightening). Первая sub-phase Phase 5 ("team presence relay") — **только schema substrate**, никаких rows / writers / value types. M006 `org` (1 row per device, PK = id UUID, columns id/name/created_at_ms/created_by_member_id) + M007 `team_members` (PK = id UUID, columns id/org_id/role/pubkey_hex/display_name/added_at_ms/removed_at_ms nullable, partial index `team_members_org_active WHERE removed_at_ms IS NULL` под "active members" UI query) + M008 `team_keys` (PK = id UUID — embedded as 16B `keyID` в envelope §6, columns id/generated_at_ms/deprecated_at_ms nullable/generated_by_member_id, partial index `team_keys_active WHERE deprecated_at_ms IS NULL`, **forever-retained** для decrypt'а past `presence_history`). Plus `TeamMemberRole` enum (admin/member, `String, Codable, Sendable, CaseIterable`) в новом домен-фолдере `LeafCore/Sources/LeafCore/Team/`. **FK strategy** — никаких SQL FOREIGN KEY constraints (insertion order paradox в 5.1.D + `PRAGMA foreign_keys` не enabled — false sense of safety; logical FK документированы в Schema doc-comments). **663 SPM tests** (+11 от baseline 4.10.B=652 — 8 spec-mandated + 3 sanity bonus), все 5 xcodebuild schemes BUILD SUCCEEDED. Substrate под 5.1.B (helpers + value types) → 5.1.C (`EnvelopeCodec` real impl) → 5.1.D (`OrgService.createPersonalOrg` — first rows) → 5.1.E (UI + integration test + landing). **Promotes** Phase 5 architecture contract `docs/superpowers/specs/2026-05-04-phase-5-architecture-contract.md` Draft → Active. Spec: `docs/superpowers/specs/2026-05-04-phase-5-1-A-migrations.md`. Tactical: `.claude/plans/phase-5-1-A.md`. Code review verdict (`superpowers:code-reviewer`) — BLOCKING: none, R1 partial-index assertion tightening applied.

2026-05-04 (вечер) — **Phase 4.10.B merged в `main`** (commit `77d47fd`, no-ff merge `feature/phase-4-10-B` с preserved 5-commit history; ship в alpha.10 — отдельный шаг после spokey smoke в реальном использовании). Закрыли gap Phase 4.10.A: Activity tab показывал sessions/events, но без file/window context — юзер не мог ответить на «3 часа сидел в Xcode, что *именно* делал». Layer A enrichment + on-device session aggregation: (1) **B-0..B-3 LeafCore** — stateless `SessionFeedMapper` (single-pass O(N), idle = boundary, gap-threshold + min-duration moat-tunable) + `AttentionGranularityPolicy` protocol (forward-compat shim под Share Controls) + `AppCategory` taxonomy enum (dev/browse/communication/design/other, public-safe) + `AttentionEmissionPlanner` (diff suppression на (bundleID, window_title)); (2) **B-2 collector enrichment** — AX `kAXFocusedWindowAttribute → kAXTitleAttribute` (window_title) + `AXWebArea → AXURL` (browser_url) на NSWorkspace activation + новый periodic window-poll tick; (3) **B-4..B-7 UI** — Sessions/Raw events toggle в Activity tab (default Sessions) с real `NSImage` app-иконкой через `AppIconResolver` cached + colored category dot (`CategoryColors` theme) + duration; "Recent sessions · today" блок на Home под Top Apps; (4) **moat refactor** (`cf14afa`) — bundle ID preset перенесён из public LeafCore в `LeafCorePrivate/Prod/Insights/ProdAppCategoryClassifier.swift` (gitignored), composition root в Agent.swift через `#if LEAF_PROD`; (5) **AX fallback fix** (`0b617bc`) — discovered post-rebuild smoke что Xcode возвращает `AXFocusedWindow = missing value` когда не frontmost (Telegram/Terminal не страдают). Fix — chain `AXFocusedWindow → AXMainWindow → Windows[0]` в `AXWindowContextProvider`. **652 SPM tests**, +64 от 4.10.A baseline 589, все 5 xcodebuild schemes BUILD SUCCEEDED. **AX permission gotcha** — `tccutil reset Accessibility tech.gundem.leaf` revoke'ит permission на оба `/Applications/Leaf.app` и `DerivedData/.../Leaf.app` записи; после rebuild bundle re-grant нужен. **`recentSessions` API** — новый `DerivedInsights.recentSessions(period:limit:)` + `InsightsSnapshot.recentSessions: [ActivitySession]` additive. Whitepaper sync — `709ce50` в leaf-docs main (data-collection.md L3 default note + mvp-scope.md Sessions surfacing note + changelog entry). Tactical: `.claude/plans/phase-4-10-B.md`.

2026-05-04 (день) — **alpha.9 SHIPPED** — Phase 4.7 A+B+C bundle (+ Native UI redesign) ушёл юзерам через Sparkle. Single merge `feature/phase-4-7-a` → `main` с `--no-ff` (commit `2d786c4`, preserved 41-commit A→B→C history); bump `MARKETING_VERSION` 1.0.0-alpha.7 → 1.0.0-alpha.9 + `CFBundleVersion` 8 → 9 (commit `385530e`, skipped alpha.8 — UI redesign + 4.7 шипятся вместе одним релизом). release.sh archive → notarytool Accepted (`7f975957-e2d2-41bd-bb5c-46252b60d42e`) → staple .app+.dmg → ditto zip (15M) → generate_appcast (3 items) → R2 upload + CF cache purge. Live: `https://updates.gundem.tech/appcast.xml` указывает на `1.0.0-alpha.9` / sparkle:version=9, delta-paths Leaf9-{6,7,8}.delta. **Release tooling gotcha (поймано в этом ship'е):** stamps в `build/releases/.stamps/` от alpha.7 ship не очистились — первый прогон `release.sh 1.0.0-alpha.9` пропустил archive/export/verify/dmg/notary/staple/zip как "skip ... (stamp exists)" и переупаковал старые alpha.5/6/7 артефакты (без alpha.9 dmg/zip). Обнаружено через финальные `du: ... No such file or directory`. Fix: `release.sh 1.0.0-alpha.9 --redo-from archive`. См. release tooling improvements ниже — release.sh должен auto-clean stamps на новой версии или включать version-marker в имя stamp.

2026-05-04 (earlier) — **Phase 4.7.C landed on `feature/phase-4-7-a` (pushed)** (11 commits от `8a6aa59` до `f3ed06e` поверх Phase 4.7.B; A+B+C bundle полностью на feature branch, теперь merged + shipped в alpha.9). Track C scope ("triage / state-change depth + skeleton queries + ModeClassifier substrate") closed E2E: **C-0** types-only `Mode` / `Pulse` enums + `ClassifiedMode` struct + `ModeClassifier` protocol в `LeafCore/Insights/` (skeleton под Phase 4.9 — `DefaultModeClassifier` impl откладывается, `presence_state.derived_mode` остаётся NULL); **C-1** Linear `Issue.history` fragment расширен 6 transition flavors (cost-free piggy-back на existing connection); **C-2..C-6** 6 transition mappers + RawEvent flavors: `linear_priority_changed` / `linear_label_added` / `linear_label_removed` / `linear_assignee_changed` (anonymized self-other bucketing — 7 cases, raw third-party IDs не покидают provider) / `linear_cycle_changed` / `linear_estimate_changed` (все mirror 4.6.B status pattern: actor.id == viewer.id + cursor guard + degenerate-noop reject); **C-7** `linear_project_update_authored` (separate Linear top-level type, piggy-back fragment в той же query — single HTTP call vs plan's "separate query" deviation: simpler implementation, same correctness); **C-8** `linear_document_edited` skeleton (piggy-back, per-tick graceful degrade без persistent flag — same simplification choice as C-7); **C-9** `linear_initiative_observed` (`viewer.initiatives` piggy-back — context signal type, observedAtMs = tick timestamp); **C-10** GitHub `pr_review_thread_resolved` (single new case в mapEvents switch, only action=resolved emits); **C-11** `ShareEventTypeKey` registry +10 keys → 43 total (8 outcome-bearing ON by default; documents/initiatives skeleton OFF); **C-12** end-to-end integration test (full batch со всеми 11 snapshot kinds → 12 RawEvents с правильными signal types). **563 SPM tests** (+68 от 4.7.B baseline 495). Все 5 xcodebuild schemes BUILD SUCCEEDED (Leaf / LeafAgent / LeafMCP / LeafCore / LeafCorePrivate). ADR-010 sentinel-injection regression tests на каждом из 11 новых event_kinds. Tactical: `.claude/plans/phase-4-7-C.md`. **Branch pushed** (`origin/feature/phase-4-7-a`). Перед alpha.9 ship — manual smoke на dev Mac (Linear UI: change priority/label/assignee/cycle/estimate + project update + GitHub: resolve PR review thread → wait 15min → SQLCipher REPL query новых event_kinds + ADR-010 sentinel walk).

2026-05-02 — Phase 4.7.B landed on `feature/phase-4-7-a` (20 commits `be26210..0a1a367`). Track B "presence-first first-class APIs": B-0 `PresenceStateWriter` + `writeEventsOffsetAndPresence`; **GitHub** (B-1..B-5) 5 fetch methods + `presence_state.github`; **Linear** (B-6..B-8) viewer.assignedIssues/teams.activeCycle/attachments + `presence_state.linear`; **Slack** (B-9..B-13) presence/DND/mentions/files + `presence_state.slack` (+`dnd:read`/`files:read` scopes); **B-14** registry +11 keys; **B-15..B-18** 4 MCP tools (12 total); **B-19** docs. 495 SPM tests, +118 от 4.7.A. Substrate под Phase 5 broadcast готов. Tactical: `.claude/plans/phase-4-7-B.md`.

2026-05-01 — Phase 4.7.A landed (commits `0610816..70b5f0f`): 11 новых event_kind discriminators поверх existing tick'ов, M005 `presence_state` table (writes в B), `LinearIDExtractor` + GitHub commit/PR hooks, `ShareEventTypeKey` registry. **377 SPM tests** (+56 от baseline 4.6=321). Tactical: `.claude/plans/phase-4-7-A.md`. **Native UI redesign** (`74386a2`) ранее на main, ждёт alpha.8. **alpha.7 SHIPPED 2026-04-30** (Phase 4.6 A+B+C). Live: `https://updates.gundem.tech/appcast.xml`.

## Где мы
- **Whitepaper v1.10** published в `leaf-docs.gundem.tech`. Структура `01-vision / 02-product / 03-architecture / 04-market / 05-reference`.
- **Section A done (Phase 0-2, 2026-04-23 → 2026-04-26).** Foundation: 3-target Xcode project, `LeafCore`/`LeafCorePrivate` SPM split, encrypted storage (GRDB 7 fork + SQLCipher AES-256), Agent daemon + 4 MVP collectors, Derived Insights Engine, MenuBarApp Native UI, stdio MCP server. Tactical: `.claude/plans/phase-2*.md`.
- **Section B done (Phase 3.0-3.5, 2026-04-27 → 2026-04-28).** Distribution: Apple Developer ID + notarytool + Sparkle 2 + R2/CF + EdDSA-signed appcast. Shipped **1.0.0-alpha.5** → **alpha.6** (2026-04-29) → **alpha.7** (2026-04-30, bundled Phase 4.6). Tactical: `.claude/plans/phase-3*.md`.
- **Layer B MVP closed (Phase 4.1-4.5, 2026-04-28 → 2026-04-29).** Linear (PKCE loopback fixed-port + GraphQL per-action attribution через `activity.user.isMe` + `creator.isMe` backstop), GitHub (Device Flow RFC 8628, REST `/users/<X>/events` polling), Slack (PKCE distributed-app через HTTPS relay `oauth.gundem.tech/<provider>/callback` Cloudflare Worker — bouncer на loopback). 7 MCP tools, popover lines per provider. Tactical: `.claude/plans/phase-4-{1,2,3,4,5}.md`.
- **Phase 4.6 done E2E (Layer B Depth, 2026-04-30, alpha.7 ready).** Layer B перешёл из "что произошло + сколько раз" → "что произошло + насколько долго + какой темп". **A** latency depth (GitHub PR cycle + review delay; Linear completion duration; Slack reactions count + huddle session distribution — all через generic `LatencyStats` aggregation). **B** Linear status transitions (новый `linear_status_transition` event flavor через nested history fragment + client-side actor filter). **C** synthesis (surface existing `weekOverWeekDelta` + новая `longestUninterruptedWindow` 8-й MCP tool + per-provider streaks 60-day lookback). **8 MCP tools** total. **321 SPM tests**, 6 xcodebuild green. Tactical: `.claude/plans/phase-4-6{,-A-3,-B,-C-1,-C-2,-C-3}.md`.
- **Phase 4.7 A+B+C SHIPPED в alpha.9 (2026-05-04).** Bundle одним merge'ем `feature/phase-4-7-a` → `main` (`2d786c4`) + version bump (`385530e`). **A** (2026-05-01): 11 новых event_kind discriminators поверх existing tick'ов, `presence_state` M005, `LinearIDExtractor` + GitHub commit/PR hooks. **B** (2026-05-02): "presence-first first-class APIs" — 11 новых event_kinds + 4 link-counts enrichment + 4 новых MCP tools (12 total). **C** (2026-05-04): "triage / state-change depth + skeleton queries + ModeClassifier substrate" — 11 новых event_kinds (priority/labels[2]/assignee/cycle/estimate Linear transitions + projectUpdate/document/initiative skeletons + GitHub pr_review_thread_resolved); ModeClassifier protocol + Mode/Pulse enums types-only (DefaultModeClassifier откладывается на 4.9). **Plus Native UI redesign** (`74386a2`) bundled в этот же ship. ShareEventTypeKey registry 22 → 43. **563 SPM tests** (+242 от 4.6 baseline 321). 5 xcodebuild schemes BUILD SUCCEEDED. Tactical: `.claude/plans/phase-4-7-{A,B,C}.md`.
- **Phase 5 substrate stack 5.1.x CLOSED (2026-05-05 день).** Architecture contract `docs/superpowers/specs/2026-05-04-phase-5-architecture-contract.md` Active. **5.1.A** schema (M006 `org` / M007 `team_members` / M008 `team_keys` + `TeamMemberRole`). **5.1.B** value types + 6 GRDB helpers inline в Database.swift. **5.1.C** `EnvelopeCodec` real impl (AES-GCM-256) + `EnvelopeHeader.peek` + `ProdEnvelopeCodec` (gitignored moat). **5.1.D** `OrgService.createPersonalOrg(displayName:)` + `currentOrg()` + `TeamKeystore` (X25519 priv + per-rotation teamKey 0o600 атомарные writers) + `LeafError.orgAlreadyExists`. **5.1.E** `OrgReader` Observable + `OrganizationView` Create CTA + `TeamView` members list + LeafApp wiring + persistence E2E integration test. **714 SPM tests** end-of-stack, все 5 xcodebuild schemes BUILD SUCCEEDED. Tactical: `.claude/plans/phase-5-1-{A,B,C,D,E}.md`.
- **Phase 5.3.A landed (2026-05-06 день).** First sub-phase Phase 5.3 — pure DB lifecycle mutators (`markTeamMemberRemoved` / `deprecateTeamKey` / `readTeamKey(byID:)`) на team_members / team_keys. Substrate-only под 5.3.B-E. **816 SPM tests** (+14 от 5.2.E baseline 802), все 5 xcodebuild schemes BUILD SUCCEEDED. Tactical: `.claude/plans/phase-5-3-A.md`.
- **Phase 5.2 invite handshake CLOSED (2026-05-05 поздно вечер).** Sub-phases 5.2.A→E все залендены. **5.2.A** `IdentityService.ensureLocalIdentity` + `KeyAgreement` ECDH wrapper + `InviteKDF` protocol + `ProdInviteKDF` HKDF-SHA256 (moat info-string). **5.2.B** `InviteBlob` + `InvitePlaintext` + `InviteBlobCodec` protocol + `ProdInviteBlobCodec` AES-GCM-256 impl. **5.2.C** leaf-oauth-relay расширен `/v1/invite/*` (POST/GET/DELETE) + KV namespace `INVITES` + 24h TTL + Vitest tests (live на `oauth.gundem.tech/v1/invite/*`). **5.2.D** `RelayClient` HTTP (URLSession-based, JSON, base64url body) + `InviteService` admin orchestrator + `GenerateInviteSheet` + `InviteOutboxReader` + TeamView "+ Add member" CTA. **5.2.E** `InviteAcceptService` (invitee orchestrator: fetchInvite + acceptInvite full chain — preflight orgAlreadyExists → IdentityService → InviteBlobHeader.peek → KeyAgreement.sharedSecret → InviteKDF.deriveWrapKey → InviteBlobCodec.decode → keystore-first then DB rows; codec'owy inviteBlobMalformed re-classified в layer как inviteOTPInvalid для retry-able UX) + `InviteAcceptReader` 6-state machine + `AcceptInviteSheet` 2-step view (pubkey-share / token-fetch → OTP+displayName-accept) + OrganizationView empty-state second CTA + Onboarding `.team` step partial + LeafApp env wiring (Window + MenuBarExtra) + 3 E2E URLProtocol-stub handshake tests (matching teamKey bytes invariant). **802 SPM tests** end-of-5.2 (789 → 802, +13 за 5.2.E), все 5 xcodebuild schemes BUILD SUCCEEDED. Wire surface live: 3 HTTPS endpoints `/v1/invite/*`. On-disk after handshake: invitee имеет matching org + 2 team_members + active teamKey (бит-в-бит == admin teamKey) + own X25519 keypair. Lifecycle mutators (`markTeamMemberRemoved`/`deprecateTeamKey`) → 5.3. WS broadcast (`ProdEnvelopeCodec` first real consumer) → 5.4. Onboarding screen 6 final polish → 5.5. Two-Mac handshake ship-gate manual за юзером. Tactical: `.claude/plans/phase-5-2-{A,D}.md` + `~/.claude/plans/5-2-e-invitee-stateful-shamir.md`.
- **Linear** = только таски. Второй мозг = whitepaper.

## Архитектура
Полная картина — `.claude/shared/architecture.md` + whitepaper `leaf-docs/docs/03-architecture/`. TL;DR: two surfaces (Native UI primary + MCP bonus), opt-in transparency + Share Controls, granularity L1-L5, Layer A (NSWorkspace/CGEventSource/EventKit/Focus/AX/FSEvents/Claude Code hooks), Layer B MVP = Linear+GitHub+Slack (+ Phase 4.6 latency/transitions/synthesis depth, + Phase 4.7.A wide-cheap event_kind expansion + cross-provider Linear ID linking, + Phase 4.7.B presence-first state-snapshot endpoints + `presence_state.{github,linear,slack}` composite writers + 4 new MCP tools, + Phase 4.7.C triage-depth Issue.history fragment + ProjectUpdate/Document/Initiative piggy-back + GitHub pr_review_thread_resolved + ModeClassifier types-only skeleton), presence relay через Cloudflare DO + AES-GCM, 19 SQLCipher tables (12 base + `presence_state` M005 + `org`/`team_members`/`team_keys` M006-M008 Phase 5.1.A), zero LLM в MVP, Sparkle 2 + EdDSA + R2 distribution. **12 MCP tools** total (8 baseline + 4 Phase 4.7.B presence-first; 4.7.C не добавляет новых).

## Следующим
- **Post-ship alpha.9 verification (target — следующие 24-48ч).** (a) Existing alpha.7 юзеры триггерят Sparkle update (auto-check on launch) → должен подтянуть `Leaf9-7.delta` (~4MB) через `https://updates.gundem.tech/appcast.xml`; (b) **Slack re-consent flow** required — Phase 4.7.B добавила scopes `dnd:read` + `files:read`, существующие tokens не покрывают → юзеры должны передёрнуть Slack connection в Settings → Connections; (c) BTM agent disposition после Sparkle in-place replace — production update path не должен страдать (gotcha (d) ниже dev-only); (d) on-device smoke — wait 15min после первой fresh ship'нувшейся версии → SQLCipher REPL: `SELECT json_extract(payload_json, '$.event_kind'), count(*) FROM events WHERE ts > strftime('%s','now','-30 minutes')*1000 GROUP BY 1` → expect все 33 новых event_kinds eventually appear (после Linear/GitHub/Slack triggers).
- **Phase 4.7 carry-overs (non-blocking, file as Linear cleanup для 4.8):** (a) hoist GitHubCollector UTC `DateFormatter` to static (B-3 M-1); (b) split `GitHubAPIProvider.swift` (~403 lines / 6 structs) → `GitHubAPISummaries.swift` if 4.8 adds more structs (B-3..B-5 M); (c) add `testTick_LatestPushCheckStatus_PicksMostRecentPush` test (B-5 I-1); (d) document `fetchEvents` failure short-circuit asymmetry в GitHubCollector (B-5 I-2); (e) **NEW (4.7.C):** if Linear API rejects entire query когда workspaces не expose'ят `documents` field — refactor C-8 piggy-back на separate HTTP call с persistent flag (current piggy-back assumes per-tick graceful degrade); (f) **NEW (4.7.C):** consider tightening `testQueryShapeIncludesAssignedIssuesBlock` slice further if 4.8 adds more viewer{} fragments (current bound `assignedIssues(`..`teams(first:`). None block alpha.9.
- **Phase 4.8 — Deep payload metadata (NOT designed yet, follow-up к 4.7).** `commit_pushed` += files_count/+/-lines, `pr_opened` += requested_reviewers/size_category, `linear_status_transition` enrichment, slack message += mention_count/link_count, `linear_reactions_given/received`, expression-индекс на `payload.event_kind` если query latency surfaces, cycle-time analytics. Brainstorm в свою очередь после 4.7 ship'а.
- **Phase 4.9 — Derived modes (NOT designed yet, follow-up к 4.7).** Имплементация `DefaultModeClassifier` (skeleton groundwork лежит в 4.7.C): code/coordination/review/focus/meeting + pulse heavy/medium/light. Mode transitions log в новую `mode_history` table. `presence_state.derived_mode` начинает писаться. Новые MCP tools `get_current_mode` + `get_mode_distribution`. Self-UI mode-tinted timeline. Brainstorm после 4.8 ship'а.
- **Phase 5 (presence relay) — IN FLIGHT.** Sub-phase progression per contract §9: ✅ 5.1.A→E (schema → helpers → codec → service+keystore → UI/landing). ✅ 5.2.A→E (identity/KDF → blob codec → relay endpoints → admin invite UX → invitee accept-flow). ✅ 5.3.A (DB lifecycle mutators substrate). ⏳ **5.3.B next** — `RotationBlobCodec` + `RotationKDF` (HKDF-SHA256 info-string `"leaf/rotation/v1"` для domain separation от invite codec) → 5.3.C (`RelayClient` extension + leaf-relay `/v1/key-rotation/*` endpoints) → 5.3.D (`KeyRotationService` admin orchestrator: `insertTeamKey(new)` → `deprecateTeamKey(old)` → optional `markTeamMemberRemoved` атомарно + `RotationOutbox` journal + N-1 pairwise ECDH wraps per contract §7) → 5.3.E (peer fetch loop `RotationFetchService` + `readTeamKey(byID:)` для buffered-snapshot decrypt path + UI per-row Remove menu + `RemovedFromTeamBanner` + ship-gate two-Mac smoke). → 5.4 (`presence_outgoing` + `presence_history` M009/M010 + WS broadcast loop в Agent + Swift WS client + Team presence grid live UI; first real consumer `ProdEnvelopeCodec`) → 5.5 (Onboarding screen 6 final integration + member-removal UX polish). Расширение `gundemtech/leaf-relay` (приватный repo от Phase 4.4 Slack OAuth bouncer'а), не from-scratch.
- **Layer C (V1.5+)**: MCP-aggregator поверх Notion / Figma / Jira / Gmail / Calendar.
- **Phase 3.5+ cleanup**: delete `KeychainKeyStore.swift` + `LeafError.keychainUnavailable` после ~2 недель stable runtime alpha.6 (target ~2026-05-13).
- **leaf-relay репо housekeeping**: README + CI deploy hook (Wrangler) + документация по `oauth.gundem.tech/<provider>/callback` routing для будущих v1.1+ providers (Notion / Figma).
- **Release tooling improvements** (lessons из alpha.7 + alpha.9): (a) `release.sh` должен auto-bump `CURRENT_PROJECT_VERSION` (или хотя бы fail-fast если build number == prior-version) — alpha.6 ship забыл bump, обнаружили только при alpha.7; (b) `upload-release.sh` whitelist не включает `*.html` release notes — добавить когда понадобится; (c) **NEW (alpha.9):** stamps в `build/releases/.stamps/` не version-aware — на новом MARKETING_VERSION release.sh пропустит archive/notary/staple/zip если stamps от прошлого ship'а лежат → переупакует старые артефакты, новой версии не будет в R2. Fix options: (i) auto-`rm -rf $RELEASES/.stamps` если `$VERSION` не совпадает с last-shipped marker, (ii) включать version в имя stamp (`archive.1.0.0-alpha.9` вместо `archive`), (iii) auto-append `--redo-from archive` если detected version-mismatch. Detection: финальные `du: ... No such file` lines в release.sh output на новой версии — сейчас только этот канал, нужен fail-fast assert.
- **Sparkle ship gotchas**: (a) Xcode Debug LeafAgent залипает в launchd registration → SMAppService.register() в shipped Leaf.app filter "already registered" — `pkill -f "DerivedData.*LeafAgent"` pre-ship; (b) macOS TCC AX permission иногда требует toggle off+on после Sparkle bundle replace (CDHash меняется); (c) **CFBundleVersion должен быть monotonically increasing per ship** — Sparkle update detection ломается без bump'а; (d) **dev-only**: переключение между `/Applications/Leaf.app` (alpha) и `DerivedData/.../Leaf.app` (Debug) сбрасывает BTM parent disposition в `disabled` — агент `spawn failed exit 78` пока юзер не toggle'нёт Login Items → "Allow in the Background" → Leaf ON. Production update path (Sparkle in-place replace) не страдает.

## Open tensions
Живут в `leaf-docs/docs/05-reference/open-tensions.md`. Топ-2:
- **OT-1** distributed deletion локальной forever-history у ex-teammates.
- **OT-2** storage compression для forever retention.
