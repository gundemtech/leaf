# Текущее состояние проекта

_Срез "где мы сейчас" за 30 секунд. Детали каждой phase — в git log + `docs/superpowers/specs/` + `.claude/plans/`. История — в whitepaper changelog._

## Последнее обновление

**2026-05-20 — Track-9 T3 (GitHub INBOX feeder — `gh_pr_review_requested/_removed` + URL enrichment on 15 kinds + `viewer_login` presence finalization + deriver-side notifications synthesis) landed** на `feature/track-9-substrate` (off T2 wrap tip `5ec26f58`, 5 commits = 1 docs spec + 2 atomic feat + 2 test). Substrate-only silent ship — UI без изменений (registry default-OFF surfaces auto через Privacy → Share Controls). **Track-9 third phase.**

T3 adds 2 new event_kinds `gh_pr_review_requested` + `gh_pr_review_request_removed` (PullRequestEvent payload.action="review_requested"/"review_request_removed" — outbound only, captures viewer requesting others; both default OFF per ADR-020, departs from Phase 4.7.A GH default-true precedent to match Track-9 convention). Parse path extends `ProdGitHubAPIProvider.mapPullRequestEvent` switch (moat) with `guard let reviewer = payload["requested_reviewer"] as? [String: Any], let reviewerLogin = reviewer["login"] as? String, !reviewerLogin.isEmpty else { return [] }` — stripped feed (missing `requested_reviewer` field) silent-skips emission entirely per ADR-010 safety (better drop than corrupt downstream analytics). `requested_reviewer_login` plaintext field per pattern parity с Track-3 D2 `PRMetadata.requestedReviewers: [String]` (existing privacy frontier; master spec §6 #6 amendment T10). **15 kinds URL enrichment**: 3 static helpers `composePRURL` / `composeIssueURL` / `composeCommentURL` on `ProdGitHubAPIProvider` (public for moat-test access — pure string interpolation, never read title/body/comment text). `pr_url = "https://github.com/{repo}/pull/{n}"` injected via metadata dict at 5 mapper sites (mapPullRequestEvent / mapIssuesEvent / mapReviewEvent / mapPRReviewThreadEvent / mapPRReviewCommentEvent / mapIssueCommentEvent). `issue_url = "https://github.com/{repo}/issues/{n}"` parallel. `comment_url` overlay on 2 comment kinds composed from parent URL + `comment.id` (`#discussion_r{id}` for PR review-comment / `#issuecomment-{id}` for issue comment). Forward-only — historical events stay unenriched per mirror T2 precedent. `CommentURLKind` discriminator enum. Reserved-key check at `GitHubCollector.makeEvent` line 528-545 verified pr_url/issue_url/comment_url NOT in reserved Set — metadata merge-through clean.

**`presence_state.github.viewer_login` finalization** — single-line addition к existing `githubPresence` composite dict в `GitHubCollector.swift:326-335`. `login` parameter already in scope (passed into `provider.fetch*` methods since OAuth bootstrap); T3 finalizes Phase 4.7.B partial. NSNull-on-empty mirrors existing 2 fields (`prs_awaiting_top_repo` / `latest_push_check_status`) — distinguishes "key absent, field not tracked" from "key present, value null". JSON shape valid for both populated and null cases.

**Deriver-side notifications synthesis** (NOT new capture-path event_kind per §1.1 deviation from master spec line 242). `ProdInsights+InboxItems.queryGitHubNotifications(cutoffMs:)` moat reads last-2-tick `gh_notifications_pulse` rows (Phase 4.7.B substrate already emits each tick with top-level `reason_*_count` keys via `GitHubCollector.swift:387-389`), diffs per-reason counters → synthesizes 1 InboxItem per non-zero positive delta. Negative deltas dropped (user dismissed notifications). Cold-tick degenerate (only 1 pulse row in window) returns `[]` graceful. Reason → InboxKind heuristic: `review_requested → .reviewRequest` / `mention → .mention` / `comment → .commentOnMyWork` / `assigned → .commentOnMyWork` / `state_change → .commentOnMyWork` (last 2 conflated heuristically — existing 5-case `InboxKind` enum doesn't fit, T8 enum expansion per master spec §3.4 line 108 fixes). `sourceURL = https://github.com/notifications`. `ProdInsights+InboxItems.queryReviewRequests(cutoffMs:)` lights up `.reviewRequest` InboxKind — SQL `GROUP BY repo_full_name, number / SUM(*) / MAX(ts) / MAX(pr_url)` aggregates by PR identity. `queryCommentsOnMyWork` GitHub branch (replaces P1 `return []` stub) reads `viewer_login` from `presence_state.github.state_json` via `json_extract` (graceful degrade to `[]` if absent/empty), aggregates `gh_pr_review_comment_authored` + `gh_issue_comment_authored` events by `(repo, number)`. Filter `actor.login != viewer_login` anticipatory — `/users/<login>/events` REST polling today returns viewer-as-actor only (`TODO(T8)` carry — InboxKind semantic split between `.myRecentComments` vs `.commentsByOthersOnMyWork`).

**Slack DM verify outcome — NEGATIVE** (§1.1 master spec line 244 conditional resolved). Substrate отсутствует at all 4 layers (`SlackEventKinds.swift` 21 cases — no DM kind; `SlackCollector` 923 LOC — no DM-fetch path / no DM emission; `ShareEventTypeRegistry` — no Slack DM entry; `presence_state.slack` composite — no DM-bucket field). Carry post-Track-9 — separate phase needed for `conversations.list types=im` polling + `slack_dm_received` event_kind + registry entry + ADR-010 walkback for DM channel anonymization.

**Substrate purity verified:** zero new SQLCipher migrations (M001-M018 + M024 + M026 + M027 = 30 tables preserved), 2 new event_kinds (`gh_pr_review_requested`, `gh_pr_review_request_removed` both default OFF), registry **196 → 198**, zero new MCP tools (`git diff 5ec26f58..HEAD -- LeafMCP/ Packages/LeafCore/Sources/LeafCore/MCP/` empty), zero SQLCipher migration delta (`git diff 5ec26f58..HEAD -- Packages/LeafCore/Sources/LeafCore/DB/` empty). Privacy walkback narrow grep — 0 actual hits in T3-touched file scope (1 hit at `ActivityFeedMapper.swift:96` is pre-existing negative documentation, not a payload read; same line as T2 wrap notes). Sentinel-injection 4 tests `RelayBodyLeakageTests.test_t3_walkback_GH{URLEnrichment,ReviewRequested,ViewerLogin,IntegrationSweep}` — 3 per-invariant (pinpoint regression) + 1 integration sweep (all 3 sentinels through 3 event flavors). T1 lineage padded sentinel pattern (`LEAKED_SENTINEL_GH_T3_PR_BODY` / `_REVIEWER` / `_VIEWER_LOGIN`). **5/5 xcodebuild schemes Debug build SUCCESS** (LeafCore / LeafCorePrivate / Leaf / LeafAgent / LeafMCP). **SPM tests: 2868 XCTest + 45 Swift-Testing = 2913 total, 0 failures, 4 skipped** (T2 baseline 2848 + 20 net new: 14 moat + 6 public — 3 moat parse + 5 moat URL + 6 moat inbox + 2 public viewer_login + 4 public sentinel; 7 fence bump assertions modify literals without adding test rows). 7 historical track-baseline parity fences mechanically bumped 196 → 198 (same file set as T2 commit `c49eea4a`). HomeView.swift LOC unchanged 261. `just check-tokens` 3-tier clean.

**Files committed (5 public commits, moat work local-only):**
- `add096b2 docs(track-9-T3): spec` — 1 file / +818 LOC (`docs/superpowers/specs/2026-05-20-track-9-T3-github-inbox-feeder.md`).
- `4fb51965 feat(track-9-T3): GitHubCollector viewer_login presence dict finalization` — 2 files / +80.
- `0ccb5e49 feat(track-9-T3): GitHubEventKindKey +2 + ShareEventTypeKey + EventKindIcon + ActivityFeedMapper + dispatch coverage for review_requested/_removed` — 6 files / +40.
- `bd972a42 test(track-9-T3): 4 sentinel-injection regression tests (3 per-invariant + 1 sweep)` — 1 file / +306.
- `ef4f6694 test(track-9-T3): bump registry parity fences 196 -> 198` — 7 files / +11/-11.

**LeafCorePrivate moat (gitignored, local-only):** `ProdGitHubAPIProvider.swift` (mapPullRequestEvent switch +2 cases + 3 URL composition static helpers + URL injection at 5 mapper sites + `CommentURLKind` discriminator enum), `ProdInsights+InboxItems.swift` (queryReviewRequests new + queryCommentsOnMyWork GitHub branch replacing P1 stub + queryGitHubNotifications synthesis new + inbox aggregation wired 3 new GH feeders into `inboxItems(filter:query:)`). 14 moat tests landed: `ProdGitHubAPIProviderHotExtensionsTests` (3 parse + 5 URL) + `ProdInsightsInboxItemsTests` (6) — local-machine verified.

**Out of scope (T3 spec §1 hard exclusion, carry to subsequent phases):** `gh_notification_received` as capture-path event_kind (§1.1 deviation — deriver-side preferred, master spec §5.1 amendment T10). Retroactive URL backfill on historical events (T8 `InboxSourceURLDeriver`). Slack DM bucket routing into INBOX (post-Track-9 — substrate absent). Per-notification ID polling via `GET /notifications?per_page=50` (post-Track-9 — substantial new API surface). Cross-actor anonymization buckets for reviewer logins (post-Track-9 — pattern parity with existing Track-3 D2 plaintext). Inbound review request flow as discrete events (T8 + post-Track-9 — surfaced via notifications synthesis). InboxKind enum expansion `.assigned`/`.stateChange` (T8 — master spec scope lock #3). Real-GitHub smoke (T10 wrap). `get_github_inbox` MCP tool (future).

Spec: `docs/superpowers/specs/2026-05-20-track-9-T3-github-inbox-feeder.md`. Plan: `.claude/plans/track-9-T3.md` (gitignored).

**2026-05-20 — Track-9 T2 (Linear INBOX feeder — `linear_comment_authored_to_me` + workspace_slug + `linear_issue_url`) landed** на `feature/track-9-substrate` (off T1 tip `07a88bc0`, 9 commits = 1 docs spec + 6 atomic feat T2..T7 + 1 sentinel test + 1 post-review NIT fix-bundle). Substrate-only silent ship — UI без изменений (registry default-OFF surfaces auto через Privacy → Share Controls). **Track-9 second phase.**

T2 adds 1 new event_kind `linear_comment_authored_to_me` (per-issue aggregate sibling to existing Phase 4.7.A `linear_comment_authored`, default OFF per ADR-020). Discriminator filter is `actor.id != viewer.id` on Phase 4.7.A-batched viewer-touched issues (assigned-to-me ∪ created-by-me). Provider parser (`ProdLinearGraphQLProvider` moat) extends existing per-comment loop with opposite predicate — single-pass, zero new HTTP calls. `LinearIssueSnapshot.incomingCommentCount: Int = 0` defaulted trailing param preserves all existing callsites + Sendable+Hashable auto-synth. `LinearIssueBatch.workspaceSlug: String? = nil` defaulted trailing field carries `viewer.organization { urlKey }` piggybacked into existing LeafPoll viewer block (+1 complexity point, well within 75-pt budget). `LinearCollector` actor state caches workspaceSlug; capture happens BEFORE `commentToMeEvents` emission so tick-1 URLs populate cleanly (post-review NIT-1 fix). `presence_state.linear.workspace_slug` JSON dict key added for cross-process readers (no schema migration — JSON-blob shape). `makeCommentToMeEvent(issue:periodEndMs:workspaceSlug:)` factory composes `linear_issue_url = "https://linear.app/{slug}/issue/{key}"` Optional payload field — omitted when slug nil/empty (cold-tick / free-tier / schema drift). `ProdInsights+InboxItems.queryLinearCommentsOnMyWork` (LeafCorePrivate moat) lights up `commentOnMyWork` InboxKind Linear branch — SQL `GROUP BY issue_key, SUM(to_me_count_in_window), MAX(period_end_ms), MAX(linear_issue_url)` aggregates across polling ticks. `InboxItem` mapping: `kind=.commentOnMyWork / severity=.muted / title=issue_key / sourceMeta="Linear · {key}" / sourceURL=URL(linear_issue_url)?`. `ActivityFeedMapper.mapLinear` +1 case (label "{key}: N comment{s} to you") + `EventKindIcon` +1 mapping (`bubble.left.fill`). Defense-in-depth schema-drift guard on incoming-tally loop (`guard !viewerId.isEmpty else { return 0 }`) — symmetric to outbound natural safety, prevents over-inflation when viewer.id missing (post-review NIT-A/B fix).

**Deferred to T8 (explicit §1.1 deviation from master spec line 159):** 4.7.C router event INBOX routing (assignee→to_self / priority→Urgent / label→blocked / project-health-red) — requires new `InboxKind` enum cases per master spec scope lock #3. **Deferred post-Track-9:** @-mention detection (Linear `Notification` query path, separate phase); `linear_issue_url` on other linear_* event_kinds (T8 if deriver demands); multi-workspace handling (integrations PK single-row constraint).

**Substrate purity verified:** zero new SQLCipher migrations (M001-M018 + M024 + M026 + M027 = 30 tables preserved), zero new MCP tools (`git diff 07a88bc0..HEAD -- LeafMCP/` empty), zero UI surface changes (`git diff 07a88bc0..HEAD -- Leaf/Views/Window/Home/HomeView.swift` empty). ShareEventTypeKey registry 195 → 196 (`linearCommentAuthoredToMe`, default OFF). 7 historical track-baseline parity fences mechanically bumped 195 → 196. Privacy walkback narrow grep — 0 actual hits in T2-touched file scope (1 hit at `ActivityFeedMapper.swift:96` is pre-existing negative documentation, not a payload read). Sentinel-injection regression test `test_t2_walkback_linearCommentToMe_urlIsStructurallyComposed` two-pronged: (1) factory boundary — `makeCommentToMeEvent` URL composition asserts structural-only content; (2) write boundary — `writeEventsOffsetAndPresence` with sentinel-bearing body payload asserts presence_state.linear.state_json clean. **5/5 xcodebuild schemes Debug build SUCCESS** (LeafCore / LeafCorePrivate / Leaf / LeafAgent / LeafMCP). **SPM tests: 2848 XCTest + 45 Swift-Testing = 2893 total, 0 failures, 4 skipped** (T1 baseline 2869 → +24 net new across public + moat: 4 LinearGraphQL public round-trips + 6 Linear moat parser + 2 buildLinearPresenceState + 3 makeCommentToMeEvent + 4 ProdInsights InboxItems moat + 1 sentinel + 4 fence bumps). HomeView.swift LOC unchanged 261. `just check-tokens` 3-tier clean.

**Files committed (9 public commits, moat work local-only):**
- `feb81b35 docs(track-9-T2): spec` — 1 file / +564 LOC (`docs/superpowers/specs/2026-05-20-track-9-T2-linear-inbox-feeder.md`).
- `67942f81 feat(track-9-T2): LinearIssueSnapshot +incomingCommentCount` — 2 files / +37.
- `99c68d93 feat(track-9-T2): LinearIssueBatch +workspaceSlug` — 2 files / +26.
- `1a9e8a43 feat(track-9-T2): LinearCollector workspaceSlug actor state + presence dict` — 2 files / +47.
- `494b74a6 feat(track-9-T2): makeCommentToMeEvent factory + emission loop` — 2 files / +111.
- `54192e00 feat(track-9-T2): ShareEventTypeKey + EventKindIcon + ActivityFeedMapper` — 3 files / +20.
- `e4d66c82 test(track-9-T2): sentinel-injection regression` — 1 file / +93.
- `c49eea4a test(track-9-T2): bump registry parity fences 195 -> 196` — 7 files / +11/-11.
- `35071974 fix(track-9-T2): post-review NIT-1 — capture workspaceSlug before emission` — 1 file / +9/-7.

**LeafCorePrivate moat (gitignored, local-only):** `ProdLinearGraphQLProvider.swift` extensions (viewer.organization{urlKey} GraphQL fragment + incomingCommentCount per-comment loop + workspaceSlug Page-struct field + fetch-loop capture + LinearIssueBatch return), `ProdInsights+InboxItems.swift` queryLinearCommentsOnMyWork SQL + InboxItem assembly. 9 moat tests landed in `ProdLinearGraphQLProviderTests` (6) + `ProdInsightsInboxItemsTests` (4) — local-machine verified.

**Out of scope (T2 spec §1 hard exclusion, carry to subsequent phases):** **T5** YOU·NOW branch deriver UI impl (currentTaskIdentity per-IDE dispatch, YouNowBlock active-state line 2). **T7** WhereStoppedSnapshot field additions + 4-line layout. **T8** InboxKind enum expansion (+5-7 new cases) + 4.7.C router events routing + `linear_issue_url` on other linear_* kinds if deriver demands. **Post-Track-9:** @-mention detection via Linear Notification query; multi-workspace handling; `get_linear_inbox_to_me` MCP tool; real-Linear smoke (deferred to T10 wrap).

Spec: `docs/superpowers/specs/2026-05-20-track-9-T2-linear-inbox-feeder.md`. Plan: `.claude/plans/track-9-T2.md` (gitignored).

**2026-05-19 — Track-9 T1 (Collector payload extensions + recentLastCommit deriver) landed** на `feature/track-9-substrate` (off `feature/phase-8-1-substrate` `e659b9e5`, 12 commits = 1 docs spec + 9 atomic feat/refactor/test T1..T9 + 1 wrap docs). Substrate-only silent ship — UI без изменений кроме 2 Settings toggle rows. **Track-9 first phase.**

T1 substrate adds 3 collector payload field extensions (no new event_kinds): `xcode_active_doc_changed.line: Int?` (Track-6 P2 Xcode collector расширен AX-line capture через `AXSelectedTextRange` + `AXLineForIndex` parameterized attribute — два AX-вызова, **no document text read**, AXLineForIndex returns Int directly; failure modes — AX trust revoked / focused element not text area / Xcode pid stale — gracefully yield nil + omit payload field); `vscode_workspace_opened.workspace_root: String` (~-prefixed sanitized path emit'ится FSEvents watcher'ом из `parseWorkspaceJSON.sanitizedPath`; gated by `LocalAppsStore.ideWorkspacePathTrackingEnabled` default ON; defense-in-depth — только tilde-prefixed paths достигают payload, bare `/Users/...` silently dropped); `jetbrains_recent_project_observed.workspace_root: String` (XML `<entry key="$USER_HOME$/...">` parsed → `~/...`; `$PROJECT_DIR$/...` keys yield workspaceRoot nil — not home-anchored). New substrate API: `DerivedInsights.recentLastCommit(maxAgeMs: Int64) throws -> RecentCommitSnapshot?` + `RecentCommitSnapshot { subject, branch, atMs }` Equatable+Sendable value type в `LeafCore/Insights/` + `ProdInsights+LastCommit.swift` SQL impl (LeafCorePrivate moat, gitignored). SQL uses `json_extract(payload_json, '$.event_kind') = 'gh_commit_pushed'` pattern from sibling `ProdInsights+TodayMetrics.queryCommitCount` (not the LIKE pattern initially in plan — sibling reuse preferred). `StubInsights.recentLastCommit → nil` + protocol extension default returning nil. Settings → System Observers IDE storage sub-section +2 toggle rows (axLineCaptureEnabled / ideWorkspacePathTrackingEnabled, both default ON, IDEStorageToggleRow primitive matches existing `vscodeStorageEnabled`/`jetbrainsStorageEnabled` pattern). 4 new sentinel-injection regression tests in `RelayBodyLeakageTests` (`test_t1_walkback_xcodeActiveDocChanged_lineNeverLeaksContent` / `_vscodeWorkspaceOpened_workspaceRootIsTilde` / `_jetbrainsRecentProjectObserved_workspaceRootIsTilde` / `_integrationSentinelSweep`). **P6 sentinel refactor:** `LEAKED_SENTINEL_VSCODE_P6_PATH` → `LEAKED_SENTINEL_VSCODE_USERNAME` — sentinel injection moved from path-basename (`Desktop/SENTINEL`) → username position (`/Users/SENTINEL/Desktop/myws`); rationale — post-T1 the basename position MAY legitimately survive into `workspace_root` payload field (tilde-prefixed, no PII), so old sentinel lost discriminating power. New assertion ensures username sentinel never appears + `/Users/` absolute prefix never anywhere + positive `workspace_root == "~/Desktop/myws"`. Integration sweep sentinel list updated.

**DEVIATION from Track-9 master spec §5.1:** workspace_root emit'ится на FSEvents-driven `vscode_workspace_opened` (НЕ title-parser `vscode_active_doc_changed`). Reason: title parser physically not has access to absolute path (title sanitized by VSCode itself before AX exposure). T5 deriver (separate phase) consumes via cheap indexed DB-JOIN `latest vscode_workspace_opened WHERE workspace_name = $X` вместо in-memory cache + cross-process state. Cold-start case — if no `workspace_opened` event yet, deriver gracefully falls back, no regression. Master spec §5.1 amendment carry to T10 wrap.

**Substrate purity verified:** zero new SQLCipher migrations (M001-M018 + M024 + M026 + M027 = 30 tables preserved), zero new event_kinds, zero new MCP tools, ShareEventTypeKey registry frozen at 195 baseline (`git diff feature/phase-8-1-substrate -- Share/ShareEventTypeRegistry.swift` empty). Privacy walkback narrow grep AC-17 — 0 hits forbidden fields в T1 file scope (`absolute_path|full_comment_body|raw_email|notes_body|email_subject|note_body|file_contents|raw_prompt|tool_input|tool_response|response_body`). **5/5 xcodebuild schemes Debug build SUCCESS** (LeafCore / LeafCorePrivate / Leaf / LeafAgent / LeafMCP). **SPM tests: 2824 XCTest + 45 Swift-Testing = 2869 total, 0 failures, 4 skipped**, ~32 visible net new T1 tests (3 RecentCommitSnapshot + 1 DerivedInsightsStub + 4 ProdInsightsLastCommit moat + 4 LocalAppsStoreTrack9T1 + 3 XcodeStateMachine line + 4 ProdXcodeAdapterAXLine moat + 4 VSCodeWorkspaceWatcher T1 + 5 JetBrains T1 + 4 RelayBodyLeakageTests t1_walkback). HomeView.swift LOC unchanged 261 (T1 doesn't touch HomeView, ≤280 budget preserved). `just check-tokens` 3-tier clean.

**Files committed (8 commits, public surface only):**
- `319c844a feat(track-9-T1): RecentCommitSnapshot value type` — +2 files / +44 LOC.
- `56c6ae3c feat(track-9-T1): DerivedInsights.recentLastCommit protocol method` — +2 files / +27 LOC (protocol + extension default + StubInsights override + stub test).
- `d8c7e7ca feat(track-9-T1): LocalAppsStore +2 toggle properties` — +2 files / +68 LOC.
- `0a2a4fbd feat(track-9-T1): XcodeObservation +line field, payload emission` — 3 files / +54 LOC (XcodeObservation +line: Int? trailing default, XcodeStateMachine.makeDocChangedEvent emit, 3 tests, `docChanged` predicate comment).
- `f5521518 feat(track-9-T1): ProdXcodeAdapter AX line capture + LocalAppsStore gating` — 1 file public (`AXLineProvider` protocol +10 LOC); ProdAXLineProvider impl + parseObservation static helper + 4 tests all in LeafCorePrivate moat (gitignored).
- `60b5af71 feat(track-9-T1): VSCodeWorkspaceWatcher workspace_root payload` — 2 files / +93 LOC (buildEvent +workspaceRootEnabled trailing default, defense-in-depth tilde check, walkback contract comment T1 lines 6+8, caller wires `localAppsStore.ideWorkspacePathTrackingEnabled` via trailing-default init param).
- `1e574660 feat(track-9-T1): JetBrainsRecentProjectsWatcher workspace_root payload` — 2 files / +136 LOC (extractWorkspaceRoot helper `$USER_HOME$` → `~`, ParsedEntry.workspaceRoot field, buildEvent +workspaceRoot+workspaceRootEnabled, caller wires same flag).
- `168cfde8 feat(track-9-T1): Settings System Observers +2 toggle rows` — 1 file / +27 LOC (IDEStorageSettingsSection extended with 2 IDEStorageToggleRow primitives, matching existing pattern).
- `aefa5342 refactor(track-9-T1): rename P6 VSCode sentinel to username position` — 1 file / +19 LOC.
- `ed574d81 test(track-9-T1): 4 sentinel-injection regression tests` — 1 file / +159 LOC.

**LeafCorePrivate moat (gitignored, local-only, не пушится в публичный leaf репо per pre-push-leaf checklist):** `ProdInsights+LastCommit.swift` (json_extract SQL + empty-branch normalization), `ProdAXLineProvider.swift` (AXIsProcessTrusted gate + 2-call sequence), `ProdXcodeAdapter+ParseObservation.swift` (helper extension), `Tests/LeafCorePrivateTests/Insights/ProdInsightsLastCommitTests.swift` (4 tests), `Tests/LeafCorePrivateTests/Apple/ProdXcodeAdapterAXLineTests.swift` (4 tests). Build + test verified locally on this machine.

**Out of scope (T1 spec §1 hard exclusion, carry to subsequent T2..T10 phases):** **T5** YOU·NOW branch deriver UI impl (`currentTaskIdentity()` per-IDE dispatch reading T1 payload fields → `.git/HEAD` walk → branch + LinearIDExtractor → emit `branch` + `linearID` in YOU·NOW snapshot; YouNowBlock UI surface for `.active`-state line 2 rendering). **T7** `WhereStoppedSnapshot.{anchorFilePath, anchorLine, recentLastCommit}` field additions + `ProdWhereStoppedDeriver` extension consuming `recentLastCommit`; M028 `where_stopped_log` add columns OR resolve via deriver-side JOIN against `events.payload_json`; WhereStoppedBlock 4-line layout. `vscode_active_doc_changed.workspace_root` — master spec §5.1 deviation; T5 deriver consumes via `vscode_workspace_opened` DB-join. VSCode/JetBrains AX line capture — Electron AX surfaces vary per renderer pid; JBR custom AX implementations not consistent. AI subagent failure detector — Phase 4.9 AI rollup. `get_weekly_metrics` MCP tool — post-Track-9 if requested.

Spec: `docs/superpowers/specs/2026-05-19-track-9-T1-collector-payload-extensions.md`. Plan: `docs/superpowers/plans/2026-05-19-track-9-T1-collector-payload-extensions.md` (gitignored).

**2026-05-19 — Track-8 Phase 8.9 (Home UX polish + a11y + sweeps + Track-9 stub, P9) landed** на `feature/phase-8-9-polish` (off `feature/phase-8-1-substrate` `c0abb7ba`, 13 commits = 2 docs spec + 8 feat/refactor T1..T8 + 1 polish sweep + 1 Track-9 stub + 1 review-feedback fix). FF в `feature/phase-8-1-substrate` после этого commit'а. **Track-8 wrap.**

P9 closes Track-8 с тремя deliverables: (1) **6 view-only carry-over resolutions** из master spec §9.1 (C-3 ViewThatFits / C-4 DateFormatter cache / C-7 a11y / C-17 InboxBlock test extraction / C-18 icon differentiation / C-22 formatRelative unification). (2) **3 sweeps** — HIG manual / a11y subagent audit / perf manual. (3) **Track-9 spec stub** + master spec §9.1 inline per-carry status markers (24/24 tagged: 6 RESOLVED P9 + 18 DEFERRED → Track-9 stub).

**C-22 unification (T1-T4):** New `Packages/LeafCore/Sources/LeafCore/Home/HomeRelativeTimeFormatter.swift` (41 LOC) — pure-function bucket ladder `format(deltaMs:nowMs:) -> String` ("now" / "Nm ago" / "Nh ago" / "yesterday" / "N days ago" / absolute "MMM d") shared across 3 Home block callsites. Cached `private static let absoluteFormatter` with `en_US_POSIX` locale + `TimeZone(identifier: "UTC")` pin (review-feedback fix — без TZ pin test `testAbsoluteBucket_14days` упал бы в TZ ≤ UTC-1, latent CI flake). 8 XCTest cases lock the contract (now / minutes / hours / yesterday / days / absolute / clock-skew guard / zero-delta). Callsite migrations: `WhereStoppedBlock.swift:89` full inline replacement (drops local `formatRelative` static + `absoluteFormatter` static = -17 LOC); `WithYouOnThisBlock.swift:189` + `YouNowBlock.swift:267` thin wrapper preservation (callsites stay one-liner, body delegates to shared helper). User-visible string semantics change in WithYou + YouNow: "Ns / Nm / Nh / Nd ago" + formatDuration-based "X min ago" → canonical "now / Nm ago / Nh ago / yesterday / N days ago / MMM d" — richer wording matches WhereStopped voice + master spec §4.3 mockup (R-3 in spec §9).

**C-17 + C-18 (T5-T6):** New `Packages/LeafCore/Sources/LeafCore/Home/InboxFiltering.swift` (31 LOC) — pure-function `filtered(items:filter:query:) -> [InboxItem]` extracting `InboxBlock.filteredItems` computed property logic to LeafCore. Delegates kind-filter to existing `InboxFilter.admits(_:)` predicate (no new property additions on InboxFilter). Query semantics preserved: whitespace-trim + lowercased + substring match on `title || sourceMeta`. 10 XCTest cases lock contract (empty / .all / .reviews / .questions / .mentions filter + case-insensitive + whitespace-trim + sourceMeta-match + AND combination). `InboxBlock.swift:47` filteredItems now single-line delegate. C-18 same commit: `noMatchState` icon swap `LeafIcons.brand.leaf` → `LeafIcons.nav.searchSF` ("magnifyingglass" SF Symbol) — semantic discriminator between "All clear" (positive, achievement) and "No matches" (search miss, user-actionable). `emptyDataState` keeps brand.leaf.

**C-3 + C-4 (T7):** `TodayBlock.swift`. C-3: `metricsRow` wrapped in `ViewThatFits(in: .horizontal)` with two branches — wide horizontal 4-cell row (current shape preserved: focused / AI ratio / sessions / commits + Spacer) + narrow 2×2 grid (focused + AI ratio | sessions + commits). No `@State`, no `GeometryReader`, no fixed cutoff — SwiftUI picks first branch that fits. Reading order preserved для VoiceOver narrate consistency. **Substrate has `TodayMetrics.switchCount` field**, но `metricsRow` ships 4 cells (not 5) per current Phase 8.3 production ship — added as a Track-9 candidate (out of P9 scope: ViewThatFits = narrow-window fallback, не "add missing cell"). C-4: `sectionDateFormatter` hoisted to `private static let` с `.current` locale + `setLocalizedDateFormatFromTemplate("EEE d MMM")` — `sectionLabel` no longer allocates per body re-eval. Locale captured at first access; relaunch refreshes if user changes system locale mid-session.

**C-7 (T8 + reverted in sweep T9):** Iteration cycle landed final shape per `OQ-P9-1` design question + a11y audit BLOCKER finding. Initial T8 wrapped outer `.away` in `Button(action: handleTap) { content }.buttonStyle(.plain).accessibilityAddTraits(.isButton)` to satisfy master spec §9.1 C-7 wording. A11y audit (general-purpose subagent) flagged this as BLOCKER: nested Button (outer wrap + inner Resume CTA Button at `YouNowBlock.swift:224`) causes VoiceOver focus + hit-test ambiguity on macOS. Reverted to `.contentShape(Rectangle()) + .onTapGesture { handleTap() } + .accessibilityAction(named: Text("Resume")) { handleTap() }` — VoiceOver users navigate to the rotor-addressable "Resume" action without wrapping, inner Resume CTA Button keeps its own `.isButton` trait + label + independent focus per macOS HIG. `YouNowTapModifier` private struct retained (final shape in `YouNowBlock.swift:321-352`).

**Sweeps (T9-T10 a11y + HIG + perf):** Combined polish sweep commit `f8464da2` applied **6 a11y inline fixes** beyond C-7 BLOCKER: `YouNowBlock.rowLayout` VStack `.accessibilityElement(children: .combine)` (title + line2 + line3 read as one element); `YouNowBlock.leadingIcon` LeafIconChip fallback `.accessibilityHidden(true)` (was only `nsImage` branch hidden); `TodayBlock.metricCell` `.accessibilityElement(children: .ignore) + .accessibilityLabel("\(value) \(label)")` (combines "42m" + "focused" as "42m focused"); `InboxItemRow` dropped `.disabled(item.sourceURL == nil)` — macOS removes disabled Buttons from VoiceOver focus tree, making informational D3-derived rows unreachable; `handleTap()` already guards nil URL + conditional `.isButton` trait keeps non-interactive announce correct; `HomeView` loading state ProgressView `.accessibilityHidden(true)` (redundant with sibling "Reading recent activity…" Text) + 3 em-dash placeholder cards `.accessibilityHidden(true)` (decorative scaffold; VoiceOver previously read "em dash" 3 times). **HIG sweep**: `just check-tokens` 3-tier clean (no raw `Color.` / `.font(.system(` / `DateFormatter()` per-render / `ForEach.indices` hits). 4 intentional pixel-level literals retained (avatar 32×32, intensity 3×8 bars — documented in P8.4 spec). **Perf sweep**: all DateFormatter allocations confirmed `static let` cached post-C-4. **A11y carry-forwards to Track-9 stub**: design-system primitive a11y audit (LeafPill / LeafInput / LeafIconChip used as Button labels); WhereStoppedBlock `headerText` Timer-driven refresh; pluralization helpers; TodayBlock pill label/hint split; InboxFilterRow selection-change announcement; WithYouOnThisBlock label/hint separation.

**Track-9 stub (T11):** New `docs/superpowers/specs/2026-05-19-track-9-substrate-enrichment.md` groups 14 unresolved §9.1 carries + 1 Phase 5.6 dep + 1 v1.1 deferred + P9 a11y carry-forwards по 8 themes: A YOU·NOW depth (C-5/6/9 + current-state YOU·NOW backlog: git polling deriver, intensity bars Input Monitoring TCC, label tweak, .away/.deepWorkFocus enrichment) / B empty state + presence enrichment (C-10/13) / C route + URL plumbing (C-12/15/16) / D WHERE STOPPED enrichment (C-20/21) / E Analytics surface (C-23/24) / F reader state machine (C-1/2) / G InboxBlock SQL re-fetch (C-14) / H localization (C-19). Phase 5.6 = C-11. v1.1 = C-8. Stub explicitly STATUS: STUB — full brainstorm pending in separate session.

**Master spec §9.1 annotation (T11):** P9 status block injected at top of §9.1. 24 carries individually tagged inline (6 `**[RESOLVED P9 — commit <sha>]**` + 18 `**[DEFERRED → Track-9 stub]**`) per AC-12 explicit requirement (review feedback caught aggregate-only initial pass). RESOLVED commits: C-3 + C-4 = `0dbadcd7`; C-7 = `f8464da2`; C-17 = `52f4483e + 835141c7`; C-18 = `835141c7`; C-22 = `cd6a1c52 + d3669782 + 0b80dfc9 + 2a698f11`.

**Out of scope (P9 spec §1.1 hard exclusion):** 14 Track-9-deferred carries (substrate-touching), Phase 5.6 dep (C-11 relay status plumbing), v1.1 deferred (C-8 git branch --list shell exec for Resume CTA staleness), Track-9 localization track (C-19), full Track-9 spec brainstorm (separate session), Track-8 substrate-branch → main merge (collective ship after substrate-branch full wrap including all P3..P9 commits, separate ship-prep session).

**Independent code review:** APPROVE-WITH-NITS — 1 IMPORTANT TZ-flake fix applied (formatter UTC pin), 2 IMPORTANT spec AC text drifts reconciled (AC-1 4-cell / AC-3 .accessibilityAction path / AC-6 thin-wrapper acknowledgment), 1 IMPORTANT AC-12 per-carry markers added inline (24/24 tagged). NIT findings retained as code-comment notes (thin formatRelative wrappers may unify further in Track-9; conditional `.isButton` trait pattern acceptable today). All 6 P9-target carries functionally resolved.

**Zero substrate touches verified:** `git diff feature/phase-8-1-substrate -- Packages/LeafCore/Sources/LeafCore/DB/ / Privacy/ShareEventTypeKey.swift / LeafMCP/` all empty. ShareEventTypeKey registry frozen at 195 baseline post-Track-6. No new SQLCipher migrations / event_kinds / MCP tools. **Privacy walkback narrow grep** — 0 hits forbidden fields (`absolute_path|full_comment_body|raw_email|notes_body|email_subject|note_body|file_contents|raw_prompt|tool_input|tool_response|response_body`) в P9 file scope. **5/5 xcodebuild schemes Debug build SUCCESS** (LeafCore / LeafCorePrivate / Leaf / LeafAgent / LeafMCP). **SPM tests: 2793 XCTest + 45 Swift-Testing = 2838 total**, 0 failures (+15 net new vs P8 baseline 2823 — 8 HomeRelativeTimeFormatter + 10 InboxFiltering − 3 test count baseline discrepancy resolved as expected variance; actual delta matches +18 helper tests across two files). **HomeView LOC 261/280** ✓. **HomeRelativeTimeFormatter LOC 42/80** ✓. **InboxFiltering LOC 31/60** ✓. `just check-tokens` 3-tier clean.

Track-8 wrap manual smoke (master spec §10 click-through P3..P8) deferred to Track-8 substrate-branch → main merge ship-prep session (all of P3..P9 integrated). Automated verification gates all green.

Spec: `docs/superpowers/specs/2026-05-19-phase-8-9-polish.md`. Plan: `.claude/plans/phase-8-9.md` (gitignored). Track-9 stub: `docs/superpowers/specs/2026-05-19-track-9-substrate-enrichment.md`.

**2026-05-19 — Track-8 Phase 8.8 (Activity → Analytics rename, P8) landed** на `feature/phase-8-8-analytics-rename` (off `feature/phase-8-1-substrate` `866dd04f`, 8 commits = 1 docs spec + 6 feat T1..T6 + 1 post-review fix T7). FF в `feature/phase-8-1-substrate` после этого commit'а.

Mechanical-rename phase: Activity tab swap for honest Analytics placeholder. Three-layer rename + content replacement, zero substrate touch. **`WindowSection.activity`** → **`.analytics`** (enum case + title "Activity" → "Analytics" + icon mapping); **`LeafIcon_Nav.activity`** → **`.analytics`** + Asset.xcassets imageset folder + inner SVG file rename (`leaf-nav-activity.imageset/leaf-nav-activity.svg` → `leaf-nav-analytics.imageset/leaf-nav-analytics.svg` + `Contents.json` filename ref); 8 call-site updates: `Sidebar.swift:34` group items + `RootView.swift:65` switch case + `TokensIconsSection.swift:60` design-system browser entry + `WindowState.swift:13,28` title/icon + `LeafNavRowPreview.swift:15` token preview row + `LeafTabPreview.swift` design-system local enum (hygiene rename for consistency) + `LeafToolbarPreview.swift:22` + `LeafWindowLayoutPreview.swift:24,43` (post-review NIT-1 fix). **`AnalyticsView.swift`** 40 LOC new file at `Leaf/Views/Window/Analytics/` — pure static placeholder: header `Text("Analytics").font(LeafType.title.large)` matching `ConnectionsView` precedent + `LeafCard(padding:.regular)` + `LeafEmptyState(icon: LeafIcons.brand.leaf, title: "Analytics view coming soon", description: "Patterns, trends, and weekly summaries will land here.")`. Zero `@State` / `@Observable` / `InsightsReader` / `RouteCoordinator` injections; no tap targets; no animations. Empty-state copy lifted verbatim from master spec §5 line 272 with title hoisted into `LeafEmptyState.title` for visual hierarchy parity с P6/P7. **Deleted (-498 LOC view code):** `Leaf/Views/Window/Activity/ActivityView.swift` (275) + `SessionRow.swift` (68) + `ActivityRow.swift` (155) + empty folder. **Substrate preserved (MCP consumers):** `Packages/LeafCore/Sources/LeafCore/Insights/ActivityFeedMapper.swift` (994 LOC, consumed by `LeafMCP/Tools/RecentActivityTool`), `EventKindIcon.swift`, `ActivityFeedEntry` / `ActivitySession` types, `ActivityFeedMapperTests` + `ActivityFeedMapperZoomP5Tests` + `ActivityFeedMapperLocalOSTests` + `EventKindIconTests` все unchanged. **Doc-comment touch-ups (T5):** `Leaf/Resources/AppIconResolver.swift:8` stripped `SessionRow` mention; `Packages/LeafCore/Sources/LeafCore/Insights/EventKindIcon.swift:4` stripped `ActivityRow` mention — both preserve surrounding context. **No `AppRoute.activity` case existed** (sidebar-only navigation), zero backwards-compat shims needed. **Zero new SQLCipher migrations / event_kinds / MCP tools / ShareEventTypeKey delta** (registry frozen 195 baseline post-Track-6, AC-15/16/17 verified empty `git diff feature/phase-8-1-substrate -- DB/ / Privacy/ShareEventTypeKey.swift / MCP/ + LeafMCP/`). **Privacy walkback narrow grep AC-9** — 0 hits forbidden fields в 6 Phase 8.8 file scope. **No sentinel-injection test added** — P8 reads no event data (pure static placeholder). Tokens-only fidelity — `just check-tokens` 3-tier clean. **5/5 xcodebuild schemes green** (LeafCore / LeafCorePrivate / Leaf / LeafAgent / LeafMCP Debug build SUCCESS). **SPM tests:** **2778 XCTest + 45 Swift-Testing = 2823 total**, 0 failures, 4 skipped (**±0** net new vs Phase 8.7 baseline — pure UI rename + placeholder, no new tests by design). AnalyticsView.swift LOC 40 (≤45 target / ≤60 cap). Independent code review: **APPROVE-WITH-NITS** — NIT-1 (3 stale "Activity" string literals in Tokens Preview files `LeafToolbarPreview` + `LeafWindowLayoutPreview` — fix inline T7) / NIT-2 (`InsightsSnapshot.recentActivity` field + `InsightsReader.refresh()` call orphaned после ActivityView deletion, ~200 rows fetched + serialized per refresh tick for zero UI consumers — carry C-24 to master spec §9.1 для Track-9 drop когда Analytics surface design settled, LeafMCP RecentActivityTool calls `recentActivity` через own reader so substrate cannot drop). 3 remaining "Activity" hits в `Leaf/` — LayerB integration detail screens (`LinearDetailScreen`/`GitHubDetailScreen`/`SlackDetailScreen` `LeafSection(title: "Activity")`) — per-provider activity-feed section titles, не nav-chrome — стоят.

Per-task substrates (6 atomic commits + T7 review fix): **T1** Asset.xcassets imageset folder + inner SVG file + `Contents.json` filename ref rename (icon enum + callsites still point to old name; runtime image-not-found warning transitional). **T2** `LeafIcon_Nav.activity` → `.analytics` enum + 2 callsites (asset-callsite match restores). **T3** `WindowSection.activity` → `.analytics` enum + title + 5 callsites (RootView still calls `ActivityView()` transitionally). **T4** `AnalyticsView.swift` new placeholder + `RootView.swift:65` swap. **T5** delete 3 obsolete view files + empty folder + 2 doc-comment touch-ups. **T6** master spec §9.1 carry-over C-23 (Analytics real content → Track-9) + 8 verification gates. **T7** post-review NITs fix bundle — 3 Tokens Preview stale strings renamed + C-24 carry append.

**Out of scope (P8 spec §1.1 hard exclusion):** Week-scoped surface chip strip mirroring TODAY pills — substrate gap (`DerivedInsights.weeklyMetrics(now:)` doesn't exist, adding violates "substrate-only swap" framing) → carry C-23 Track-9. Full Analytics surface (week breakdown chart, deep-work streaks, peak-hour, top tools, WoW deltas, AI ratio over time) — explicit master spec §5 deferral to Track-9. HIG/a11y/perf sweep — P9. Manual smoke — Track-8 wrap. AI narrative — v1.1. `AppRoute` deep-link case rename — no `.activity` case exists, sidebar-only navigation.

**Production reality:** AnalyticsView рендерит честный "Analytics view coming soon" placeholder — лучше misleading данных предыдущего ActivityView ("просто какие-то данные without insight" per master spec §5 line 277). До Track-9 design Analytics tab non-functional but transparent. После Track-9 — real Analytics surface (charts/streaks/peak-hour/etc) появится без UI-chrome-changes (single-line `placeholderCard` swap → real content).

Carry-overs appended to master spec §9.1 (`docs/superpowers/specs/2026-05-18-track-8-home-ux-design.md`): **C-23** Analytics surface real content (week chip strip + charts/streaks/deltas) → Track-9 dedicated brainstorm + spec covering substrate additions (`DerivedInsights.weeklyMetrics(now:)` or equivalent) + LeafCorePrivate Prod impl + full Analytics surface design. **C-24** `InsightsSnapshot.recentActivity` + `InsightsReader.refresh()` orphaned fetch — drop substrate-side once Analytics design settled (LeafMCP RecentActivityTool calls `recentActivity` directly через own reader, фундамент остается).

Spec: `docs/superpowers/specs/2026-05-19-phase-8-8-analytics-rename.md`. Plan: `.claude/plans/phase-8-8.md` (gitignored).

**2026-05-19 — Track-8 Phase 8.7 (WHERE STOPPED block wire-up) landed** на `feature/phase-8-7-where-stopped` (off `feature/phase-8-1-substrate` `b2d1b55b`, 7 commits = 1 docs spec + 5 feat T1..T5 + 1 post-review NIT fix). FF в `feature/phase-8-1-substrate` после этого commit'а.

Phase 8.1 substrate (`WhereStoppedSnapshot` + `DerivedInsights.recentWhereStopped(limit:)` + `ProdWhereStoppedDeriver` поверх Track-1 D3 `where_stopped_log` M014) wired через `InsightsReader.refresh()` → `InsightsSnapshot.whereStopped` → `HomeContent(snapshot:)` → `WhereStoppedBlock(snapshot:)`. **`InsightsSnapshot`** extended обоими init signatures defaulted `whereStopped: WhereStoppedSnapshot? = nil` (mirror Track-7 `workState: WorkStateSummary? = nil` Optional-pattern, 5-я итерация defaulted-init дисциплины — P3 todayMetrics / P4 youNowState / P5 sameTaskTeammates / P6 inboxItems / P7 whereStopped). **`InsightsReader.refresh()`** прирос 19-м sequential SQL call `try insights.recentWhereStopped(limit: 1).first` между `inboxItems` fetch и `InsightsSnapshot` init — `.first` потому что substrate API возвращает `[WhereStoppedSnapshot]` (most-recent-first per `ProdWhereStoppedDeriver`), а master spec §4.5 требует singular "most recent stop-point". **`WhereStoppedBlock`** full rewrite (P2 placeholder → real, 27 → ~115 LOC): `@Environment(RouteCoordinator.self)` injection (matches `TodayBlock.swift:17` precedent — `RouteCoordinator` is `@Observable`, не `ObservableObject`); whole-card `Button` → `coordinator.pushHomeWorkState()` (Track-7 P3 `WorkStateDetailScreen` destination, no-arg `RouteCoordinator` method already shipped, `WorkStateRoute` already registered в `HomeView.navigationDestination(for:)`); empty branch keeps `LeafEmptyState(icon: LeafIcons.brand.leaf)` + updated copy "No recent stop-points captured." внутри `LeafCard(padding: .regular)`; populated branch — header `"WHERE YOU STOPPED · \(formatRelative(...))"` (`leafSectionLabel()` style, `text.tertiary`) + body `VStack { Text(excerpt) text.primary lineLimit(2) tail-truncated + optional Text(wipSignals.joined(separator: " · ")) text.tertiary }`. **`formatRelative`** — local 6-bucket ladder `now` / `Nm ago` / `Nh ago` / `yesterday` / `N days ago` / `MMM d` (cached static `en_US_POSIX DateFormatter` per P3 carry C-4 pattern; `max(0, ...)` clock-skew guard). **`hasUsableSnapshot`** defensive guard (post-NIT fix) — treats `snapshot != nil && !snapshot.excerpt.isEmpty` as populated; bare-non-nil snapshot с empty excerpt falls through to empty state + header drops age suffix (substrate today never emits empty excerpts; future producer regressions degrade gracefully). **`wipSignals`** filter trims whitespace-only entries before `.joined` (NIT-3 defense-in-depth). Animation `.easeInOut(duration: 0.25)` on `value: snapshot` для empty↔populated cross-fade (Equatable auto-synth holds). a11y: `.accessibilityElement(children: .combine)` + `.isButton` + label "Open work state details" + branching hint. **`HomeView`** — single call-site swap `WhereStoppedBlock()` → `WhereStoppedBlock(snapshot: snapshot.whereStopped)`, 1-line delta (line 216). **Substrate purity:** zero changes в `Packages/LeafCore/Sources/LeafCore/Home/WorkState/WhereStoppedSnapshot.swift` + `Packages/LeafCorePrivate/Sources/LeafCorePrivate/Prod/Detection/ProdWhereStoppedDeriver.swift`. **Zero new SQLCipher migrations / event_kinds / MCP tools / ShareEventTypeKey delta** (registry frozen 195 baseline post-Track-6, AC-13/14/15 verified empty `git diff feature/phase-8-1-substrate -- DB/ / Privacy/ShareEventTypeKey.swift / MCP/`). **Privacy walkback narrow grep AC-9** — 0 hits forbidden fields (`absolute_path|full_comment_body|raw_email|notes_body|email_subject|note_body|file_contents|raw_prompt|tool_input|tool_response|response_body`) в 4 Phase 8.7 file scope. **No sentinel-injection test added** — P7 reads only pre-walked `WhereStoppedSnapshot.excerpt` (ADR-010 walked в `ProdWhereStoppedDeriver` via `BodyExcerptCapPrivate`) + `wipSignals` enum-like markers + opaque IDs/timestamps. Pattern parity с P3/P4/P5 (no sentinel) vs P6 (INBOX titles carry untyped strings → sentinel needed). Tokens-only fidelity — `just check-tokens` 3-tier clean. **5/5 xcodebuild schemes green** (LeafCore / LeafCorePrivate / Leaf / LeafAgent / LeafMCP Debug build SUCCESS). **SPM tests:** **2778 XCTest + 45 Swift-Testing = 2823 total**, 0 failures, 4 skipped (+2 net new vs Phase 8.6 baseline 2821 — 2 InsightsSnapshotTests defaulted-nil + round-trip). HomeView.swift LOC 259 (≤280 budget — same as P6, single line delta nets ±0). Independent code review: **APPROVE-WITH-NITS** — NIT-1 (empty-state `frame(maxWidth:.infinity, alignment:.leading)` consistency tap-target → applied) / NIT-3 (excerpt-empty fallback + wipSignals whitespace-only filter → both applied). NIT-2 (`headerText` re-compute per body re-eval, defensible per snapshot static) → P9 polish, no apply. C-22 (`formatRelative` duplication w/ P5 `WithYouOnThisBlock`) carry to master spec §9.1.

Per-task substrates (5 atomic commits): **T1** `whereStopped` defaulted field + 2 round-trip tests (TDD red → green via 2 compile-error verification). **T2** `InsightsReader` 19th SQL call + thread-through. **T3** `WhereStoppedBlock(snapshot:)` shell + empty state + `HomeContent` call-site swap. **T4** populated body + header age + tap routing + animation + a11y. **T5** master spec §9.1 carry-overs C-20..C-21 append + final verification (all gates).

**Out of scope (P7 spec §1.1 hard exclusion):** Line 2 dedicated «last commit subject ≤ 4h ago» fetch (substrate gap — `recentLastCommit(maxAgeMs:)` LeafCorePrivate helper не существует Phase 8.1) — carry C-20 Track-9 / Phase 4.9; `anchorEventId → file path:line` resolution for mockup-style "WorkStateCard.swift:142" file ref (substrate gap — `events.payload_json` не retains file_path+line для AX/FSEvents kinds yet) — carry C-21 Track-9; WIP signals chip-styled rendering (text-only render P7) → P9 polish; Activity → Analytics rename (P8); HIG sweep / a11y audit / perf (P9); AI narrative (v1.1).

**Production reality:** `WhereStoppedSnapshot` substrate today читает M014 `where_stopped_log` rows которые populates `ProdWhereStoppedDeriver` (idle gate 30 min, WIP signal detection из `git_commit` payload subjects + `xcode_active_doc_changed` proximity). Может возвращать nil на fresh DB / idle gate not met / non-prod `StubInsights` (returns `[]` → `.first` = nil). После накопления stop-points в M014 — populated rows появятся автоматически без UI-change (substrate-driven, та же mechanism что и P3/P4/P5/P6).

Carry-overs appended to master spec §9.1 (`docs/superpowers/specs/2026-05-18-track-8-home-ux-design.md`): **C-20** Line 2 last-commit ≤4h dedicated fetch — substrate enrichment Track-9 / Phase 4.9. **C-21** `anchorEventId → file path:line` resolution + collector payload allow-list extension — substrate enrichment Track-9. **C-22** `formatRelative` duplication between `WhereStoppedBlock` + `WithYouOnThisBlock` — helper unification `HomeRelativeTimeFormatter` Track-9 / P9 polish.

Spec: `docs/superpowers/specs/2026-05-19-phase-8-7-where-stopped.md`. Plan: `.claude/plans/phase-8-7.md` (gitignored).

**2026-05-18 — Track-8 Phase 8.6 (INBOX block wire-up) landed** на `feature/phase-8-6-inbox` (off `feature/phase-8-1-substrate` `8a1903b5`, 13 commits = 2 docs spec + 10 feat T1..T10 + 1 post-review NIT fix). FF в `feature/phase-8-1-substrate` после этого commit'а.

Phase 8.1 substrate (`InboxItem` + `InboxKind` + `InboxSeverity` + `InboxFilter` + `DerivedInsights.inboxItems(filter:query:)` + `ProdInsights+InboxItems.swift` moat) wired через `InsightsReader.refresh()` → `InsightsSnapshot.inboxItems` → `HomeContent(snapshot:)` → `InboxBlock(items:)`. **`InsightsSnapshot`** extended обоими init signatures defaulted `inboxItems: [InboxItem] = []` — нулевой blast radius (4-я итерация defaulted-init дисциплины: P3 todayMetrics / P4 youNowState / P5 sameTaskTeammates / P6 inboxItems). **`InsightsReader.refresh()`** прирос 18-м sequential SQL call `try insights.inboxItems(filter: .all, query: "")` между `sameTaskTeammates` fetch и `InsightsSnapshot` init — `.all` / `""` defaults потому что filter + search живут view-side `@State` (no SQL re-fetch on keystroke; ≤100 items per Phase 8.1 14-day cutoff). **`InboxBlock`** full rewrite (P2 placeholder → real, 30 → ~80 LOC): `LeafInput` search field (default `.md` size, system magnifyingglass prefix) + `InboxFilterRow` 4 `LeafPill` chips (All / Reviews / Questions / Mentions, `.accent` selected / `.neutral` default) + `LeafDivider` + 3-state branch (empty-data "All clear." без CTA / no-match "No matches." + "Clear filters" CTA / `populatedBody` `ScrollView { LazyVStack { ForEach } }.frame(maxHeight: 320)`). **`InboxItemRow`** new file ~80 LOC: leading 8pt `LeafDot` (severity → `.danger`/`.warning`/`.muted` tone) + `VStack` (`titleLine` HStack `Text(item.title)` + conditional `(N)` aggregation suffix `text.tertiary` когда `aggregatedCount > 1` + `Text(item.sourceMeta)` `text.tertiary`) + whole-row `Button` tap → `NSWorkspace.shared.open(sourceURL)` когда non-nil + `.disabled(sourceURL == nil)` + a11y `.isButton` trait conditional. **`InboxFilterRow`** new file ~35 LOC: 4 `Button { LeafPill(tone: selected == chip.filter ? .accent : .neutral) }` + `.accessibilityAddTraits([.isButton, .isSelected])` on selected chip (post-review NIT — proper VoiceOver semantic trait вместо `accessibilityHint("Selected")`). **`HomeView`** — single call-site swap `InboxBlock()` → `InboxBlock(items: snapshot.inboxItems)`, 1-line delta. Animation `.easeInOut(duration: 0.25)` на `value: filteredItems` для row transitions при filter/search changes (computed property re-eval + Equatable `[InboxItem]` triggers correctly per P5 precedent). **Substrate purity:** zero changes в `LeafCore/Insights/InboxItem.swift` + `LeafCorePrivate/Prod/Insights/ProdInsights+InboxItems.swift`. **Zero new SQLCipher migrations / event_kinds / MCP tools / ShareEventTypeKey delta** (registry frozen 195 baseline). **Privacy walkback AC-14** — 0 hits forbidden fields в 3 P6 view files. **Sentinel-injection test** `testEventBodyDoesNotLeakIntoPresenceState_INBOX` в `RelayBodyLeakageTests` — first Track-8 sentinel (P3/P4/P5 needed none — derived metrics / state enums / opaque IDs only). Pattern parity with `_Linear`/`_GitHub`/`_Slack` peers (`writeEventsOffsetAndPresence` + `presence_state.state_json` inspection + padded sentinel `LEAKED_SENTINEL_INBOX_BODY`). Defense-in-depth — INBOX is local-render-only; test locks invariant "events.body and presence_state.state are disjoint surfaces". Tokens-only fidelity — `just check-tokens` 3-tier clean. **5/5 xcodebuild schemes green** (LeafCore / LeafCorePrivate / Leaf / LeafAgent / LeafMCP Debug build SUCCESS). **SPM tests:** **2776 XCTest + 45 Swift-Testing = 2821 total**, 0 failures, 4 skipped (+3 net new vs Phase 8.5 baseline 2818 — 2 InsightsSnapshotTests defaulted-init + roundtrip + 1 sentinel). Independent code review: **APPROVE-WITH-NITS** — all 17 AC pass, 2 inline fixes applied (header comment T8 marker dropped + `.isSelected` a11y trait swap), 3 carries to master spec §9.1 (C-17 filteredItems substring unit test missing → P9 / C-18 empty/no-match same icon → P9 / C-19 severityWord hardcoded English → Track-9 localization). Reviewer accepted vacuous-but-parity-correct sentinel test per master spec §7 mandate.

Per-task substrates (10 atomic commits): **T1** `inboxItems` defaulted field + 2 round-trip tests (TDD red → green). **T2** `InsightsReader` 18th SQL call + assignment. **T3** `InboxBlock(items:)` shell + empty-data state + `HomeContent` call-site swap. **T4** `InboxFilterRow` + `@State selectedFilter` + computed `filteredItems`. **T5** `LeafInput` search field + extend `filteredItems` query match. **T6** `InboxItemRow` shell + scrollable list (320pt cap). **T7** row tap + `NSWorkspace.open` + disabled gating. **T8** inline `(N)` aggregation count suffix. **T9** no-match state + Clear filters CTA + sentinel-injection test. **T10** animation + master spec §9.1 carry-overs C-14..C-16 append + final verification.

**Out of scope (P6 spec §1 hard exclusion):** substrate enrichment of `sourceURL` for Layer B (Phase 4.8 Linear / Phase 4.9 GitHub), per-keystroke SQL re-fetch (P6 uses snapshot + view-side in-memory filter), background re-fetch on filter change, WHERE STOPPED wire-up (P7), Activity → Analytics rename (P8), polish/a11y/perf sweep (P9), `RouteCoordinator.openURL` extraction (Track-9 candidate if other blocks adopt external URL navigation).

**Production reality:** `inboxItems` likely returns `[]` сегодня — Layer B (Linear/GitHub/Slack mention sources Phase 4.8/4.9) ещё не lit up; D3 detection tables (`open_questions` / `blockers`) могут содержать entries но `sourceURL` всегда nil (carry C-16 — substrate gap, D3 tables не preserve originating URL). Block рендерит empty-data state "All clear." до того как substrate начнёт отдавать items. После lit-up — populated rows появятся автоматически без UI-change (substrate-driven, та же mechanism что и P3/P4/P5).

Carry-overs appended to master spec §9.1 (`docs/superpowers/specs/2026-05-18-track-8-home-ux-design.md`): **C-14** search debounce / SQL re-fetch когда cardinality > 1000 → Track-9 / Phase 4.9. **C-15** `RouteCoordinator.openURL(_:URL)` extraction если P7 WHERE STOPPED тоже opens external URLs → Track-9. **C-16** `InboxItem.sourceURL` nil для D3-derived items → substrate enrichment Track-9 / Phase 4.8/4.9. **C-17** `filteredItems` substring-match unit test missing → P9 polish. **C-18** empty/no-match states share `LeafIcons.brand.leaf` → P9 polish (semantic visual differentiation). **C-19** `severityWord` hardcoded English → Track-9 localization track.

Spec: `docs/superpowers/specs/2026-05-18-phase-8-6-inbox.md`. Plan: `.claude/plans/phase-8-6.md` (gitignored).

**2026-05-18 — Track-8 Phase 8.5 (WITH YOU ON THIS block wire-up) landed** на `feature/phase-8-5-with-you` (off `feature/phase-8-1-substrate` `ff09a3aa`, 8 commits incl. spec + post-review fix). FF в `feature/phase-8-1-substrate` после этого commit'а.

Phase 8.1 substrate (`TeammateMatch` + `MatchConfidence` + `SameTaskMatcher` + `TaskIdentity` + `TeammatePresenceReader` (stub) + `DerivedInsights.sameTaskTeammates(rule:)` + `ProdInsights+SameTaskTeammates.swift` + `ProdInsights+CurrentTaskIdentity.swift`) wired через `InsightsReader.refresh()` → `InsightsSnapshot.sameTaskTeammates` → `HomeContent(snapshot:)` → `WithYouOnThisBlock(matches:)`. **`InsightsSnapshot`** extended обоими init signatures defaulted `sameTaskTeammates: [TeammateMatch] = []` — нулевой blast radius (mirror P3 todayMetrics / P4 youNowState — 3-я итерация defaulted-init дисциплины). **`InsightsReader.refresh()`** прирос 17-м sequential SQL call `try insights.sameTaskTeammates(rule: .hierarchical)` между `youNowState` fetch и `InsightsSnapshot` init. **`WithYouOnThisBlock`** full rewrite (P2 placeholder → real, 29 → 195 LOC): empty state branch (`LeafEmptyState` + "→ Team" CTA, `windowState.section = .team`), `populatedBody` `ForEach(matches.prefix(5), id: \.memberID)` под sort'нутый `SameTaskMatcher` output (confidence asc → lastActivityAtMs desc → displayName asc), `teammateRow` (initials-circle avatar + displayName + "app · Nm ago" + tinted confidence badge) — whole row tappable `Button` routing к Team tab + `accessibilityAddTraits(.isButton)`. Confidence tint mapping: `onSameLinearIssue + onSameBranch → status.success` (green), `onAdjacentBranch → status.warning` (amber) per master spec §4.3 HIGH/MEDIUM. Overflow footer "→ +N more on this task" когда `matches.count > 5`. Animation `easeInOut(duration: 0.25)` на `value: matches` для row transitions. **Avatar tint palette** deliberate exclude `status.warning + status.success` (review NIT-1 fix) — иначе hash-deterministic palette pick мог рандомно совпасть с amber/green badge tint → false semantic pairing; swap'нуты на `accent.emphasis + status.danger`. **`HomeView`** — single call-site swap `WithYouOnThisBlock()` → `WithYouOnThisBlock(matches: snapshot.sameTaskTeammates)`, 1-line delta. **Substrate purity:** zero changes в `LeafCore/Insights/SameTaskMatcher / TeammateMatch / TaskIdentity / TeammatePresenceReader`. **Zero new SQLCipher migrations / event_kinds / MCP tools / ShareEventTypeKey delta** (registry frozen 195 baseline). **Privacy walkback AC-11** — 0 hits forbidden fields в `WithYouOnThisBlock.swift`. No new privacy surface — все `TeammateMatch` поля (memberID opaque / displayName broadcast / currentApp Share-Controls-filtered / contextLabel derived / confidence enum / lastActivityAtMs timestamp) уже в payload pre-P5. Tokens-only fidelity — `just check-tokens` 3-tier clean. **5/5 xcodebuild schemes green**. **SPM tests:** **2773 XCTest + 45 Swift-Testing = 2818 total**, 0 failures, 4 skipped (+2 net new vs Phase 8.4 baseline — InsightsSnapshotTests defaulted-init + roundtrip). Independent code review: **APPROVE-WITH-NITS** — NIT-1 (palette collision → applied), NIT-3 (formatRelative signature divergence → carry), NIT-4 (hash randomization → documented carry), MINOR-1 (group accessibility hint → P9 sweep), MINOR-3 (plan xcworkspace path → fixed gitignored plan).

Per-task substrates (6 atomic commits): **T1** `sameTaskTeammates` defaulted field + 2 round-trip tests (TDD red → green). **T2** `InsightsReader` fetch + assignment. **T3** `WithYouOnThisBlock(matches:)` shell + empty-state + `HomeContent` call-site swap. **T4** teammate row + helpers (avatarCircle / confidenceBadge / secondLine / confidenceTint / avatarTint / initials / formatRelative) + 5-row cap. **T5** overflow footer "+N more". **T6** master spec §9.1 carry-overs append + final verification.

**Out of scope (P5 spec §1 hard exclusion):** offline/stale footer (P9/Phase 5.6 carry), "N active elsewhere" count в empty CTA (Phase 5.4 carry), per-teammate detail screen + `RouteCoordinator.pushTeam(memberID:)` (Track-9), `TeammateMatch.durationSec` substrate hardcode (Phase 5.4/Track-9). INBOX / WHERE STOPPED / Analytics rename / polish — P6/P7/P8/P9.

**Production reality:** `TeammatePresenceReader` остаётся `StubTeammatePresenceReader → []` до Phase 5.4 light-up (DB-backed reader против `presence_history` — table ещё не migrated). Block в проде сегодня всегда рендерит empty state с "→ Team" CTA. После Phase 5.4 — populated rows появятся автоматически без UI-change (substrate-driven).

Carry-overs added to master spec §9.1 (`docs/superpowers/specs/2026-05-18-track-8-home-ux-design.md`): **C-10** empty-state N count → Phase 5.4. **C-11** offline footer → Phase 5.6. **C-12** row tap без per-teammate selection → Track-9. **C-13** `TeammateMatch.durationSec` hardcoded 0 → Phase 5.4/Track-9.

Spec: `docs/superpowers/specs/2026-05-18-phase-8-5-with-you.md`. Plan: `.claude/plans/phase-8-5.md` (gitignored).

**2026-05-18 — Track-8 Phase 8.4 (YOU·NOW block wire-up) landed** на `feature/phase-8-4-you-now` (off `feature/phase-8-1-substrate` `e93b97fd`, 7 commits). FF в `feature/phase-8-1-substrate` после этого commit'а.

Phase 8.1 substrate (`YouNowState` enum + 4 sub-structs + `DerivedInsights.youNowState(now:)` + `ProdInsights+YouNowState.swift`, with `YouNowStateDeriver` priority resolution `inMeeting > deepWorkFocus > away(.screenLocked) > away(.idle) > active > away(degenerate)`) wired through `InsightsReader.refresh()` → `InsightsSnapshot.youNowState` → `HomeContent(snapshot:)` → `YouNowBlock(state:)`. **Substrate touch-up:** add `lastAppBundleID: String?` field to `YouNowAway` so the Resume CTA can gate against `LocalAppsStore.isEnabled(_:)` — `YouNowStateDeriver` carries `inputs.frontmostBundleID` through all 3 away branches (screenLocked / idle / degenerate). New `YouNowState.empty` static unifies the cold/no-signal default; `StubInsights.youNowState` collapses to `return .empty`. **`YouNowBlock`** full rewrite (P2 placeholder → real, 28 → 259 LOC): 4-case switch (active / inMeeting / deepWorkFocus / away) with tone-tinted leading `LeafIconChip` + 3-line content layout (title in tone, support text in secondary, footer in tertiary + intensity bars on `.active`). Master spec §4.2 shorthand resolved to actual tokens: `accent.primary` (active green) / `status.info` (meeting blue) / `status.warning` (focus amber) / `text.tertiary` (away grey). Per-reason away icons (`lock.fill` / `moon.zzz.fill` / `powersleep`). **Resume CTA** на `.away` рендерится только при 4-AND условиях: `lastAppBundleID` non-nil + `LocalAppsStore.isEnabled(bundleID)` + `lastLinearID` non-nil + `idleSec ≤ 86_400` (24h). Click routes via `NSWorkspace.shared.urlForApplication(withBundleIdentifier:) → .open(url)`. Card tap gate (`YouNowTapModifier`) confines `.contentShape(Rectangle()) + .onTapGesture` к `.away` only — non-away states intentionally inert per spec §3.4 (cursor signal avoided для dead-tap). Cross-fade transition 250ms easeInOut на `value: state`. `LocalAppsStore` consumed как `@StateObject` (UserDefaults-backed shared suite — instance-agnostic reads, reactivity gap carry-forward C-5). **`InsightsSnapshot`** extended обоими init signatures defaulted `youNowState: YouNowState = .empty` — нулевой blast radius на 23 fixture-сайта (mirror P3 todayMetrics pattern). **Zero new SQLCipher migrations / event_kinds / MCP tools / ShareEventTypeKey delta** (registry frozen на 195 post-Track-6, AC-19 verified empty `git diff … DB/`). **Privacy walkback grep AC-13** — 0 hits forbidden fields (`absolute_path|full_comment_body|raw_email|notes_body|prompt|tool_input|response_body|email_subject|note_body`) в Phase 8.4 file scope. Tokens-only fidelity — `just check-tokens` 3-tier clean (intensity bars dropped from `RoundedRectangle(cornerRadius: 1)` to plain `Rectangle()` — LeafRadius.sm = 6pt too large для 3×8pt bar). **5/5 xcodebuild schemes green** (LeafCore / LeafCorePrivate / Leaf / LeafAgent / LeafMCP Debug build SUCCESS). **SPM tests:** **2771 XCTest + 45 Swift-Testing = 2816 total**, 0 failures, 4 skipped (+5 net new vs Phase 8.3 baseline — 3 deriver bundleID round-trip + 1 InsightsSnapshot defaulted init + 1 YouNowState.empty). HomeView.swift LOC 259 (≤280, AC-18 ✓); YouNowBlock.swift 259 LOC. Independent code review: **APPROVE-WITH-NITS** — IMP-2 (dead-tap UX → gated via `YouNowTapModifier`) / IMP-3 (defensive fallback after guard → semantic fallback chain `lastApp ?? lastAppBundleID ?? "app"`) / MIN-1 (dead `.transition(.opacity)` dropped) / MIN-5 (intensity-bars `accessibilityElement(children: .ignore)`) — все applied. IMP-1 (LocalAppsStore reactivity) carry-forward as C-5.

Per-task substrates: **T1** YouNowAway field + .empty static + StubInsights collapse + deriver wire (TDD red → green). **T2** Deriver bundleID round-trip tests (3 cases). **T3** InsightsSnapshot defaulted field. **T4** InsightsReader fetch + assignment. **T5+T6** HomeView call-site + YouNowBlock body atomic. **T7+T8** review polish + token fix.

**Out of scope (P4 spec §1.1 hard exclusion):** WITH YOU ON THIS / INBOX / WHERE STOPPED wire-up (P5-P7), Activity → Analytics rename (P8), polish / accessibility audit / performance (P9), AI narrative (v1.1). Substrate-gap deferrals (carry §9.1): C-5 LocalAppsStore reactivity → app-root `.environmentObject` injection. C-6 inMeeting tap → Calendar `ical://` deep-link. C-7 `YouNowBlock` accessibility Button wrap on `.away`. C-8 Resume CTA branch-deletion staleness. C-9 substrate enrichment for `meetingTitle` / `meetingEndsAtMs` / `meetingSource .both`.

Spec: `docs/superpowers/specs/2026-05-18-phase-8-4-you-now.md`. Plan: `.claude/plans/phase-8-4.md`.

**Post-ship enrichment (2026-05-18 evening manual smoke):** Phase 8.4 spec mockup §3 предполагал rich YOU·NOW row для `.active` — `Xcode · HomeView.swift / feature/track-8-home · LEAF-204 / 42 min focused · intensity ▮▮▮▯`. После live smoke на собственном Mac (AX TCC прошёл нетривиальный fight — см. ниже) реальное substrate output для Xcode foreground: `Xcode · Leaf — BrowserAllowListStore.swift · Started 9:58 PM · 44s` — **первая строка contextLabel ✅ работает** (window_title через AX), но (a) **branch + linearID empty** для всех apps (`extractBranchToken` в `currentTaskIdentity()` ищет в window_title `feature/...` token, Xcode title branch не содержит), (b) **intensity ▮▮▮▯ empty** (Track-4 S3 IntensityAggregator default OFF — требует Input Monitoring TCC dual-prompt), (c) format `Started HH:MM · Xm` instead of `X min focused` (UI choice). Все три — substrate / settings / design choices вне Phase 8.4 contract. **Перенесено целиком в новый Track-9** (substrate enrichment for YOU·NOW depth — separate phase, отдельный spec через `superpowers:brainstorming` когда стартанём). Phase 8.4 scope (UI surface wire-up + первая строка contextLabel) ✅ закрыт полностью; Track-9 owner — "1-в-1 как mockup §3".

**Substrate touch-up landed (cross-Phase relevant):** `9d22c9af feat(agent): prompt for Accessibility trust on first launch` — без этого Agent silently `ax=false` навечно. `AXIsProcessTrustedWithOptions([kAXTrustedCheckOptionPrompt:true])` на startup spawns standard system dialog первый раз → TCC auto-creates entry с current CDHash → user toggles ON. Production-relevant (не только dev): первые юзеры signed DMG также теряли AX без этого fix.

**Dev workflow gotcha (saved to memory `feedback_leaf_launch_workflow.md`):** живёт связка из 3 issues для Debug build UX smoke — (1) `/Applications/Leaf.app` prod alpha hijacks SMAppService respawn over DerivedData Debug via LaunchServices priority; (2) TCC CDHash drift per Xcode rebuild → stale Accessibility entries silently denied; (3) before `9d22c9af` no first-launch AX prompt. Recipe для clean Debug smoke — kill prod agent / lsregister Debug / `tccutil reset Accessibility tech.gundem.leaf` / launch via `open` DerivedData path / verify lsof. Permanent dev fix (DevEx backlog): либо `mv /Applications/Leaf.app /Applications/Leaf-prod-backup.app`, либо separate Debug bundle ID `tech.gundem.leaf.dev` via xcconfig.

**2026-05-18 — Track-8 Phase 8.3 (TODAY block wire-up) landed** на `feature/phase-8-3-today-block` (off `feature/phase-8-1-substrate` `baa2e4f0`, 4 commits). FF в `feature/phase-8-1-substrate` после этого commit'а.

Phase 8.1 substrate (`TodayMetrics` + `SurfacePill` + `DerivedInsights.todayMetrics(now:)` + ProdInsights+TodayMetrics.swift, 4 integration tests) wired through `InsightsReader.refresh()` → `InsightsSnapshot.todayMetrics` → `HomeContent(snapshot:)` → `TodayBlock(metrics:)`. **`SurfacePillRouter`** (LeafCore) — public enum `SurfacePillRoute` + `route(forPillID:)` маппит `SurfacePill.id` строки (camelCase, эмитируемые Phase 8.1 `entry.surface.id` для capture surfaces / `"linear"`/`"github"`/`"slack"` для Layer B) в push-target. Capture-surface pills routed через `RouteCoordinator.pushHome(_:)`, Layer B — через `pushHomeLayerBProvider(_:)`. **`TodayBlock`** rewrite (P2 placeholder → real): 5 `LeafMetricAmbient` cells (focused / AI ratio / sessions / switches / commits) + `LeafDivider` + `LeafPill` strip с `+N` overflow expand-inline (`@State pillsExpanded`, no modal/sheet). Empty state — `LeafEmptyState` inside same `LeafCard` когда `metrics == .empty`. **Zero new SQLCipher tables / migrations / event_kinds / ShareEventTypeKey delta** (registry frozen, AC-10 verified via empty `git diff … DB/`). **Privacy walkback grep AC-12** — 0 hits forbidden fields (`absolute_path|full_comment_body|raw_email|notes_body`) в Phase 8.3 file scope. Tokens-only fidelity — `just check-tokens` 3-tier clean. `InsightsSnapshot` extended обоими init signatures defaulted `todayMetrics: TodayMetrics = .empty` — нулевой blast radius на 23 fixture-сайта в `LeafCoreTests/InsightsSnapshotTests.swift`. **5/5 xcodebuild schemes green** (LeafCore / LeafCorePrivate / Leaf / LeafAgent / LeafMCP Debug build SUCCESS). **SPM tests:** **2766 XCTest + 45 Swift-Testing = 2811 total**, 0 failures, 4 skipped (+4 net new vs Phase 8.2 baseline — SurfacePillRouterTests). HomeView.swift LOC 259 (≤280, AC-13 ✓). Independent code review: **APPROVE-WITH-NITS** (все NIT'ы либо ложные позитивы, либо deferred per spec OQ/§10).

Per-task substrates: **T1** SurfacePillRouter + 4 tests, TDD red→green. **T2** Snapshot field + reader fetch (defaulted, no fixture sweep). **T3** HomeContent snapshot param. **T4** TodayBlock body rewrite.

**Out of scope (P3 spec §1 hard exclusion):** YOU·NOW / WITH YOU ON THIS / INBOX / WHERE STOPPED wire-up (P4-P7), Activity → Analytics rename (P8), polish / accessibility / performance / narrow-window `ViewThatFits` (P9), AI narrative (v1.1).

**Carry-forward known limitation:** Spec §6 + MS-5 предполагали `InsightsReader.State.error` несёт last-known snapshot ("error banner above metrics, card still renders with last-known values"). Текущий `InsightsReader.State.error(message: String)` snapshot не сохраняет — это spec inaccuracy, требует reader state-machine refactor вне P3 scope. Carry в P9 polish или Track-8 wrap. До тех пор `.error` state рендерится top-level full-page banner в HomeView (existing P2 behavior).

Spec: `docs/superpowers/specs/2026-05-18-phase-8-3-today-block.md`. Plan: `.claude/plans/phase-8-3.md`.

**2026-05-17 — Track-7 (Home dashboard surface) landed** на `main` (collective merge P1 + P2-collapsed + P3 + P4 + P5 polish через `feature/track-7-integration`).

Полный Native UI dashboard поверх alpha.16 capture depth: **9 surface drill-down cards** (6 Track-6 capture: Claude Code / Xcode / IDEs / Browsers / Zoom / Calendar; 3 Layer B: Linear / GitHub / Slack) + **1 Work State card** (Track-1 D3 detection surface). Главные новые элементы: `SurfaceCard` / `SurfaceRow` / `SurfaceDetailLayout` primitives, `HomeSurface` enum + `SurfaceCatalog` static array, `LayerBProvider` enum, `WorkStateRoute` + `WorkStateSummary`, `RouteCoordinator` с тремя `pushHome*` методами + 3 `navigationDestination(for:)` registrations в HomeView, `LivePresenceWidget` tappable columns с chevron в title HStack, `filesTouched` inline в Today section, `AppRoute.settings(section:sub:)` deep-link plumbing.

**Per-phase substrates.**
- **P1 (Foundation + Claude Code)** — primitives + ClaudeCodeSurfaceCardViewModel + ClaudeCodeDetailScreen template.
- **P2-collapsed (Capture surfaces)** — 5 surfaces (Xcode / IDEs / Browsers / Zoom / Calendar) на template byte-for-byte. **GoogleCalendarDetailScreen** = LeafEmptyState placeholder (carve-out до GCP gate clears). `ConnectionsView:199` waiver resolved through BrandGoogleBlue asset extraction. 2 stateless helpers (`URLDomainExtractor`, `IDEFamilyClassifier`).
- **P3 (Work State)** — Track-1 D3 detection promotion. 9 LeafCore types (`Decision` / `OpenQuestion` / `Blocker` / `WhereStoppedSnapshot` / `WorkStateSummary` + 4 enums). `WorkStateCard` standalone composite (НЕ `SurfaceCard` — Work State не `HomeSurface`). `WorkStateDetailScreen` second `LeafTab` (Decisions / Questions / Blockers / Where Stopped) bypasses `SurfaceDetailLayout` (categorical data + sub-tab structure incompatible с chartBlock).
- **P4 (Layer B drill-downs)** — LivePresenceWidget tappable per column. `LayerBProvider` enum. 3 detail screens reusing SurfaceDetailLayout (Linear / GitHub / Slack). GitHub + Slack sticky reauth banners outside ScrollView. `ReviewActivityResult` typed decoder.
- **P5 (Polish)** — `ReauthBannerKeys` hoist (~40 LOC, single source of truth для GitHub/Slack dismiss keys; 3 sites refactored). HIG sweep verified baseline (all touch targets имеют explicit accessibilityLabel + accessibilityAddTraits — SurfaceCard / SurfaceRow / SurfaceCardCompact / WorkStateCard / LivePresenceWidget columns). OQ-T7-9 chevron amendment (always-visible accepted — P4 de facto reality supersedes hover-only resolution; trackpad-only users + iOS-conventional discoverability).

**Substrate-only invariant verified:** zero new event_kinds / migrations / MCP tools / schema columns / ShareEventTypeKey delta (registry frozen в P5). Track-7 = pure UI surface поверх alpha.16 capture substrate.

**Tests:** SPM **2695 XCTest + 45 Swift-Testing = 2740 total**, 0 failures, 4 skipped (P4 baseline 2563 → +132 cumulative от P2-collapsed/P3/P4 plus P5 polish). **5/5 xcodebuild schemes** (LeafCore / LeafCorePrivate / Leaf / LeafAgent / LeafMCP) green. **`just check-tokens`** 3-tier clean. **Privacy walkback grep AC-5** — 0 hits forbidden fields в Track-7 file scope (Leaf/Views/Window/Home/ + Leaf/Views/Window/SurfaceDetail/ + Leaf/Models/LayerB/ + Leaf/Models/WorkState*.swift + Packages/LeafCore/Sources/LeafCore/Home/).

**Relaxed acceptance:** AC-13 Localizable.strings — English hardcoded ships as known limitation (post-Track-7 localization track owns extraction). AC-14 Performance — code-review level via Stage 6 subagent (Instruments deferred; no observed jank).

**Deferred to v1.1** (per P5 spec §10 D-3): headline trend `<50% expected days` rule; real daily sparkline data в chart slots (LeafCorePrivate SQL helpers); clickable linked PRs → Linear issue drill-down; `ReviewActivityPeriod.last30Days`; clickable contextRef → LayerB drill-down; real-time D3 detector tick badge; sub-tab + range state restoration; empty-state Whitepaper deep-links; LeafCorePrivate Phase E Prod SQL helpers (real numbers light up).

Master spec: `docs/superpowers/specs/2026-05-17-track-7-ui-surface-polish-design.md`. Per-phase specs P1-P4 + P5 spec в `docs/superpowers/specs/`. Tactical plans gitignored.

**2026-05-17 — Phase 1 Code Style Foundation landed** на `feature/code-style-foundation` (off `main` `60bf38e9`, PR [#12](https://github.com/gundemtech/leaf/pull/12) draft).

Параллельный refactor track — **не блокирует** Track-7. Apple `swift-format` 6.x + SwiftLint 0.63.2 + hybrid baseline strategy (strict pool empty + aspirational pool grandfathered). **`STYLE.md`** + **`CONTRIBUTING.md`** at root документируют existing codebase conventions (PascalCase types, factory-enum DI, `@MainActor @Observable` ViewModels, XCTest-dominant tests, ADR-010 walkback discipline). **`.swiftlint.yml`** — 41 opt-in rules с inline rationale per rule (strict/aspirat. + count), 22 disabled rules (swift-format owner overlap + project exceptions). **`.swiftlint-baseline.json`** — 603 grandfathered entries. **`.editorconfig`** baseline (4-space Swift / 2-space yml / tab justfile). **`.github/workflows/code-style.yml`** на macos-26 — `--reporter github-actions-logging` → PR annotations + step summary; `continue-on-error: true` на lint/fmt steps (Phase 1 report-only). **`justfile`** +5 recipes (`lint` / `lint-fix` / `format` / `format-check` / `check-style`). **Zero `.swift` changes** per контракт. **CI smoke PR #12 green** в 28s. **Все 5 xcodebuild schemes Debug build SUCCESS**. **swift-format reports ~1338 stylistic violations** — формат-roadmap для Phase 2 cleanup PR. Spec: `docs/superpowers/specs/2026-05-17-phase-1-code-style-foundation.md`.

**2026-05-16 — Track-6 P1 (Claude Code Deep) landed** на `feature/track-6-P1-claude-code-deep` (off main `9b2a53b`).

Claude Code surface поднят с 2 generic event_kinds (`tool_use` / `user_prompt`) до **16 specific** (2 retroactive + 14 net-new `claude_*`). **Subagent capture first-time enabled** — collector скипал `<parent>/subagents/agent-*.jsonl` через 1-level glob; теперь 2-level narrow walk дискаверит subagent transcripts (Task 6), парсер инжектит `agent_id` из path + `agent_type` + `description` через cached `ClaudeCodeSubagentMetaReader` (200-char description cap, ADR-010 walkback). **Hybrid capture pipeline (D1):** jsonl floor (always-on, no TCC) + opt-in hook bridge ceiling. **`leaf-hook-bridge`** thin SwiftPM executable (~117KB unstripped, ~5ms cold start) reads JSON stdin → forwards line-delimited envelope `{event_name, payload_json, ts_ms}` через POSIX unix-domain socket в `~/Library/Application Support/Leaf/hooks.sock`. **`ClaudeCodeHookSocketListener`** в Agent (POSIX socket + DispatchSource — NWListener unix-domain rejected per known instabilities) парсит line-by-line; **`ClaudeCodeHookParser`** маппит 5 hook flavours (PreToolUse/PostToolUse/UserPromptSubmit/SessionEnd/PreCompact) на claude_* event_kinds. **`tool_use_id` LRU dedup** (cap 2048, half-trim eviction) — hook arrives first (sub-50ms p99); jsonl filter дропает matching tool_use_ids на следующем tick (hook wins per spec §2.3 — strictly richer payload via `duration_ms` + `permission_mode`). **`claude_tokens_used`** жёстко jsonl-only (Anthropic hooks не expose tokens per #11008/#11535) — per-turn shape `{model, input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens, service_tier}`. **M024** partial expression index `idx_events_ai_subagent` на `events(json_extract(payload_json, '$.agent_id'))` WHERE `signal_type='aiCollaboration'` — substrate под Phase 4.9 subagent rollup queries. Numbering: M019-M023 reserved Track-5. **`AIToolsStore`** @Observable UserDefaults (`leaf.aitools.*` namespace) + **`AIToolsHookInstaller`** (POSIX flock + atomic temp+rename, idempotent install/uninstall, preserves user's foreign hooks). **Settings → AI Tools** новая section (Claude Code row: master toggle + token sub-toggle + "Why this is safe" disclosure + install status badge). **Onboarding step `aiTools`** между `observers` и `team` ("Install Claude Code hooks" → installer.install → advance / "Skip — use jsonl fallback" → advance без install). **ADR-010 walkbacks per kind** — parser allowlist strips forbidden fields (`command`/`tool_input`/`tool_response`/`content`/`thinking`/`signature`/`iterations`/`prompt`/`url`/`old_string`/`new_string`/etc) before RawEvent emission; `RelayBodyLeakageTests.testEventBodyDoesNotLeakIntoPresenceState_ClaudeCode` (public) + `ClaudeCodeCollectorCrossHookTests.testParserDoesNotLeakForbiddenFieldsAcrossAllClaudeKinds` (moat) sentinel-inject `LEAKED_SENTINEL_CLAUDE_P1` в forbidden positions across all 11 visible kinds. **DispatchCoverageTests** +3 parity fences. **EventKindIcon** +14 SF Symbol mappings. **152 → 168 ShareEventTypeKey** entries (all 16 default OFF per ADR-020 / contract §2.5). **29 SQLCipher tables** (M001-M018 + M024). **2099 SPM tests** (+87 net new vs 2012 baseline). **3/3 xcodebuild schemes green**. Tactical plan: `docs/superpowers/plans/2026-05-15-track-6-P1-claude-code-deep.md` (gitignored). Spec: `docs/superpowers/specs/2026-05-15-track-6-P1-claude-code-deep.md`.

**2026-05-16 — Track-6 P3 (Browsers Deep) landed** на `feature/track-6-P3-browsers-deep` (off origin/main + 4 Stage 0-3 docs).

Safari + Chrome + Arc capture-depth-parity с Track-3 D1/D2/D3 — per-tab navigation + per-tab activation + per-browser bookmark count delta, all gated behind opt-in per-domain allow-list. 8 net-new event_kinds (168 → **176** ShareEventTypeKey post-P1, default OFF). **AppleScript-only mechanism** (no SQLite History.db watcher per research §6 D1 — avoids FDA cliff on Safari + locking pain on Chrome). **6 new pure state machines** в LeafCore/OS: `Safari/Chrome/ArcNavStateMachine` (per-tab URL diff: Safari positional `i<N>` key, Chrome/Arc stable `tab.id`) + `Safari/Chrome/ArcActiveStateMachine` (front-window active diff). **M026 `browser_domain_allow` table** (30 SQLCipher tables M001-M018+M024+M026; M019-M023 reserved Track-5, M025 reserved). **B2+D1 filter pipeline:** adapter wrapper applies `DomainAllowListReader.granularity` to URLs BEFORE state machines see them — default `domain_only` for non-allow-listed → `path_stripped` → `full_url` per per-domain rule. **`BrowserBookmarksWatcher` actor**: own FSEventStream per browser, Chrome multi-profile via `~/Library/Application Support/Google/Chrome/**/Bookmarks` (no FDA), Safari FDA-gated via `~/Library/Safari/Bookmarks.plist`. **`BrowserBookmarksFeatureGate` protocol** — toggle gating через `LocalAppsStore.{chromeEnabled,safariEnabled}` (default OFF). **Allow-list IPC invalidation** через `DistributedNotificationCenter`. **Settings UI:** Privacy → Browser Allow-list + System Observers → 2 bookmark toggle rows + Safari FDA deep-link banner. **Moat в LeafCorePrivate (gitignored):** `Prod{Safari,Chrome,Arc}Adapter.swift` + `ProdAppleScriptAdapterRegistry` + `BrowserBookmarkCountDeriver` + `DBDomainAllowListReader`. **1467 → 1470 SPM tests** baseline P3 (integration combined: ~2099 + ~80 P3 net new — actual count locked после full test run on integration branch). **3/3 xcodebuild schemes green** (Leaf / LeafAgent / LeafMCP). **Independent code review** APPROVE после fix-bundle. Tactical: `docs/superpowers/plans/2026-05-16-track-6-P3-browsers-deep.md`.

**2026-05-16 — Track-6 P4 (Google Calendar Deep) landed** на `feature/track-6-P4-google-calendar` (off `main` `9b2a53b`).

First Layer B Google API provider — расширяет EventKit S1 ceiling до native Google Calendar v3 API через PKCE-loopback OAuth + 5-min `events.list` syncToken polling. **6 new event_kinds**: 1 omnibus `google_calendar_event_observed` + 5 transition-bearing (focus_block start/end pair + ooo start/end pair + working_location_changed). **M027 google_calendar_typed_event_tracker** — 12-column tracker для clock-driven transitions. **syncToken cursors** в `provider_snapshots` (M015 reused). **PKCE.swift + LoopbackCallbackListener.swift reused** от Linear. **42 RelayBodyLeakageTests walkbacks** (7 fields × 6 kinds — decline_message / building_id / attendee_email / description / conference_uri / location / creator_email). **ShareEventTypeKey 176 → 182 post-integration** (152 baseline + 16 P1 + 8 P3 + 6 P4, default OFF). **presence_state.google_calendar composite write**. **Settings → Connections → Google Calendar row**. **LeafAgent collector wiring** + 2 Info.plist OAuth keys (empty defaults → silent skip). **5/5 xcodebuild schemes green**. **γ Hybrid reconciliation с EventKit S1** — double-emission acceptable до Phase 4.9 cross-source dedupe. Acceptance blocked on GCP setup per spec §7.6 — `leaf-prod` GCP project + brand + sensitive scope verification (2-6 wk wall pre-approval, 100-user cap). Spec: `docs/superpowers/specs/2026-05-16-track-6-P4-google-calendar.md`.

**2026-05-16 — Track-6 P2 (Xcode Deep) landed** на `feature/track-6-P2-xcode-deep` (off `main` `9b2a53b`).

Track-4 S2 substrate (60s AppleScript poll эмитящий `xcode_active_doc_changed` + `xcode_build_state_changed`) расширен **6 новыми event_kind**: `xcode_build_started/_finished` (AppleScript transitions + xcresulttool enrichment for error/warning counts) / `xcode_test_run_started/_finished` (FSEvents на `~/Library/Developer/Xcode/DerivedData/*/Logs/{Test,Launch}/` + `xcrun xcresulttool get *-results summary --legacy`) / `xcode_scheme_changed` / `xcode_run_destination_changed` (bucketed enum: `macos | ios_simulator | ios_device | tv_{sim,device} | watch_{sim,device} | vision_{sim,device} | unknown`, raw device names never reach RawEvent.payload). **Public LeafCore:** `RunDestinationBucket` enum, `XcresultBuildSummary`/`XcresultTestSummary` types, `XcresultParser`+`DerivedDataWatcher` protocols, `ProviderSnapshotsDerivedDataCursor` impl (single-row JSON-blob persistence в существующей `provider_snapshots` M015 — БЕЗ новой миграции, **M025 reservation released back**), `XcodeBuildLifecycleStateMachine` (4 transitions, first-tick suppression), `XcodeTestRunStateMachine` (LRU dedup cap 256). **Registry: 152 → 158 ShareEventTypeKey**, все 6 default OFF. **ActivityFeedMapper.mapLocalOS** +6 cases (allowlist-only payload reads). **EventKindIcon** +6 SF Symbol mappings. **DispatchCoverageTests #20** parity fence enumerates all 8 `xcode_*` kinds × (registry + defaults + icon + mapper whitelist). **LocalAppsStore.derivedDataWatcherEnabled** toggle. **Privacy walkbacks**: `errors[].message` / `sourceURL` / `destination.deviceName` / `modelName` / test names / build log NEVER stored — `LEAKED_SENTINEL_XCODE_P2` fence в `testXcodeP2_WalkbackSentinel`. **Independent code review**: APPROVE w/ 1 IMPORTANT + 7 nits — all applied. **Moat в worktree (gitignored, cp в main checkout перед merge)**: `ProdXcresultParser` + `ProdDerivedDataWatcher` + `ProdXcodeAdapter` extension. **Still deferred**: `Agent.swift` watcher lifecycle wiring + Settings UI 6 sub-toggles + manual smoke A–G. Spec: `docs/superpowers/specs/2026-05-16-track-6-P2-xcode-deep.md`.

**2026-05-16 — Track-6 P5 (Zoom Deep) landed** на `feature/track-6-P5-zoom-deep-clean` (off `main`). **3 net-new event_kinds** (`zoom_meeting_started` / `_ended` / `_calendar_linked`) layered on Track-4 S2 baseline. **`ZoomMeetingDurationTracker`** coalescer GRACE_START=5s / GRACE_END=30s. **Path A cross-link via EventKit inline** (`ProdZoomCalendarLinker` ±15min window + zoom.us URL regex → SHA256[:16] opaque target_ref) — independent of P4 GCP gate. **PMI L4 redaction** at parser boundary. **EventLinksStore** new branch on `zoom_meeting_started` → `link_kind=zoom_to_calendar_meeting`. **PresenceStateWriter.Provider.zoom**. **ShareEventTypeKey 152 → 155**, all default OFF. **0 new tables / 0 TCC prompts / 0 MCP tools**. **DispatchCoverageTests #21** parity fence. **25 new RelayBodyLeakageTests walkbacks**. Spec: `docs/superpowers/specs/2026-05-16-track-6-P5-zoom-deep.md`.

**2026-05-16 — Track-6 P7 (GPT Cap Documented) landed** на `feature/track-6-P7-gpt-cap`. **Doc-only phase** — 0 LoC, 0 migrations, 0 ShareEventTypeKey delta. Stage 0 research: ChatGPT Desktop ceiling = L1 attention + L2 intensity only. Vendor-blocked: no REST / no AppleScript / no App Intents / no MCP / no hook SDK. "Work with Apps" inbound-only. 4 re-evaluation triggers зафиксированы. Whitepaper: новая секция «AI co-pilot surfaces без per-event API» + changelog. Spec: `docs/superpowers/specs/2026-05-16-track-6-P7-gpt-cap.md`.

**2026-05-16 — Track-6 P6 (IDEs Surface Cap) landed** на `feature/track-6-P6-ides-cap`. **4 new event_kinds**: `vscode_active_doc_changed` (parsed `workspace_name` + `file_basename` via per-fork title parser), `vscode_workspace_opened` (FSEvents on workspaceStorage dirs), `jetbrains_recent_project_observed` (FSEvents on recentProjects*.xml), `ide_window_title_observed` fallback (default OFF, `IDETitlePathSanitizer` strips path tokens). **VSCode-family parser layer** — 4 per-fork parsers (`VSCodeStableParser`, `CursorParser`, `VSCodeInsidersParser`, `VSCodiumParser`) + `VSCodeFamilyDispatcher` + `IDETitlePathSanitizer` in `Insights/Parsers/VSCodeFamily/`. **2 FSEvents watchers** (VSCode workspaceStorage + JetBrains recentProjects) — stubbed lifecycle, wired in Stage 7. **JetBrains bundle list 11 → 13** (−AppCode, +DataGrip/RustRover/DataSpell). VSCodeInsiders added to `.dev` classifier. **IDEStorageFeatureGate** + **LocalAppsStore** IDE toggles + **Settings → System Observers → IDEs sub-section**. **DispatchCoverageTests #16** parity fence + **RelayBodyLeakageTests +5** walkbacks. **0 new SQLCipher tables**, **0 new MCP tools**. Plugin work (per-edit / debugger / terminal / extension list) = Layer D V2, separate track.

**Track-6 integration (complete)** — collective merge P1 + P3 + P4 + P2 + P5 + P7 + P6 on `track-6-integration`. Combined: **195 ShareEventTypeKey** (152 + 16 P1 + 8 P3 + 6 P4 + 6 P2 + 3 P5 + 0 P7 + 4 P6), **31 SQLCipher tables** (M001-M018 + M024 + M026 + M027; M019-M023 reserved Track-5, M025 freed). Не merged в main — стэк ждёт acceptance gate (P1 §12 + P3 §19 + P4 §10 + P2 §A–G + P5 zoom smoke + P6 vscode/JB smoke).

**2026-05-13 — Phase Track-4 S4 (Substrate Integration) landed** на `feature/track-4-S4-substrate-integration` (off Track-4 S3 `b45ef60`).

## Где мы

- **Whitepaper v0.1-beta** (team-first, 2026-05-08) на `leaf-docs.gundem.tech`. Byterover-style структура. Решения — `docs/reference/decisions.md`, OQ — `docs/reference/open-questions.md`.
- **Section A done (Phase 0-2).** Foundation: 3-target Xcode, LeafCore/LeafCorePrivate SPM split, GRDB 7 + SQLCipher, Agent + 4 collectors, Derived Insights Engine, Native UI, stdio MCP.
- **Section B done (Phase 3.0-3.5).** Distribution: Apple Developer ID + notarytool + Sparkle 2 + R2/CF + EdDSA appcast.
- **Layer B MVP closed (Phase 4.1-4.5).** Linear / GitHub / Slack OAuth + polling.
- **Phase 4.6.** Latency depth + Linear status transitions + synthesis. 8 MCP tools.
- **Phase 4.7 A+B+C shipped в alpha.9.** +33 event_kind + `presence_state` M005 + `LinearIDExtractor` + 4 presence-first MCP tools (12 total) + ModeClassifier skeleton + UI redesign. Registry 22 → 43.
- **Phase 4.10.B merged.** Activity tab session enrichment (AX window_title + browser_url + session aggregation + colored category dots). 652 baseline.
- **Phase 5.1 stack closed (2026-05-05).** Schema M006/M007/M008 + `EnvelopeCodec` AES-GCM-256 + `OrgService` + `TeamKeystore` + `OrgReader` + UI. 714 tests.
- **Phase 5.2 stack closed (2026-05-05).** Invite handshake E2E: `IdentityService` X25519 + `InviteKDF` HKDF-SHA256 + `InviteBlobCodec` AES-GCM-256 + leaf-relay `/v1/invite/*` (live) + admin + invitee + AcceptInviteSheet + Onboarding `.team` step. 802 tests.
- **Phase 5.3 stack shipped в alpha.10 + alpha.11 patch.** Member removal + key rotation: M009 `rotation_outbox` + `KeyRotationService` admin + leaf-relay `/v1/key-rotation/*` (live) + peer fetch loop + `MemberRemovalReader` + `RemovedFromTeamBanner`.
- **Phase 5.5 stack in flight (5.5.A/B/C shipped в feature branches, ждут two-Mac smoke gate).** Pending invites lifecycle: M010 `pending_invites` + `PendingInvitesStore` + deep-link routing + scenePhase clipboard auto-fetch + `.team` three-way + admin recall UI. 1007 tests at C.
- **Track 1 stack ready for acceptance gate.** D1 (capture: bodies + attachments + PR metadata, 1055) → D2 (FTS5 + event_links M012/M013, 1108) → D3 (M014 detection tables + DetectorPipeline + QueryEngine + 3 structured MCP tools, **1213**). **28 SQLCipher tables** (M001-M014). **15 MCP tools** (12 low-level + 3 structured). **48 ShareEventTypeKey** (43 baseline + 5 D3 semantic). Стэк merges коллективно после Track 1 acceptance gate (UC1/UC3/UC4/UC5/UC6 smoke).
- **Track 3 stack ready for acceptance gate.** D1 Linear (14 → 32 event_kinds, 1282) → linear-reconciliation hotfix (10 schema-drift bugs via introspection, 1283) → D2 GitHub (21 → 52 event_kinds, first OAuth scope-bump UX, 1464) → D3 Slack (8 → 27 event_kinds, second scope-bump UX, canvas+bookmark FTS, 1597; +moat HTTP Tasks 25-27 + real-Slack smoke, 1684) → D4 cross-cutting (body-kind dispatcher parity fence + `gh_pr_*` regression, **1691**). **28 SQLCipher tables** (M001-M017). **116 ShareEventTypeKey** (97 baseline + 19 D3 new, default OFF). `DispatchCoverageTests` 12 parity + 2 regression. `RelayBodyLeakageTests` 30 walkbacks. Стэк merges коллективно после Track 3 acceptance gate.
- **Track 2 stack ready for acceptance gate.** D1 (token foundation 3-tier T1/T2/T3 + 28 components + 4 templates + TokensPreview `⌘⌥T`, 1213 baseline preserved) → D2 (RootView/Sidebar/Home) → D3 (Activity/Team/Connections in-place migration) → D4 (sheets/Org/Settings/Profile/MenuBar/Onboarding/RemovedFromTeamBanner/ShareTemplateButton, old palette hard-deleted 8 files, **1213 preserved through all 4**). Snapshot Replacement complete — все production view files на едином D1 substrate, zero old-palette compiles. `check-tokens` три-tier: BASE+MIGRATION+RETIRED. Стэк ждёт acceptance gate (AC1-14 manual smoke).
- **Track 4 stack ready for acceptance gate.** S1 (Architecture Catch-up — 4 Layer A collectors: Calendar/Focus/SystemState/Spaces, +9 ShareEventTypeKey 116 → 125, 1715 tests) → S2 (AppleScript Surface — 12 macOS apps, +14 ShareEventTypeKey 125 → 139, 1847 tests, ACCEPT-WITH-NITS) → S3 (System Observers + Intensity — 8 collectors через CoreAudio/CG/NE/CoreWLAN/NSPasteboard/FSEvents/CGEventTap, M018 `intensity_aggregates`, +13 ShareEventTypeKey 139 → 152, 1959 tests, ACCEPT-WITH-NITS) → **S4 (Substrate Integration — 4 FTS body-kind extensions + dispatcher tuple refactor, `mapLocalOS` 33-kind switch + skip-list +3, `EventKindIcon` SF Symbol helper, 0 new tables / 0 new registry entries, 2012 tests, REJECT → fix-bundle reconciled mapper-collector key mismatches)**. Substrate-only через S1+S2+S4; S3 adds 1 new table. `share_event_types` runtime persistence deferred to Phase 5.4. Стэк merges коллективно после Track-4 acceptance gate.
- **Track-6 stack complete, ready for acceptance gate** (P1 + P3 + P4 + P2 + P5 + P7 + P6 on `track-6-integration` off main). **P1 Claude Code Deep**: 2 → 16 event_kinds + subagent capture + hybrid jsonl+hook pipeline + M024 + AIToolsStore. **P3 Browsers Deep**: Safari + Chrome + Arc AppleScript depth + 6 state machines + M026 + per-domain allow-list. **P4 Google Calendar Deep**: PKCE OAuth + 5-min syncToken polling + 6 new kinds + M027 + presence_state composite. **P2 Xcode Deep**: 6 build/test/scheme/destination event_kinds + xcresult parsing + DerivedData FSEvents + M015 reuse (M025 freed). **P5 Zoom Deep**: 3 duration+calendar-link event_kinds + EventKit inline cross-link (Path A) + PMI redaction. **P6 IDEs Surface Cap**: VSCode-family parser layer (4 per-fork parsers + dispatcher + sanitizer) + 2 FSEvents watchers + JetBrains bundle list 11 → 13 + 4 new event_kinds. **P7 GPT Cap**: doc-only won't-list. Combined: **195 ShareEventTypeKey** (152 baseline + 16 P1 + 8 P3 + 6 P4 + 6 P2 + 3 P5 + 0 P7 + 4 P6), **31 SQLCipher tables**. P4 acceptance blocked on GCP setup (2-6 wk wall).
- **Linear** = только таски. Второй мозг = whitepaper.

## Архитектура

Полная картина — `.claude/shared/architecture.md` + whitepaper `leaf-docs/docs/memory-architecture/`. TL;DR: MCP primary surface + Native macOS app + Phase 5 E2E team relay (Cloudflare DO + AES-GCM, миграция на Supabase + XChaCha20-Poly1305 — отдельный track). Layer A (NSWorkspace/CGEventSource/EventKit/Focus/AX/FSEvents/Claude Code hooks/AppleScript + S3: CoreAudio/CG/NE/CoreWLAN/NSPasteboard/CGEventTap intensity + P3: per-domain browser allow-list + P6: VSCode-family title parser + IDE FSEvents watchers), Layer B = Linear+GitHub+Slack, granularity L1-L5, Share Controls (default OFF per ADR-020), SQLCipher M001-M018+M024+M026+M027 (31 таблица на track-6-integration), zero-LLM substrate (whitepaper планирует tier-based summarization отдельным треком), Sparkle 2 + EdDSA + R2 distribution. **15 MCP tools**. **195 ShareEventTypeKey** entries (collective Track-6 merge complete: 152 baseline + 16 P1 + 8 P3 + 6 P4 + 6 P2 + 3 P5 + 4 P6 + 0 P7).

## Следующим

- **Track-9 (Substrate enrichment for YOU·NOW depth — new track, spec TBD)** — задача поставлена 2026-05-18 вечером после Phase 8.4 live smoke: довести YOU·NOW `.active` render до **1-в-1 с master spec §3 mockup** (`Xcode · HomeView.swift / feature/track-8-home · LEAF-204 / 42 min focused · intensity ▮▮▮▯`). Текущее substrate output после AX grant: первая строка contextLabel ✅ работает, остальные 3 строки empty. **Sub-items:** (a) **git polling deriver** — `currentTaskIdentity()` сейчас scrap'ит branch + linearID из window_title regex (Xcode title branch не содержит → всегда nil). Real fix — read git HEAD из workspace path (Track-6 P2 `xcode_active_doc_changed` payload already captures workspace) → walk-up к `.git/HEAD` → branch + `LinearIDExtractor` дают LEAF-NN. (b) **intensity bars** — Track-4 S3 `IntensityAggregator` default OFF (Input Monitoring TCC dual-prompt). Wire через Settings → System Observers → Intensity toggle ON (existing code path работает, нужен только UX hint что для full YOU·NOW рекомендуется enable). (c) **label tweak** — `Started HH:MM · Xm` → `X min focused` для spec соответствия (или гибрид). (d) **`.away` / `.deepWorkFocus` / `.inMeeting` rich enrichment** — соответствующие substrate-side gaps. Spec пишется через `superpowers:brainstorming` когда стартанём; estimate phase: ~1 session.
- **Track-6 P1 acceptance gate (manual smoke per spec §12, A–F)** — Hook install golden path / Live hook flow / Subagent depth / jsonl floor fallback / Privacy walkback / Privacy dashboard. **Ship-prep deferral:** Xcode bundle-embedding bridge binary into `Leaf.app/Contents/MacOS/` для Sparkle before alpha.16.
- **Track-6 P3 acceptance gate (manual smoke per spec §19)** — Safari 3 tabs default / Allow-list activation / Chrome incognito invisibility / Chrome multi-profile bookmark / Safari FDA flow / Privacy walkback grep.
- **Track-6 P4 acceptance gate (manual smoke per spec §10)** — Connect Google Calendar / Bootstrap / Focus block transition / OOO transition / Working location / Cancelled event / Multi-calendar / 410 Gone recovery / Token refresh / Privacy walkback grep. **Blocked on GCP setup**: new `leaf-prod` project + brand + sensitive scope verification (2-6 wk wall pre-approval, 100-user cap until verification clears).
- **Track-6 P6 acceptance gate (manual smoke)** — A. install VSCode, open `~/Desktop/Leaf/leaf`, switch active file → `vscode_active_doc_changed` event with `workspace_name="leaf"` + `file_basename` in DB. B. open a new workspace → `vscode_workspace_opened` event. C. install one JetBrains IDE, open a project → `jetbrains_recent_project_observed` event. D. customize `window.title` in vscode → `ide_window_title_observed` fallback (with toggle enabled in Settings). E. Privacy walkback: `grep -nE "absolute_path|file_contents|debugger_state" Packages/LeafCore/Sources/LeafCore/Insights/Parsers/VSCodeFamily/` → 0 hits. F. Fleet AS-probe: `osascript -e 'tell application "Fleet" to get path of front document'` → document whether AppleScript dictionary is accessible.
- **Track 4 S4 acceptance gate (manual smoke)** — A. FTS search: enable Notes/Zoom/Screenshot/Download → создать note "Sprint Q1 review", Zoom meeting "Track 4 wrap", screenshot "design draft.png", download "report.pdf" → `sqlite3 events.sqlite "SELECT body_kind,body FROM events_fts_meta JOIN events_fts ON rowid=fts_rowid WHERE events_fts MATCH 'Sprint OR Track OR design OR report'"` returns 4 rows under notes_title/zoom_meeting_name/screenshot_filename/download_filename. B. Activity tab: scroll session → "Meeting started"/"Focus on"/"Screen locked"/"Xcode: Foo.swift"/"Music: <track>"/"Notes: <title>"/"Mail: <mailbox>"/"Zoom: in meeting"/"Safari: N tabs"/"Audio route: <category>"/"Mic on/off"/"Display connected"/"VPN: connected"/"Wi-Fi: associated"/"Screenshot: <fn>"/"Download: <fn>"/"Trash emptied" rows render с semantic SF Symbols (no `app.dashed` fallback). C. Privacy walkback: `grep -nE "note_body|preview|email_subject|file_contents" Packages/LeafCore/Sources/LeafCore/Insights/ActivityFeedMapper.swift` → 0 hits. NO rows для `intensity_snapshot` / `intensity_bucket_dropped` / `clipboard_event_count` despite events in DB.
- **Track 4 S3 acceptance gate (manual smoke)** — author's Mac: Settings → "System Observers" → 10 toggles. Intensity OFF default, others ON. Toggle Intensity ON → Input Monitoring TCC dual-prompt (main app `tech.gundem.leaf` + agent `tech.gundem.leaf.agent` после Agent start). Type ~50 keystrokes mid-minute → check DB `intensity_aggregates` row + `intensity_snapshot` event без key content (`sqlite3 events.sqlite "SELECT payload_json FROM events WHERE payload_json LIKE '%intensity%' LIMIT 5"`). Lock screen mid-minute → expect `intensity_bucket_dropped state=locked`, foreground_app absent, counters zeroed. Plug headphones/USB-C dock → expect dedupe'd `audio_route_changed` + `display_connected` без device names. Empty Trash 50+ items → expect ONE `trash_changed action=emptied`. Symlinked screenshot dir (`defaults write com.apple.screencapture location ...`) → screenshot still detected.
- **Track 4 S2 acceptance gate (manual smoke)** — author's Mac: Settings → "Local Apps" → 12 toggles default OFF. Toggle Music ON → TCC под `tech.gundem.leaf.agent` → play track → DB row `music_track_changed` ≤30s. Toggle Xcode/Notes/Mail/Reminders/Safari — verify payloads только allow-listed fields. Mail sub-field gate: master ON + sub OFF → no emit; sub ON → emit `mail_active_mailbox_changed`. Deny → badge "Denied" + "Open Settings" CTA → deep-link. 24h denial cache → no re-prompt. `otool -P LeafAgent | grep NSAppleEventsUsageDescription`.
- **Track 4 S1 acceptance gate (manual smoke)** — Calendar TCC dual-prompt → schedule fake event → `meeting_state_entered/_exited`. Focus toggle → `focus_mode_enabled/_disabled`. `pmset displaysleepnow` → `system_slept`+`_locked`; wake → `_unlocked`+`_woke`. Spaces swipe → `space_switched` (coalescer drops rapid sweeps). Privacy walkback: payloads ONLY `event_kind` + `state`.
- **Track 3 acceptance gate** — manual smoke на real workspaces: D1 (comment reaction / @-mention / cycle start / 4am cold), D2 (star repo / revoke `read:project` → reader transitions → re-authorize clears banner), D3 (pin message / revoke `pins:read` → reauth flow / 4am cold canvas+emoji+usergroups), D4 (FTS query hits + cross-provider linker LEAF-NN refs в gist descriptions/release bodies/Slack canvas titles). После — collective merge стэка в `main` + whitepaper sync.
- **Track 2 acceptance gate** — AC1-14 manual smoke на собственных рабочих данных. `⌘⌥T` Tokens Preview / 3 Sheets / OrganizationView 4 OrgReader states / WindowSettingsView 3 sub-sections / ProfileView / RemovedFromTeamBanner / MenuBarContent 360pt + native Quit / Onboarding 5-step + 4-substep / ShareTemplateButton 3 LeafButton / retirement compile-time enforce / `just check-tokens` / 5/5 schemes / 1213 SPM. После — collective merge + whitepaper sync per OQ-29 (public-safe framing).
- **Track 1 acceptance gate** — UC1/UC3/UC4/UC5/UC6 smoke через MCP, coherent JSON в 64KB budget, Claude Code narrative ≤3s. После — collective merge стэка D1+D2+D3 в `main` + whitepaper sync per контракт §12.
- **Track 3 D3 follow-ups** (carry to acceptance gate): (a) AC count reconciliation OQ-D3-6 (19 implemented vs contract AC "14"). (b) `slack_member_channels_top10` configurable N для v1.1. (c) Slack reactions duplicate-emission (~50/tick — needs `(itemRef, emoji_bucket)` dedupe vs prior snapshot). (d) Cold bootstrap race (cold tick #1 before warm tick #1 → per-channel fan-out skips).
- **D3 known issue** — D2 FTS dispatcher fixed (Track 1 D3 cleaned 3 dispatchers). `BodyKind` typed enum vs `Schema.BodyKinds` string — нет round-trip test. Body-dispatch logic дублирована 3 раза — extract `BodyExtractor` walker before 4-го caller.
- **D3 LinearStuck status filter** — canonical `WorkflowState.name`; custom statuses не detect. v1.1 — switch к `WorkflowState.type`.
- **Phase 5.5 two-Mac smoke gate** — manual end-to-end 5.5.A+B+C на двух Mac'ах (full 7-step golden path + 5.5.C 8-step) перед collective merge.
- **Phase 5.5.D candidates** (post-smoke): `inviteeDisplayNameHint` capture / per-row [Refresh] / "Joined N min ago" ghost row / bulk Revoke-all-expired / centralize transient-error pattern.
- **Phase 5.6** — auto-poll loop через HEAD `/v1/invite/<token>` (требует `gundemtech/leaf-relay` extension); background scheduler.
- **Phase 5.4** (parallel) — `presence_outgoing` + `presence_history` M011/M012 + WS broadcast в Agent + Swift WS client + Team presence grid live UI; first real consumer `ProdEnvelopeCodec` 5.1.C.
- **Phase 4.7 carry-overs** (non-blocking): hoist GitHubCollector UTC `DateFormatter` to static; split `GitHubAPIProvider.swift`; document `fetchEvents` failure short-circuit; refactor 4.7.C-8 piggy-back на separate HTTP call если Linear rejects whole query на legacy workspaces.
- **Phase 4.8 — absorbed by Track 1 D1.** Standalone закрыт.
- **Phase 4.9 — Derived modes (not designed).** `DefaultModeClassifier` impl поверх 4.7.C skeleton, `mode_history` table, новые MCP tools.
- **Layer C (V1.5+)**: MCP-aggregator поверх Notion / Figma / Jira / Gmail / Calendar.
- **Cleanup**: delete `KeychainKeyStore.swift` + `LeafError.keychainUnavailable` после ~2 недель stable runtime. leaf-relay README + CI deploy hook.
- **Release tooling**: (a) `release.sh` auto-bump `CURRENT_PROJECT_VERSION` или fail-fast; (b) version-aware stamps в `build/releases/.stamps/`.
- **Sparkle ship gotchas**: (a) Xcode Debug LeafAgent залипает в launchd → `pkill -f "DerivedData.*LeafAgent"` pre-ship; (b) AX permission иногда требует toggle после bundle replace (CDHash меняется); (c) `CFBundleVersion` monotonic per ship; (d) dev-only: `/Applications/Leaf.app` ↔ DerivedData сбрасывает BTM disposition.

## Open tensions

Substrate-level (технические, не в whitepaper):
- **OT-1** distributed deletion локальной forever-history у ex-teammates.
- **OT-2** storage compression для forever retention.

Стратегические open questions (whitepaper) — `leaf-docs/docs/reference/open-questions.md`.
